<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://avchauzov.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://avchauzov.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-10-06T15:00:22+00:00</updated><id>https://avchauzov.github.io/feed.xml</id><title type="html">Andrey Chauzov</title><subtitle>Senior AI Architect with 7+ years building enterprise-scale AI solutions, specializing in NLP, MLOps, and intelligent infrastructure. Building the future of AI systems that scale. </subtitle><entry><title type="html">Hybrid Retrieval with RRF: Solving the Score Normalization Problem</title><link href="https://avchauzov.github.io/blog/2025/hybrid-retrieval-rrf-rank-fusion/" rel="alternate" type="text/html" title="Hybrid Retrieval with RRF: Solving the Score Normalization Problem"/><published>2025-10-06T00:00:00+00:00</published><updated>2025-10-06T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/hybrid-retrieval-rrf-rank-fusion</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/hybrid-retrieval-rrf-rank-fusion/"><![CDATA[<p>Vector search excels at semantic understanding but fails on exact matches: product codes, error messages, and abbreviations. The obvious solution is combining BM25 lexical scores with vector similarity. The problem? Their scores are incompatible, and naive normalization can even make results worse.</p> <p>Reciprocal Rank Fusion (RRF) solves this by ignoring scores entirely and using <strong>document ranks</strong> instead. It’s simple, requires minimal tuning, and works effectively in production.</p> <h2 id="why-weighted-averaging-fails">Why Weighted Averaging Fails</h2> <p>The naive approach to hybrid search uses weighted combination of scores:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">combined_score</span> <span class="o">=</span> <span class="n">alpha</span> <span class="o">*</span> <span class="n">vector_score</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha</span><span class="p">)</span> <span class="o">*</span> <span class="n">normalized_bm25_score</span>
</code></pre></div></div> <p>This approach is flawed because BM25 and cosine similarity scores exist in different spaces and, more importantly, come from <strong>different distributions</strong>. BM25 scores are unbounded, while cosine similarity lives in the <code class="language-plaintext highlighter-rouge">[0, 1]</code> range.</p> <p>Consider a concrete example with three documents:</p> <table> <thead> <tr> <th>Document</th> <th>BM25 Score</th> <th>BM25 Rank</th> <th>Norm. BM25</th> <th>Vector Score</th> <th>Vector Rank</th> <th>Combined (α=0.5)</th> <th>Final Rank</th> </tr> </thead> <tbody> <tr> <td>Doc A</td> <td>15.2</td> <td>#1</td> <td>1.0</td> <td>0.73</td> <td>#3</td> <td><strong>0.865</strong></td> <td><strong>#1</strong></td> </tr> <tr> <td>Doc B</td> <td>4.8</td> <td>#3</td> <td>0.0</td> <td>0.91</td> <td>#1</td> <td>0.455</td> <td>#3</td> </tr> <tr> <td>Doc C</td> <td>8.1</td> <td>#2</td> <td>0.33</td> <td>0.85</td> <td>#2</td> <td>0.590</td> <td>#2</td> </tr> </tbody> </table> <p><strong>Doc B</strong> ranks #1 in vector search (most semantically relevant) and #3 in BM25. <strong>Doc A</strong> ranks #1 in BM25 but #3 in vector search. After weighted averaging with <code class="language-plaintext highlighter-rouge">α=0.5</code>, <strong>Doc A wins the final ranking</strong> despite being semantically weakest.</p> <p>The problem: normalization aligns score ranges but can’t fix the <strong>distribution mismatch</strong>. When BM25 produces one extreme outlier (15.2 vs 8.1 vs 4.8), Min-Max normalization compresses the middle documents into a narrow band. The vector component’s distinctions (0.91 vs 0.85 vs 0.73) get overwhelmed by BM25’s normalized spread (1.0 vs 0.33 vs 0.0).</p> <p>You could tune <code class="language-plaintext highlighter-rouge">alpha</code> to balance the components, but the <strong>unboundedness</strong> of BM25 distribution makes it extremely challenging. As a result, any chosen <code class="language-plaintext highlighter-rouge">alpha</code> will be suboptimal for many queries.</p> <h2 id="rrf-ranks-instead-of-scores">RRF: Ranks Instead of Scores</h2> <p>RRF approaches the problem from a different angle: it switches from scores to ranks and calculates a fusion score based on them:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">RRF_score</span><span class="p">(</span><span class="n">d</span><span class="p">)</span> <span class="o">=</span> <span class="n">Σ</span> <span class="mi">1</span> <span class="o">/</span> <span class="p">(</span><span class="n">k</span> <span class="o">+</span> <span class="nf">rank</span><span class="p">(</span><span class="n">d</span><span class="p">))</span>
</code></pre></div></div> <p>Here, <code class="language-plaintext highlighter-rouge">rank(d)</code> is the document’s position in each ranked list (starting from 1), and <code class="language-plaintext highlighter-rouge">k</code> is a smoothing constant (typically 60).</p> <p><strong>Example</strong>: Consider how RRF weighs different results. Suppose we have two search systems (BM25 and Vector) and two documents, A and B.</p> <ul> <li><strong>Document A</strong> is ranked #1 by BM25 (a perfect keyword match) but is only ranked #25 by the vector search (semantically less relevant).</li> <li><strong>Document B</strong> is ranked #5 by both BM25 and vector search, showing consistent relevance.</li> </ul> <p>From a user perspective, we’d prefer to see consistently relevant results (even if not perfect by one criterion) rather than a result that excels in one dimension but fails in another. Which one should be ranked higher? Let’s calculate their scores with <code class="language-plaintext highlighter-rouge">k=60</code>:</p> <ul> <li> <p>Score for <strong>Document A</strong>: <code class="language-plaintext highlighter-rouge">1/(60 + 1) + 1/(60 + 25) = 1/61 + 1/85 ≈ 0.0164 + 0.0118 ≈ 0.0282</code></p> </li> <li> <p>Score for <strong>Document B</strong>: <code class="language-plaintext highlighter-rouge">1/(60 + 5) + 1/(60 + 5) = 2/65 ≈ 0.0308</code></p> </li> </ul> <p>In this scenario, <strong>Document B wins</strong>. This is the desired outcome.</p> <p>This demonstrates the core principle of RRF: it values reliable <strong>consensus</strong> over a single, brilliant-but-contradictory result.</p> <p>Why does this matter? This mechanism makes your search results more <strong>robust</strong>. It prevents situations where a document that is good on only one dimension (e.g., perfect keywords but wrong meaning) incorrectly bubbles up to the top. RRF acts as a smart tie-breaker that trusts the agreement between your different search methods.</p> <p><strong>Why this works</strong>:</p> <ul> <li><strong>Score-agnostic</strong>: A rank is a universal measure, whether it comes from a BM25 score of 8.3 or a cosine similarity of 0.9.</li> <li><strong>Consensus-driven</strong>: Documents ranking high across multiple systems are a strong signal of true relevance.</li> <li><strong>Smoothing</strong>: The constant <code class="language-plaintext highlighter-rouge">k</code> (e.g., 60) prevents extreme bias toward top-ranked results. While it’s tunable, the default often works well.</li> </ul> <h2 id="when-to-use-hybrid-retrieval">When to Use Hybrid Retrieval</h2> <p>Hybrid search isn’t always necessary. Use it when <strong>exact matches matter</strong>, such as for product IDs, error codes, legal citations, or domain-specific terms. Vector embeddings alone are not well-suited for these tasks.</p> <p>It’s also crucial to <strong>validate both retrieval paths independently</strong>. If one path consistently returns low-quality results, fixing that retriever is more important than choosing a fusion method.</p> <h2 id="implementation-from-scratch">Implementation (from scratch)</h2> <p>RRF is integrated into many vector databases, but let’s implement it from scratch to understand the mechanics.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span><span class="p">,</span> <span class="n">Dict</span><span class="p">,</span> <span class="n">Tuple</span>

<span class="k">def</span> <span class="nf">reciprocal_rank_fusion</span><span class="p">(</span>
    <span class="n">search_results_dict</span><span class="p">:</span> <span class="n">Dict</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="n">List</span><span class="p">[</span><span class="nb">str</span><span class="p">]],</span>
    <span class="n">k</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">60</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="n">List</span><span class="p">[</span><span class="n">Tuple</span><span class="p">[</span><span class="nb">str</span><span class="p">,</span> <span class="nb">float</span><span class="p">]]:</span>
    <span class="sh">"""</span><span class="s">
    Perform Reciprocal Rank Fusion on multiple ranked lists.

    Args:
        search_results_dict: Dictionary mapping retrieval method name
                           to ranked list of document IDs
        k: Smoothing constant (default: 60)

    Returns:
        List of (doc_id, rrf_score) tuples, sorted by score descending
    </span><span class="sh">"""</span>
    <span class="n">rrf_scores</span> <span class="o">=</span> <span class="p">{}</span>

    <span class="k">for</span> <span class="n">retriever_name</span><span class="p">,</span> <span class="n">doc_ids</span> <span class="ow">in</span> <span class="n">search_results_dict</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
        <span class="k">for</span> <span class="n">rank</span><span class="p">,</span> <span class="n">doc_id</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">doc_ids</span><span class="p">,</span> <span class="n">start</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
            <span class="k">if</span> <span class="n">doc_id</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">rrf_scores</span><span class="p">:</span>
                <span class="n">rrf_scores</span><span class="p">[</span><span class="n">doc_id</span><span class="p">]</span> <span class="o">=</span> <span class="mf">0.0</span>
            <span class="n">rrf_scores</span><span class="p">[</span><span class="n">doc_id</span><span class="p">]</span> <span class="o">+=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="n">k</span> <span class="o">+</span> <span class="n">rank</span><span class="p">)</span>

    <span class="c1"># Sort by RRF score descending
</span>    <span class="n">sorted_results</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span>
        <span class="n">rrf_scores</span><span class="p">.</span><span class="nf">items</span><span class="p">(),</span>
        <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span>
        <span class="n">reverse</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>

    <span class="k">return</span> <span class="n">sorted_results</span>


<span class="c1"># Example usage
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># Simulated search results from two retrievers
</span>    <span class="n">search_results</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">bm25</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">doc1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc3</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc5</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc2</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc4</span><span class="sh">"</span><span class="p">],</span>
        <span class="sh">"</span><span class="s">vector</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">doc2</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc4</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc5</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">doc3</span><span class="sh">"</span><span class="p">]</span>
    <span class="p">}</span>

    <span class="n">fused_results</span> <span class="o">=</span> <span class="nf">reciprocal_rank_fusion</span><span class="p">(</span><span class="n">search_results</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">60</span><span class="p">)</span>

    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">RRF Results:</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">doc_id</span><span class="p">,</span> <span class="n">score</span> <span class="ow">in</span> <span class="n">fused_results</span><span class="p">[:</span><span class="mi">3</span><span class="p">]:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">doc_id</span><span class="si">}</span><span class="s">: </span><span class="si">{</span><span class="n">score</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Key Considerations</strong>:</p> <ul> <li><strong>Retrieval Window</strong>: It’s good practice to fetch more results from each retriever than you ultimately need (e.g., 2x your target). This gives RRF a larger pool of candidates to fuse and re-rank.</li> <li><strong>The k parameter</strong>: While <code class="language-plaintext highlighter-rouge">k=60</code> is a balanced default, you can experiment. A lower <code class="language-plaintext highlighter-rouge">k</code> (e.g., 10-20) gives more weight to top ranks, while a higher <code class="language-plaintext highlighter-rouge">k</code> (e.g., 80-100) flattens the curve, giving deeper results more influence.</li> </ul> <h2 id="production-results">Production Results</h2> <p>Based on published benchmarks and case studies, hybrid retrieval with RRF typically shows:</p> <p><strong>Quality improvements</strong>:</p> <ul> <li>Recall on queries with specific identifiers: +25-30%</li> <li>Overall recall improvement: up to 30% compared to single-method retrieval</li> <li>NDCG@k improvements: hybrid search consistently ranks highest across benchmark datasets</li> </ul> <p><strong>Latency characteristics</strong>:</p> <ul> <li>Pure vector search: ~55ms (p50)</li> <li>Hybrid search with RRF: ~65ms (p50), ~90ms (p95)</li> <li>Fusion overhead: ~10ms</li> </ul> <p>The latency increase comes primarily from running two retrieval paths. Parallel execution keeps this overhead manageable.</p> <h2 id="the-full-architecture">The Full Architecture</h2> <p>RRF often serves as a key component in a multi-stage retrieval cascade. For example:</p> <ol> <li><strong>Stage 1: Retrieval</strong>. Fetch candidates using hybrid search (e.g., 128 from BM25 + 128 from Vector) → up to 256 candidates.</li> <li><strong>Stage 2: Fusion</strong>. Apply RRF to the candidate pool → 32 finalists.</li> <li><strong>Stage 3: Reranking</strong>. Use a powerful cross-encoder to rerank the finalists → 8 final results.</li> </ol> <h2 id="the-core-principle">The Core Principle</h2> <p>RRF solves the score normalization problem that breaks simple weighted averaging. When both retrieval methods produce reasonable results independently, RRF’s rank-based fusion effectively surfaces the most relevant documents through consensus.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Pure vector search isn't always enough. Weighted averaging of BM25 and vector scores breaks due to incompatible scales. Reciprocal Rank Fusion solves this by using ranks instead of scores.]]></summary></entry><entry><title type="html">LLM Orchestration: A Pragmatic Guide to Complexity</title><link href="https://avchauzov.github.io/blog/2025/llm-orchestration-pragmatic-guide/" rel="alternate" type="text/html" title="LLM Orchestration: A Pragmatic Guide to Complexity"/><published>2025-10-02T00:00:00+00:00</published><updated>2025-10-02T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/llm-orchestration-pragmatic-guide</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/llm-orchestration-pragmatic-guide/"><![CDATA[<p>In my experience, <strong>the vast majority of production LLM apps are simple Chains or Routers</strong>. Many developers are building complex, token-burning state machines for tasks that don’t require them, and then spend even more tokens debugging them.</p> <p>This is a pragmatic guide to choosing the right orchestration pattern.</p> <h3 id="the-spectrum-of-complexity">The Spectrum of Complexity</h3> <ul> <li><strong>Chain</strong>: A fixed <code class="language-plaintext highlighter-rouge">A → B → C</code> flow. The simplest way to link LLM calls or functions.</li> <li><strong>Router</strong>: An <code class="language-plaintext highlighter-rouge">if/else</code> gateway. A conditional step that directs input to the appropriate Chain or tool.</li> <li><strong>Graph</strong>: A stateful workflow with loops. Nodes are steps (LLM calls, tools) and edges are the paths between them, allowing for cycles and retries.</li> <li><strong>Agent</strong>: The “brain” or reasoning strategy (e.g., ReAct) that decides what to do next. It is often <strong>implemented using a graph structure</strong> to manage its execution.</li> </ul> <h3 id="a-pragmatic-decision-framework">A Pragmatic Decision Framework</h3> <h4 id="use-a-chain-when">Use a <strong>Chain</strong> when:</h4> <ul> <li>The workflow is <strong>linear and predictable</strong>.</li> <li><strong>Example</strong>: Retrieve documents → Generate an answer → Format the output.</li> </ul> <h4 id="use-a-router-when">Use a <strong>Router</strong> when:</h4> <ul> <li>You need to handle <strong>diverse requests</strong> or <strong>optimize costs</strong>.</li> <li><strong>Example</strong>: Classify a user query. Route a simple “refund policy” request to a fast, cheap model, but route a complex “enterprise API integration” query to a more capable model.</li> </ul> <h4 id="use-a-graph-when">Use a <strong>Graph</strong> when:</h4> <ul> <li>You need <strong>loops and retry logic</strong>. If a tool fails, the graph can loop back and try again with different parameters.</li> <li>You require <strong>state management</strong> across multiple steps of a long-running task.</li> <li><strong>Example</strong>: A SQL generation loop: <code class="language-plaintext highlighter-rouge">Generate SQL → Execute → (On Error) Loop back to 1 with feedback | (On Success) Format and return</code>.</li> </ul> <h4 id="use-an-agent-when">Use an <strong>Agent</strong> when:</h4> <ul> <li>You <strong>cannot predict the sequence of steps in advance</strong>. The task is open-ended and requires dynamic planning.</li> <li>The LLM needs to <strong>decide which tools to call and in what order</strong> based on intermediate results.</li> <li><strong>Example</strong>: A research agent that iteratively searches the Web, parses pages, synthesizes findings, and decides on its next search query.</li> </ul> <h3 id="the-real-cost-of-complexity">The Real Cost of Complexity</h3> <p>More complexity means higher operational costs. Based on research, the trade-offs are clear:</p> <hr/> <table> <thead> <tr> <th>Pattern</th> <th>Latency</th> <th>Cost Profile</th> <th>Debugging</th> </tr> </thead> <tbody> <tr> <td><strong>Chain</strong></td> <td>Low</td> <td>Baseline</td> <td>Easy</td> </tr> <tr> <td><strong>Router</strong></td> <td>Low</td> <td>-85% (in some cases, by routing to cheaper models)</td> <td>Easy</td> </tr> <tr> <td><strong>Graph</strong></td> <td>Moderate-High</td> <td>+Linear</td> <td>Hard</td> </tr> <tr> <td><strong>Agent / Multi-Agent Systems</strong></td> <td>High (50s+)</td> <td><strong>3.7x+</strong></td> <td>Very Hard</td> </tr> </tbody> </table> <hr/> <h3 id="the-pragmatic-migration-path">The Pragmatic Migration Path</h3> <p>Don’t start with complexity. Evolve based on need.</p> <ol> <li><strong>Always start with a Chain.</strong> It’s simple, cheap, and easy to debug.</li> <li>Add a <strong>Router</strong> when you notice cost or quality anomalies for certain requests that could be handled by different models or logic.</li> <li>Move to a <strong>Graph</strong> only when your Chain or Router frequently fails on edge cases that require <strong>retries or validation loops</strong>.</li> <li>Use an <strong>Agent</strong> only when the task is truly unpredictable.</li> </ol> <p><strong>A simple test</strong>: If you can express the entire workflow using <code class="language-plaintext highlighter-rouge">if/else</code> and <code class="language-plaintext highlighter-rouge">for</code> loops, stick with a Chain or Router (at most a Graph). Only reach for an Agent when even the <em>sequence</em> of steps is unpredictable.</p> <p align="center"> <i>Don't add an agent because it's trendy. Add one only when your Chain, Router, or Graph has failed repeatedly.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Most production apps are simple chains, yet everyone is building agents. Here’s a clear framework on when you really need loops, graphs, and agents in your LLM app.]]></summary></entry><entry><title type="html">How Qdrant’s Scalar Quantization Cut Our RAG Latency by 3x</title><link href="https://avchauzov.github.io/blog/2025/qdrant-quantization-cuts-rag-latency/" rel="alternate" type="text/html" title="How Qdrant’s Scalar Quantization Cut Our RAG Latency by 3x"/><published>2025-09-28T00:00:00+00:00</published><updated>2025-09-28T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/qdrant-quantization-cuts-rag-latency</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/qdrant-quantization-cuts-rag-latency/"><![CDATA[<p>I was building a RAG system for an internal technical documentation search: API references, architecture guides, and troubleshooting docs. The database had around 400k chunks embedded with <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> (3072 dimensions). The system worked, but it was slow and expensive. We also had plans to add more data, so we needed to push some limits.</p> <h3 id="the-bottleneck">The Bottleneck</h3> <p>Median retrieval latency was <strong>~900ms</strong>. Such high baseline latency made more advanced RAG techniques impractical, like query augmentation or HyDE, which require multiple database lookups and would further increase response times. Infrastructure costs were high as well, considering future plans to increase system complexity (and, therefore, costs).</p> <h3 id="what-i-tried-first">What I Tried First</h3> <p>My first approach was predictable: lower the <code class="language-plaintext highlighter-rouge">hnsw_ef</code> parameter (i.e., make the HNSW algorithm perform search less thoroughly). Median latency dropped 2x, but the Recall@10 metric fell from <strong>0.93 to 0.79</strong>. For downstream generation, this was an unacceptable loss in quality, as critical chunks were missing from the context.</p> <p>The second idea was to reduce vector dimensionality. OpenAI’s <code class="language-plaintext highlighter-rouge">text-embedding-3-large</code> model natively supports this via the <code class="language-plaintext highlighter-rouge">dimensions</code> parameter. However, we had plans to experiment with other models and implement multi-model approaches, so we needed an <strong>infrastructure-level solution</strong>.</p> <h3 id="the-solution">The Solution</h3> <p>After several iterations of parameter tuning, I looked for a different approach. While reading the Qdrant documentation, I found <strong>scalar quantization with a hybrid storage setup</strong>. The idea is simple: compressed, quantized vectors (converted to <code class="language-plaintext highlighter-rouge">int8</code> format) are kept in <strong>RAM for ultrafast initial search</strong>, while the original, full-precision vectors are kept on <strong>disk for accurate rescoring</strong> of the top candidates.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">collection_config</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">vectors</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">3072</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">distance</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Cosine</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">on_disk</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span>  <span class="c1"># Originals on SSD
</span>    <span class="p">},</span>
    <span class="sh">"</span><span class="s">quantization_config</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">scalar</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">int8</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">quantile</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.99</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">always_ram</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span>  <span class="c1"># Quantized vectors always in RAM
</span>        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="n">search_params</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">hnsw_ef</span><span class="sh">"</span><span class="p">:</span> <span class="mi">128</span><span class="p">,</span>  <span class="c1"># Back to the original value
</span>    <span class="sh">"</span><span class="s">quantization</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">rescore</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">,</span>  <span class="c1"># Enable rescoring to preserve accuracy
</span>        <span class="sh">"</span><span class="s">oversampling</span><span class="sh">"</span><span class="p">:</span> <span class="mf">2.0</span>  <span class="c1"># Fetch 2x more candidates to compensate for quantization errors
</span>    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>To prevent the disk from becoming a new bottleneck, I also enabled asynchronous I/O in the Qdrant configuration:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">QDRANT__STORAGE__PERFORMANCE__ASYNC_SCORER</span><span class="o">=</span><span class="nb">true</span>
</code></pre></div></div> <p>The quantized vectors sit in RAM, enabling a quick preliminary search. At this stage, Qdrant finds a group of potentially relevant documents. Then, Qdrant <strong>asynchronously loads only the original, full-precision vectors</strong> from the SSD for this small group of candidates and performs a final, accurate relevance calculation. This way, the expensive operations on full vectors are performed on a tiny subset of the data, having almost no impact on overall latency.</p> <h3 id="the-results">The Results</h3> <ul> <li><strong>Median Latency</strong>: 900ms → 300ms (3x ↓)</li> <li><strong>RAM usage</strong>: 26GB → 7GB (3.7x ↓)</li> <li><strong>Monthly Costs ($)</strong>: ~750 → ~260 (65% ↓)</li> <li><strong>Recall@10</strong>: 0.93 → 0.91 (minor decline)</li> </ul> <h3 id="the-main-takeaway">The Main Takeaway</h3> <p>When debugging algorithm performance, we often focus on a subset of parameters while treating everything else as “good defaults”. The real wins come from questioning <strong>all assumptions at once</strong> - algorithm, storage, memory architecture. Nothing is optimal until you prove it is.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[A deep dive into how we cut RAG retrieval latency by 3x and costs by 65% using Qdrant's scalar quantization and a hybrid storage strategy, without sacrificing search quality.]]></summary></entry><entry><title type="html">Why Vision-Language Models Ignore Visual Evidence (And How to Fix It)</title><link href="https://avchauzov.github.io/blog/2025/vlm-context-bias-case-study/" rel="alternate" type="text/html" title="Why Vision-Language Models Ignore Visual Evidence (And How to Fix It)"/><published>2025-09-21T00:00:00+00:00</published><updated>2025-09-21T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/vlm-context-bias-case-study</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/vlm-context-bias-case-study/"><![CDATA[<h3 id="tldr">TL;DR</h3> <p>Vision-Language Models (VLMs) suffer from a powerful contextual bias. They prefer to make “logical” inferences based on the overall scene rather than analyzing specific visual evidence. We found a simple fix: explicitly instruct the model to ignore the context.</p> <h3 id="the-problem">The Problem</h3> <p>I was using NVIDIA’s Vila model to detect idle workers in a factory setting. My initial prompt was detailed: I provided context (a description of the scene), an explanation of what constituted ‘work’ in this environment, and specific visual criteria for idleness, such as ‘a person standing still, with no hand movement.’</p> <p>Despite this detailed instruction, the model consistently failed in obvious cases. The context I provided to help the model was actually making it perform worse.</p> <p>Here’s a breakdown of its flawed reasoning versus the ground truth:</p> <ul> <li><strong>✅ Model’s Reasoning</strong>: “This is a worker wearing a uniform inside a factory → therefore, the worker must be working.”</li> <li><strong>❌ Reality</strong>: The person is standing still and yawning, clearly idle.</li> </ul> <p>The model was so confident in the context (“worker in a factory”) that it completely disregarded the visual evidence that contradicted its assumption.</p> <h3 id="the-root-of-the-problem">The Root of the Problem</h3> <p>VLMs suffer from a critical perception bottleneck that stems from three core issues:</p> <ol> <li><strong>Statistical Shortcuts in Training Data</strong>: The model learns from massive datasets where the combination of “worker uniform” + “factory setting” is overwhelmingly correlated with the label “working.” There are few, if any, training examples of a “worker in uniform not working,” creating a powerful statistical prior.</li> <li><strong>Visual Encoder Detail Loss</strong>: Encoders based on architectures like CLIP are excellent at capturing high-level semantics (“worker,” “factory,” “uniform”). However, they often lose the fine-grained details necessary for this task, such as precise body-part pose, micro-movements, or the direction of a person’s gaze.</li> <li><strong>Logical Errors from Flawed Inputs</strong>: The Large Language Model (LLM) component of the VLM applies powerful, internally consistent logic, but it does so based on the fuzzy, high-level, and sometimes inaccurate data it receives from the visual encoder. The reasoning is sound, but the premise is wrong.</li> </ol> <h3 id="the-solution">The Solution</h3> <p>A single, simple instruction in the prompt fixed nearly all of our false negatives:</p> <blockquote> <p>“Ignore scene context, clothing, and equipment. Focus ONLY on body pose and hand movement.”</p> </blockquote> <p>This prompt forces the model to discard its powerful contextual priors and base its judgment solely on the specific visual evidence requested.</p> <h3 id="why-it-works">Why It Works</h3> <p>This instruction acts as a form of <strong>causal intervention</strong>. It effectively blocks the model’s default reasoning path, which relies on indirect contextual clues. By telling it what <em>not</em> to look at, we force it down a different path—one that relies only on the direct visual evidence of body posture and movement. You are isolating the direct visual effect from the confounding, indirect contextual effect.</p> <h3 id="practical-takeaways">Practical Takeaways</h3> <p>For anyone building production-ready VLM systems, especially for analytical tasks, these lessons are critical:</p> <ol> <li><strong>Explicitly Ignore Context</strong>: When precision matters more than a narrative description, it’s worth adding a negative constraint that tells the model to “ignore the context” and focus on specific visual features.</li> <li><strong>Use Visual Prompts</strong>: Guide the model’s attention by using bounding boxes or other visual cues to designate the exact area of focus.</li> <li><strong>Add “Hard Negatives” to Fine-Tuning</strong>: If you are fine-tuning a model, enrich your dataset with examples that directly contradict common sense priors. For instance, include images of “a worker (X) in a factory context (Y), but who is NOT doing a work-related action (Z).”</li> </ol> <p>Remember: Modern VLMs are less like genuine seeing systems and more like <strong>reasoning engines with a visual input</strong>. They perceive the world through the heavy filter of textual correlations learned from their training data. To get accurate results, you have to actively guide them away from these cognitive shortcuts.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[VLMs have a strong contextual bias, prioritizing 'logical' conclusions over visual facts. We fixed this in a production case by explicitly telling the model to ignore what it thought it knew.]]></summary></entry><entry><title type="html">Our Agents Argued Endlessly. Here’s How a Hybrid AI Pattern Tamed LLM Chaos.</title><link href="https://avchauzov.github.io/blog/2025/hybrid-ai-pattern-for-high-stakes-ranking/" rel="alternate" type="text/html" title="Our Agents Argued Endlessly. Here’s How a Hybrid AI Pattern Tamed LLM Chaos."/><published>2025-09-14T00:00:00+00:00</published><updated>2025-09-14T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/hybrid-ai-pattern-for-high-stakes-ranking</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/hybrid-ai-pattern-for-high-stakes-ranking/"><![CDATA[<p>Building multi-agent systems for high-stakes domains has taught me a crucial lesson: autonomy is a double-edged sword. While LLM-powered agents excel at understanding complex data, their non-deterministic nature can lead to chaos when predictable outcomes are non-negotiable. This is the story of a Proof-of-Concept where our agents got stuck in endless debate, and how we solved it not by making the agents “smarter,” but by implementing a robust, hybrid AI pattern.</p> <h2 id="why-medical-ranking-is-a-high-stakes-game">Why Medical Ranking is a High-Stakes Game</h2> <p>The goal of our PoC was to rank medical drugs based on a variety of data sources. In a domain like medicine, the stakes are incredibly high. An unstable ranking that changes with each run is not just an inconvenience; it’s a critical failure. A system that produces a “plausible-sounding but wrong” order of recommendations could have serious consequences. Therefore, our primary success criteria were not just accuracy, but also <strong>stability, auditability, and predictability</strong>. The final output had to be deterministic and its logic easily traceable.</p> <h2 id="the-architecture-a-structured-multi-agent-workflow">The Architecture: A Structured Multi-Agent Workflow</h2> <p>Our initial architecture was a modular graph built with LangGraph. It was not a simple hierarchy, but a structured workflow designed for clarity and control:</p> <ol> <li>A <strong>Reviewer Agent</strong> would receive the initial query (e.g., “Find treatments for Type 2 Diabetes”).</li> <li>It would then dispatch tasks to multiple, specialized <strong>Model Context Protocol (MCP) Agents</strong>. Each of these agents was responsible for a specific data source, using the <strong>MCP to access medical knowledge bases</strong>, clinical trial results, and FDA databases — not for inter-agent communication.</li> <li>Finally, a <strong>Summarizer Agent</strong> would collect the structured outputs from all MCP agents to synthesize and rank the final list.</li> </ol> <p>The orchestration was handled by LangGraph; the problem emerged within the logical “brain” of the Summarizer.</p> <h2 id="the-anatomy-of-a-loop-when-pure-reasoning-fails">The Anatomy of a Loop: When Pure Reasoning Fails</h2> <p>Our first implementation gave the Summarizer agent autonomy to reason about the collected data and determine the best ranking. The result was a catastrophic failure. The agents were effectively <strong>arguing in circles</strong>, trapped in an endless debate with no resolution.</p> <p>In initial tests, the reasoning-first agent often <strong>exceeded 50+ iterations without converging</strong>, cycling through similar top candidates. The financial cost was staggering:</p> <ul> <li><strong>~50 iterations</strong> × 4 agents × ~1,000 tokens/call = <strong>~200,000 tokens</strong></li> <li>Using <strong>gpt-4.1-mini</strong> (at ~$0.001 per 1k mixed tokens), this cost <strong>~$0.20</strong> per query for a result that was fundamentally unusable.</li> </ul> <p>To combat this, we had to add explicit loop detection mechanisms — a clear sign that our core approach was flawed.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Early stopping mechanisms to prevent infinite loops
</span><span class="k">if</span> <span class="n">current_ranking</span> <span class="ow">in</span> <span class="n">ranking_history</span><span class="p">:</span>
    <span class="n">logger</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">🛑 EARLY STOPPING: Exact ranking repetition detected</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">break</span>

<span class="c1"># Check for minimal changes (similarity &gt; 95%)
</span><span class="k">if</span> <span class="n">ranking_history</span><span class="p">:</span>
    <span class="n">similarity</span> <span class="o">=</span> <span class="n">difflib</span><span class="p">.</span><span class="nc">SequenceMatcher</span><span class="p">(</span>
        <span class="bp">None</span><span class="p">,</span> <span class="nf">str</span><span class="p">(</span><span class="n">previous_ranking</span><span class="p">),</span> <span class="nf">str</span><span class="p">(</span><span class="n">current_ranking</span><span class="p">)</span>
    <span class="p">).</span><span class="nf">ratio</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">similarity</span> <span class="o">&gt;</span> <span class="mf">0.95</span><span class="p">:</span>
        <span class="n">logger</span><span class="p">.</span><span class="nf">info</span><span class="p">(</span><span class="sh">"</span><span class="s">🛑 EARLY STOPPING: Changes below threshold</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">break</span>
</code></pre></div></div> <h2 id="a-hybrid-solution-combining-llm-perception-with-formulaic-judgment">A Hybrid Solution: Combining LLM Perception with Formulaic Judgment</h2> <p>The breakthrough came when we redefined the agents’ roles. Instead of having one agent handle both understanding and judgment, we split the responsibilities — a classic hybrid AI pattern.</p> <ul> <li><strong>LLM for Perception &amp; Feature Extraction</strong>: The MCP agents’ primary role became extracting specific, structured attributes from unstructured text. They were no longer asked “what’s important?” but rather “find the value for this specific field.”</li> <li><strong>Deterministic Engine for Judgment &amp; Ranking</strong>: The Summarizer was stripped of its reasoning capabilities and rebuilt as a deterministic engine that applied a formulaic, weighted-scoring model to the structured data provided by the other agents.</li> </ul> <p>We replaced simplistic criteria with medically relevant, quantifiable metrics:</p> <ul> <li><strong>Approval Status</strong>: FDA Approved = 100, Phase 3 Trials = 50, Pre-clinical = 10.</li> <li><strong>Evidence Level</strong>: Based on the GRADE framework. Level A (High) = 100, Level B (Moderate) = 70, Level C (Low) = 30.</li> <li><strong>Contraindication Score</strong>: A risk factor where lower is better. We inverted it for scoring: (1 - num_critical_contraindications / 10) * 100.</li> </ul> <p>The core of the new Summarizer was a clear, auditable function. A key part of its robustness was handling missing data gracefully with default values.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">calculate_drug_score</span><span class="p">(</span><span class="n">drug_profile</span><span class="p">:</span> <span class="nb">dict</span><span class="p">,</span> <span class="n">weights</span><span class="p">:</span> <span class="nb">dict</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Calculates a deterministic score based on structured, pre-extracted features.
    </span><span class="sh">"""</span>
    <span class="c1"># Using .get() with default values handles edge cases and prevents errors
</span>    <span class="n">approval_score</span> <span class="o">=</span> <span class="n">drug_profile</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">approval_status_score</span><span class="sh">"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">evidence_score</span> <span class="o">=</span> <span class="n">drug_profile</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">evidence_level_score</span><span class="sh">"</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">contraindication_score</span> <span class="o">=</span> <span class="n">drug_profile</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">contraindication_score</span><span class="sh">"</span><span class="p">,</span> <span class="mi">50</span><span class="p">)</span>

    <span class="c1"># The weighted scoring formula provides an auditable ranking logic
</span>    <span class="n">weighted_score</span> <span class="o">=</span> <span class="p">(</span>
        <span class="n">approval_score</span> <span class="o">*</span> <span class="n">weights</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">approval</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.5</span><span class="p">)</span> <span class="o">+</span>
        <span class="n">evidence_score</span> <span class="o">*</span> <span class="n">weights</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">evidence</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">)</span> <span class="o">+</span>
        <span class="n">contraindication_score</span> <span class="o">*</span> <span class="n">weights</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">safety</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.2</span><span class="p">)</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">weighted_score</span>
</code></pre></div></div> <p>This new approach was remarkably effective. The system achieved a stable ranking in just 4 iterations. The stability and correctness were validated against a golden dataset of drug rankings. This also had a dramatic impact on cost:</p> <ul> <li><strong>~4 iterations</strong> × 4 agents × ~1,000 tokens/call = <strong>~16,000 tokens</strong></li> <li>This cost <strong>~$0.016 per query</strong>, representing a <strong>92% cost savings</strong> while delivering a superior, reliable result.</li> </ul> <h2 id="enterprise-implications-a-pattern-for-trustworthy-ai">Enterprise Implications: A Pattern for Trustworthy AI</h2> <p>This isn’t just a story about one PoC; it’s about a scalable pattern for building trustworthy AI systems in the enterprise. This hybrid approach is directly applicable to other high-stakes domains:</p> <ul> <li><strong>Finance</strong>: For credit scoring or fraud detection, where an LLM can parse transaction notes for features, but a deterministic model must make the final risk assessment.</li> <li><strong>Legal Tech</strong>: For ranking documents by relevance in e-discovery, where an LLM can summarize documents, but a rule-based engine ranks them based on specific legal criteria.</li> <li><strong>Industrial Safety</strong>: For analyzing sensor data, where an LLM might interpret anomalous text-based alerts, but a state machine or rule engine must decide whether to trigger a shutdown.</li> </ul> <p>The pattern allows businesses to leverage the power of LLMs for what they do best — understanding unstructured data — while cordoning off the critical decision-making logic in a component that is stable, auditable, and transparent.</p> <h2 id="the-guiding-principle-for-high-stakes-ai">The Guiding Principle for High-Stakes AI</h2> <p align="center"> <i>For systems where trust is paramount, use LLMs for perception, not judgment. Let them fill the spreadsheet, but let a deterministic engine do the math.</i> </p> <p>The “reasoning vs. rules” debate is a false dichotomy. The reality is that robust AI systems require a thoughtful synthesis of both. For exploratory or creative tasks, a reasoning-first approach is often ideal. But for high-stakes, enterprise-grade applications demanding stability and auditability, the path to success is clear: start with a deterministic, formulaic framework. Grant autonomy and reasoning capabilities incrementally, always within the guardrails of a system you can trust and explain.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[A deep dive into building a medical ranking PoC where pure LLM reasoning failed, and how a hybrid pattern combining LLM feature extraction with a deterministic rule engine achieved stable, auditable results.]]></summary></entry><entry><title type="html">VLM Pipeline Debugging: Lessons from Visual Monitoring</title><link href="https://avchauzov.github.io/blog/2025/vlm-pipeline-debugging-visual-monitoring-lessons/" rel="alternate" type="text/html" title="VLM Pipeline Debugging: Lessons from Visual Monitoring"/><published>2025-09-09T00:00:00+00:00</published><updated>2025-09-09T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/vlm-pipeline-debugging-visual-monitoring-lessons</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/vlm-pipeline-debugging-visual-monitoring-lessons/"><![CDATA[<p>Building VLM pipelines for visual monitoring has taught me that multimodal systems fail in uniquely creative ways. Like pure language models, VLMs can hallucinate objects that aren’t there, misinterpret spatial relationships, and confidently describe non-existent details. And validating them is harder. Here’s what I’ve learned from countless proof-of-concept iterations.</p> <h3 id="the-preprocessing-trap">The Preprocessing Trap</h3> <p>Image preprocessing is where most VLM pipelines silently break. I’ve seen systems fail because of aggressive cropping that removed critical context, inappropriate resolution scaling, or noise that confused the visual encoder. The “resolution curse” is real — even advanced models struggle with high-resolution images, missing fine details that a human would catch instantly.</p> <p>My debugging rule: <strong>always inspect what the model actually sees</strong>. I save intermediate preprocessing outputs and manually verify that critical information survives the pipeline. A monitoring system that crops out the very gauge it’s supposed to read is worse than useless.</p> <h3 id="surface-vs-deep-understanding">Surface vs. Deep Understanding</h3> <p>VLMs excel at surface-level scene description but falter on operational details. They’ll accurately describe a “worker in a reflective vest near a control panel” but won’t understand if the worker is actually operating it or just walking past. Often, in response to a general query, the model would give a vague “interacts.” To get specifics, I had to refine the prompt to “describe only physical interaction” to filter out false positives. This surface-level competence can be dangerously misleading in safety-critical systems.</p> <p>Another problem adds to this: <strong>weak text control</strong>. Trying to force a VLM to output a structured JSON with specific fields is a direct path to errors and inconsistent output. My solution became a two-stage scheme: first, the VLM describes what it sees very flexibly and freely, and then a more powerful LLM (without a visual module) takes this description and “polishes” it, packing it into a strict JSON. This leverages the strengths of each model: one for vision, the other for structure and logic.</p> <h3 id="the-multi-crop-strategy">The Multi-Crop Strategy</h3> <p>In high-resolution monitoring scenarios, a “multi-crop” strategy significantly reduces hallucination rates. Instead of forcing the entire image through a single encoder, I break complex scenes into overlapping crops with positional metadata. This preserves fine details while maintaining spatial context.</p> <p>The key insight here is that <strong>overlap is critical</strong>, just like with text chunking in RAG. Non-overlapping crops create artificial boundaries that confuse the model’s spatial reasoning. Overlapping crops with coordinate information, however, help it understand how the pieces fit together.</p> <h3 id="looking-inside-white-box-debugging">Looking Inside: White-Box Debugging</h3> <p>While I haven’t implemented this approach in every project yet, deep debugging requires looking inside the “black box.” Attention maps and relevancy visualizations are a powerful tool. When a VLM claims to see a red warning light, such maps can immediately show whether it’s looking at the right area or just hallucinating. This is the next step for improving the quality of debugging.</p> <h3 id="the-consistency-check">The Consistency Check</h3> <p>Cross-modal verification is my final validation step. I generate text descriptions of images and then use them to verify against the original visual content. Inconsistencies often reveal subtle preprocessing errors or model limitations that other metrics miss.</p> <h3 id="the-guiding-principle-visual-grounding">The Guiding Principle: Visual Grounding</h3> <p align="center"> <i>Every claim a VLM makes about what it sees must be traceable to specific visual evidence. If you can track the model's attention and see the relevant image regions, it provides a huge boost to the entire pipeline.</i> </p> <p>This approach proves that VLMs are not a magical replacement for everything that came before. For tasks requiring high precision and reliability, you can’t yet get rid of proven <strong>classical CV</strong> methods, and especially not <strong>fine-tuning</strong> for a specific task. VLMs are powerful for general context understanding, but to build truly robust systems, they must be combined with more traditional and controllable approaches.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Hard-won insights from building PoC VLM pipelines for visual monitoring, where hallucinations hide in plain sight, preprocessing decisions make or break everything, and the question is: can we forget classical CV?]]></summary></entry><entry><title type="html">Pragmatic LLM Debugging: A Survival Guide to Chaos</title><link href="https://avchauzov.github.io/blog/2025/pragmatic-llm-debugging/" rel="alternate" type="text/html" title="Pragmatic LLM Debugging: A Survival Guide to Chaos"/><published>2025-09-02T00:00:00+00:00</published><updated>2025-09-02T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/pragmatic-llm-debugging</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/pragmatic-llm-debugging/"><![CDATA[<p>Lately, I’ve been diving deep into prototyping and debugging LLM applications — from RAG to multimodal agents. The circumstances are almost always the same: no time for complex solutions, no perfect datasets, and a pressing need for high quality right now (or yesterday). This forced me to develop a pragmatic, almost detective-like approach to debugging, which I want to share.</p> <h3 id="step-1-simplify-and-isolate">Step 1: Simplify and Isolate</h3> <p>When a system behaves unpredictably, my first reaction is to <strong>disconnect everything non-essential</strong>. Any “enhancing” but optional steps (like complex re-rankers or additional prompts) are temporarily removed. This helps isolate the core of the problem. If a task is too complex, I try swapping a giant model for something smaller and faster. It’s surprising how often this not only speeds up iterations but also <strong>improves stability</strong> for a specific task.</p> <h3 id="step-2-a-granular-breakdown">Step 2: A Granular Breakdown</h3> <p>After simplifying, I begin a step-by-step analysis, where each component is tested separately. Think of it as examining every clue at a crime scene.</p> <ul> <li><strong>For RAG Systems</strong>: I follow the entire chain. First, I check what the retriever is actually finding. Are the chunks relevant? Is context being lost? <strong>LLM-as-a-Judge</strong> with a simple boolean score (e.g., ‘relevant’ or ‘not relevant’) can do 80% of the job and help to <strong>check retrieval quality without manual labeling</strong>.</li> <li><strong>For Multimodal Systems (VLMs)</strong>: If the input is an image, I verify what the model “sees” in the crop. Problems often arise from incorrect preprocessing (tiny crop or noisy context) or when the model hallucinates details that aren’t there — a classic multimodal hallucination. Crucially, I also step back and ask: <strong>is the model even capable of performing</strong> this task, or are we asking for the impossible?</li> <li><strong>For Agentic Systems</strong>: <strong>Modular testing</strong> is key. I break down complex instructions into simple prompts and test one after another, increasing complexity. With tracing tools like Langfuse, you can see the agent’s entire “thoughts” and pinpoint exactly <strong>where its logic went off track</strong>. More classical ML tools like W&amp;B are also useful, but on the later stages, especially if we have custom metrics to observe.</li> </ul> <h3 id="step-3-the-pursuit-of-consistency">Step 3: The Pursuit of Consistency</h3> <p>The goal of debugging isn’t just to find a bug, but to achieve stable, predictable behavior. To do this, I heavily rely on <strong>structured and granular output</strong>. I instruct the model to return its response in JSON format and even apply post-processing to force the output into the required schema. This granularity is not just for reliability in the moment; it also makes it much easier to <strong>spot performance drift</strong> over time.</p> <h3 id="the-guiding-principle-interpretability">The Guiding Principle: Interpretability</h3> <p>Finally, the most important point, which underpins this entire approach:</p> <p align="center"> <i>Every decision the system makes must be explainable with human logic. If I can't explain why an agent chose a particular tool or why a RAG system gave a specific answer, then we don't understand the system.</i> </p> <p>This pragmatic approach — from simplification to a detailed breakdown — helps me quickly manage complex systems that, at first glance, seem like uncontrollable black boxes. It proves that even without perfect conditions, a structured, iterative process can lead to high-quality results.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[My approach to breaking down complex RAG and agent systems when time is short, golden datasets are missing, and quality needs a fast boost.]]></summary></entry></feed>