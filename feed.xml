<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://avchauzov.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://avchauzov.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-12-13T14:29:14+00:00</updated><id>https://avchauzov.github.io/feed.xml</id><title type="html">Andrey Chauzov</title><subtitle>Senior AI Architect with 7+ years building enterprise-scale AI solutions, specializing in NLP, MLOps, and intelligent infrastructure. Building the future of AI systems that scale. </subtitle><entry><title type="html">Token Optimization: Three Production Patterns That Reduce LLM Costs by 70%</title><link href="https://avchauzov.github.io/blog/2025/token-optimization-layered-architecture/" rel="alternate" type="text/html" title="Token Optimization: Three Production Patterns That Reduce LLM Costs by 70%"/><published>2025-12-02T00:00:00+00:00</published><updated>2025-12-02T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/token-optimization-layered-architecture</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/token-optimization-layered-architecture/"><![CDATA[<p>Production LLM costs scale quadratically $O(N^2)$ with input length during the prefill phase. For RAG systems retrieving 10 documents at 500 tokens each, this means 5,000 input tokens per request before generation starts.</p> <p>Three patterns reduce these costs: <strong>API-level prompt caching</strong> for static context, <strong>semantic caching</strong> for similar queries, and <strong>dynamic compression</strong> for long variable inputs. These are not alternatives—they stack. Each layer operates on tokens that survived previous filters.</p> <h2 id="pattern-1-api-level-caching">Pattern 1: API-Level Caching</h2> <p>OpenAI and Anthropic implement token-level caching at infrastructure level. The mechanism is simple: if your prompt prefix matches a cached prefix byte-for-byte, the API reuses computed attention states. Cost reduction is approximately <strong>90%</strong> for cached tokens.</p> <p>The constraint is positional. Cached content must be at the start of the messages array. One character difference breaks the cache.</p> <p><strong>Primary Target</strong>: System Context</p> <p>Static instructions, few-shot examples, policy documents—anything that does not change between requests.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>

<span class="c1"># Static context positioned first
</span><span class="n">SYSTEM_CONTEXT</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">You are a financial analyst.
Company guidelines: [1000 tokens of static policies]
</span><span class="sh">"""</span>

<span class="k">def</span> <span class="nf">query_with_cache</span><span class="p">(</span><span class="n">user_query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">SYSTEM_CONTEXT</span><span class="p">},</span>  <span class="c1"># Cached
</span>            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_query</span><span class="p">}</span>         <span class="c1"># Variable
</span>        <span class="p">]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span>
</code></pre></div></div> <p><strong>Common Implementation Error</strong>: Placing timestamps or request IDs at the top of messages. This invalidates the cache immediately. Variable metadata goes to headers or end of user message.</p> <p><strong>Savings</strong>: 50–90% on static context. First request pays full cost. Subsequent requests with identical prefix hit cache.</p> <h2 id="pattern-2-semantic-similarity-caching">Pattern 2: Semantic Similarity Caching</h2> <p>API caching requires exact token match. Semantic caching handles queries that are lexically different but semantically identical.</p> <p>The implementation is straightforward: convert queries to embeddings, compare using cosine similarity. If similarity exceeds threshold $\theta$, return cached response.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>
<span class="kn">from</span> <span class="n">sklearn.metrics.pairwise</span> <span class="kn">import</span> <span class="n">cosine_similarity</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>

<span class="k">class</span> <span class="nc">SemanticCache</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">threshold</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.95</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">cache</span> <span class="o">=</span> <span class="p">{}</span>  <span class="c1"># {hash: (embedding, response)}
</span>        <span class="n">self</span><span class="p">.</span><span class="n">threshold</span> <span class="o">=</span> <span class="n">threshold</span>

    <span class="k">def</span> <span class="nf">_embed</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">np</span><span class="p">.</span><span class="n">ndarray</span><span class="p">:</span>
        <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">embeddings</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
            <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">,</span>
            <span class="nb">input</span><span class="o">=</span><span class="n">text</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">data</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">embedding</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">query_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_embed</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>

        <span class="k">for</span> <span class="n">cached_emb</span><span class="p">,</span> <span class="n">response</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">.</span><span class="nf">values</span><span class="p">():</span>
            <span class="n">similarity</span> <span class="o">=</span> <span class="nf">cosine_similarity</span><span class="p">(</span>
                <span class="n">query_emb</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">),</span>
                <span class="n">cached_emb</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
            <span class="p">)[</span><span class="mi">0</span><span class="p">][</span><span class="mi">0</span><span class="p">]</span>

            <span class="k">if</span> <span class="n">similarity</span> <span class="o">&gt;=</span> <span class="n">self</span><span class="p">.</span><span class="n">threshold</span><span class="p">:</span>
                <span class="k">return</span> <span class="n">response</span>
        <span class="k">return</span> <span class="bp">None</span>

    <span class="k">def</span> <span class="nf">set</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">response</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
        <span class="n">query_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_embed</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">[</span><span class="nf">hash</span><span class="p">(</span><span class="n">query</span><span class="p">)]</span> <span class="o">=</span> <span class="p">(</span><span class="n">query_emb</span><span class="p">,</span> <span class="n">response</span><span class="p">)</span>

<span class="c1"># Usage
</span><span class="n">cache</span> <span class="o">=</span> <span class="nc">SemanticCache</span><span class="p">(</span><span class="n">threshold</span><span class="o">=</span><span class="mf">0.95</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">query_with_semantic_cache</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">cached</span> <span class="o">=</span> <span class="n">cache</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">query</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">cached</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">cached</span>

    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">query</span><span class="p">}]</span>
    <span class="p">).</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span>

    <span class="n">cache</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">response</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span>
</code></pre></div></div> <p><strong>Economics</strong>: Embedding costs ~$0.0001 per query. LLM inference costs ~$0.01 per query. Break-even after 2–3 cache hits.</p> <p><strong>Threshold Selection</strong>: $\theta = 0.95$ is conservative. Lower values ($\theta = 0.90$) increase hit rate but risk semantic drift. For customer support or internal Q&amp;A, hit rates of 30–50% are typical.</p> <p><strong>Savings</strong>: 10–50% depending on query patterns.</p> <h2 id="pattern-3-dynamic-compression-with-llmlingua-2">Pattern 3: Dynamic Compression with LLMLingua-2</h2> <p>For long, unique context (RAG documents), caching does not help. Compression reduces token count by removing low-information content.</p> <p>LLMLingua-2 reformulates prompt compression as token-level binary classification (Keep/Discard). Unlike the original LLMLingua that uses perplexity from causal models, LLMLingua-2 trains a compact BERT-based classifier on data distilled from larger models. This approach eliminates unidirectional context limitations and provides task-agnostic compression.</p> <p>The key advantage: LLMLingua-2 is 3-6× faster than perplexity-based methods while maintaining higher accuracy on out-of-domain data.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">llmlingua</span> <span class="kn">import</span> <span class="n">PromptCompressor</span>
<span class="kn">from</span> <span class="n">openai</span> <span class="kn">import</span> <span class="n">OpenAI</span>

<span class="n">client</span> <span class="o">=</span> <span class="nc">OpenAI</span><span class="p">()</span>

<span class="n">compressor</span> <span class="o">=</span> <span class="nc">PromptCompressor</span><span class="p">(</span>
    <span class="n">model_name</span><span class="o">=</span><span class="sh">"</span><span class="s">microsoft/llmlingua-2-bert-base-multilingual-cased-meetingbank</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">cpu</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">use_llmlingua2</span><span class="o">=</span><span class="bp">True</span>  <span class="c1"># Explicit LLMLingua-2 mode
</span><span class="p">)</span>

<span class="n">RETRIEVED_DOCS</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
Document 1: Apple Inc. reported revenue of $89.5 billion for Q3 2024...
Document 2: Services revenue grew 14% year-over-year to $23.1 billion...
[5 documents, ~300 words total]
</span><span class="sh">"""</span>

<span class="n">QUESTION</span> <span class="o">=</span> <span class="sh">"</span><span class="s">What was Apple</span><span class="sh">'</span><span class="s">s Q3 revenue?</span><span class="sh">"</span>

<span class="k">def</span> <span class="nf">compress_and_query</span><span class="p">(</span><span class="n">context</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">question</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">rate</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.5</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="c1"># Compress with structural token preservation
</span>    <span class="n">compressed</span> <span class="o">=</span> <span class="n">compressor</span><span class="p">.</span><span class="nf">compress_prompt</span><span class="p">(</span>
        <span class="n">context</span><span class="p">,</span>
        <span class="n">rate</span><span class="o">=</span><span class="n">rate</span><span class="p">,</span>           <span class="c1"># Keep 50% of tokens
</span>        <span class="n">force_tokens</span><span class="o">=</span><span class="p">[</span><span class="sh">'</span><span class="se">\n</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">?</span><span class="sh">'</span><span class="p">]</span>  <span class="c1"># Preserve line breaks and questions
</span>    <span class="p">)</span>

    <span class="n">compressed_text</span> <span class="o">=</span> <span class="n">compressed</span><span class="p">[</span><span class="sh">"</span><span class="s">compressed_prompt</span><span class="sh">"</span><span class="p">]</span>

    <span class="c1"># Metrics
</span>    <span class="n">original_words</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">context</span><span class="p">.</span><span class="nf">split</span><span class="p">())</span>
    <span class="n">compressed_words</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">compressed_text</span><span class="p">.</span><span class="nf">split</span><span class="p">())</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Compression: </span><span class="si">{</span><span class="n">original_words</span><span class="si">}</span><span class="s"> → </span><span class="si">{</span><span class="n">compressed_words</span><span class="si">}</span><span class="s"> words (</span><span class="si">{</span><span class="n">compressed_words</span><span class="o">/</span><span class="n">original_words</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="o">%</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">response</span> <span class="o">=</span> <span class="n">client</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">model_name</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">system</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">compressed_text</span><span class="p">},</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="n">question</span><span class="p">}</span>
        <span class="p">]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span>
</code></pre></div></div> <p><strong>Critical Parameter</strong>: <code class="language-plaintext highlighter-rouge">force_tokens</code></p> <p>This parameter prevents removal of structurally important tokens (newlines, punctuation). Without it, compression can merge sentences and break document boundaries, degrading retrieval context.</p> <p><strong>Performance Profile</strong>: Compression adds 100-300ms latency for 8K tokens. But prefill cost $O(N^2)$ dominates for $N &gt; 500$. At compression rate $r = 0.5$ (50% reduction), net speedup is approximately $1.7\times$.</p> <p><strong>Accuracy Trade-off</strong>: At $r \leq 0.2$ (keep 20% of tokens), accuracy loss is minimal (within 2%) for QA tasks. Beyond $r = 0.1$ (keep 10%), grammatical coherence degrades. For code or JSON, damage occurs earlier.</p> <p><strong>Savings</strong>: 50–80% on long context.</p> <h2 id="measurement">Measurement</h2> <p>Token optimization requires tracking effective token usage, not just raw counts.</p> <p>Key metrics:</p> <ul> <li><strong>Cache Hit Rate</strong>: $\frac{\text{hits}}{\text{total requests}}$. Target: &gt;30% for API cache, &gt;40% for semantic cache.</li> <li><strong>Effective Compression Ratio</strong>: $\frac{\text{original tokens}}{\text{processed tokens}}$. Includes both caching and compression.</li> <li><strong>Cost Reduction</strong>: $1 - \frac{\text{current cost}}{\text{baseline cost}}$.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">dataclasses</span> <span class="kn">import</span> <span class="n">dataclass</span>

<span class="nd">@dataclass</span>
<span class="k">class</span> <span class="nc">TokenMetrics</span><span class="p">:</span>
    <span class="n">total_input_tokens</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">cached_input_tokens</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">output_tokens</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">cache_hits</span><span class="p">:</span> <span class="nb">int</span>
    <span class="n">cache_misses</span><span class="p">:</span> <span class="nb">int</span>

    <span class="k">def</span> <span class="nf">effective_cost</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_price</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">,</span>
                      <span class="n">output_price</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.03</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
        <span class="sh">"""</span><span class="s">Cost in $/1K tokens (typical LLM API pricing)</span><span class="sh">"""</span>
        <span class="n">uncached</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">total_input_tokens</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">cached_input_tokens</span>
        <span class="n">cached_cost</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">cached_input_tokens</span> <span class="o">*</span> <span class="p">(</span><span class="n">input_price</span> <span class="o">*</span> <span class="mf">0.1</span><span class="p">)</span> <span class="o">/</span> <span class="mi">1000</span>
        <span class="n">uncached_cost</span> <span class="o">=</span> <span class="n">uncached</span> <span class="o">*</span> <span class="n">input_price</span> <span class="o">/</span> <span class="mi">1000</span>
        <span class="n">output_cost</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">output_tokens</span> <span class="o">*</span> <span class="n">output_price</span> <span class="o">/</span> <span class="mi">1000</span>
        <span class="k">return</span> <span class="n">cached_cost</span> <span class="o">+</span> <span class="n">uncached_cost</span> <span class="o">+</span> <span class="n">output_cost</span>

    <span class="k">def</span> <span class="nf">hit_rate</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
        <span class="n">total</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">cache_hits</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">cache_misses</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">cache_hits</span> <span class="o">/</span> <span class="n">total</span> <span class="k">if</span> <span class="n">total</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="k">else</span> <span class="mf">0.0</span>
</code></pre></div></div> <h2 id="implementation-strategy">Implementation Strategy</h2> <p>These patterns form a hierarchy. Apply them in order:</p> <p><strong>Layer 1 (API Caching)</strong>: Apply first. Zero overhead for static prompts. Expected savings: 50–90%.</p> <p><strong>Layer 2 (Semantic Caching)</strong>: Add when queries show lexical variation but semantic similarity. Common in customer-facing systems. Expected savings: 10–50%.</p> <p><strong>Layer 3 (Compression)</strong>: Apply only when $N &gt; 1000$ tokens. Compression overhead (100–300ms) must be justified by prefill cost reduction. Expected savings: 50–80%.</p> <p>In production RAG:</p> <ol> <li>System instructions cached at API level</li> <li>User queries cached semantically</li> <li>Retrieved documents compressed dynamically</li> </ol> <h2 id="edge-cases-and-limitations">Edge Cases and Limitations</h2> <p><strong>API Cache Breaks Silently</strong></p> <p>If prompt structure changes—reordering, adding metadata—cache misses occur without warning. Monitor hit rate. Drop below 20% indicates structural drift.</p> <p><strong>Semantic Cache False Positives</strong></p> <p>At $\theta = 0.90$, semantic drift becomes noticeable. For high-stakes applications (finance, medical), keep $\theta \geq 0.95$ or add manual validation for borderline cases.</p> <p><strong>Compression Destroys Structure</strong></p> <p>Token pruning breaks syntax for code, JSON, and reasoning chains (CoT). LLMLingua-2’s BERT-based classification helps, but for code-heavy prompts, use function-level compression instead of token-level. Use <code class="language-plaintext highlighter-rouge">force_tokens</code> parameter to preserve critical structural elements (newlines, brackets, punctuation).</p> <p><strong>Overhead Dominates Small Inputs</strong></p> <p>For $N &lt; 500$, compression latency exceeds prefill savings. The crossover point depends on hardware, but general rule: compress only when input exceeds 1000 tokens.</p> <p><strong>Embedding Model Drift</strong></p> <p>Semantic cache depends on embedding model. If you update from <code class="language-plaintext highlighter-rouge">text-embedding-3-small</code> to newer model, cache becomes invalid. This requires migration or cache flush.</p> <h2 id="benchmarks">Benchmarks</h2> <p>Internal RAG system (10-document retrieval, 500 tokens/document, 10,000 requests/day):</p> <hr/> <table> <thead> <tr> <th>Configuration</th> <th>Tokens/Request</th> <th>Daily Cost</th> <th>Savings</th> <th>Latency ($P_{50}$)</th> </tr> </thead> <tbody> <tr> <td>Baseline</td> <td>5000</td> <td>$500</td> <td>—</td> <td>850ms</td> </tr> <tr> <td>+ API Caching</td> <td>2000</td> <td>$250</td> <td>50%</td> <td>850ms</td> </tr> <tr> <td>+ Semantic Cache</td> <td>1500</td> <td>$180</td> <td>64%</td> <td>820ms</td> </tr> <tr> <td>+ Compression (2×)</td> <td>750</td> <td>$120</td> <td>76%</td> <td>1030ms</td> </tr> </tbody> </table> <hr/> <p>Compression adds ~200ms overhead. Acceptable for batch processing or async workflows. For real-time systems with $P_{95} &lt; 500ms$ latency requirements, compression is typically excluded.</p> <h2 id="when-to-use">When to Use</h2> <p><strong>API Caching</strong>: Always. No downside if you can structure prompts with static prefix.</p> <p><strong>Semantic Caching</strong>: When &gt;20% of queries are semantically similar. Common in support chatbots, internal Q&amp;A, FAQ systems.</p> <p><strong>Compression</strong>: When retrieved context regularly exceeds 1000 tokens and latency tolerance is &gt;500ms. Not suitable for real-time chat interfaces.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[API-level caching, semantic similarity-based caching, and dynamic compression with LLMLingua form a layered approach to token reduction. Each pattern targets different inefficiencies in the prompt processing pipeline.]]></summary></entry><entry><title type="html">Hierarchical Signal Tuning: Optimizing Components Before Fusion</title><link href="https://avchauzov.github.io/blog/2025/hierarchical-signal-tuning-hybrid-search/" rel="alternate" type="text/html" title="Hierarchical Signal Tuning: Optimizing Components Before Fusion"/><published>2025-11-26T00:00:00+00:00</published><updated>2025-11-26T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/hierarchical-signal-tuning-hybrid-search</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/hierarchical-signal-tuning-hybrid-search/"><![CDATA[<p>Hybrid search combines Dense and Sparse retrieval to improve ranking quality. Standard implementations assume that component systems ($S_{\text{dense}}$ and $S_{\text{BM25}}$) provide valid input signals. This assumption fails in production.</p> <p>Fusion methods (Linear Combination, RRF) optimize how components are merged. They do not optimize the components themselves. Tuning $\alpha$ or RRF constant $k$ yields minimal improvement when input rankings are corrupted.</p> <h3 id="the-component-problem-unweighted-field-heterogeneity">The Component Problem: Unweighted Field Heterogeneity</h3> <p>Documents contain structured fields (Title, Body, Metadata). Standard BM25 implementations treat all fields equally. This ignores signal density.</p> <p>Two common errors:</p> <p><strong>1. The “Blob” Approach</strong>: Concatenating all fields into single text. Title matches become equal to body matches. Positional signal is lost.</p> <p><strong>2. The “Flat” Approach</strong>: Querying fields with equal weight (default <code class="language-plaintext highlighter-rouge">multi_match</code> in Elasticsearch). Term frequency in long fields dominates short but high-signal fields.</p> <p><strong>Concrete Example:</strong></p> <p>Query: <code class="language-plaintext highlighter-rouge">connection timeout</code></p> <ul> <li><strong>Doc A (Relevant)</strong>: Title contains “Connection Timeout”</li> <li><strong>Doc B (Noise)</strong>: Body contains “…issue not related to connection timeout…” (long troubleshooting log)</li> </ul> <p>Flat BM25 often ranks Doc B higher due to term frequency scaling with document length. The Sparse component recommends incorrect documents.</p> <h3 id="why-reciprocal-rank-fusion-does-not-solve-this">Why Reciprocal Rank Fusion Does Not Solve This</h3> <p>RRF (Reciprocal Rank Fusion) is often assumed to eliminate component tuning requirements. The formula:</p> \[RRF(d) = \sum_{r \in \text{rankings}} \frac{1}{k + r(d)}\] <p>RRF normalizes score distributions. It does not correct ranking errors. If unweighted BM25 ranks noise at position #1 and signal at position #10, RRF propagates this error. Aggregation does not create relevance from poor rankings.</p> <h3 id="stage-1-component-level-optimization">Stage 1: Component-Level Optimization</h3> <p>Component rankings must be fixed before fusion is applied. BM25 is treated as a weighted sum:</p> \[S_{\text{BM25}}(q, d) = \sum_{f \in \text{fields}} w_f \cdot \text{BM25}(q, d_f)\] <p><strong>Optimization Protocol:</strong></p> <ol> <li>Disable fusion ($\alpha=0$ or disable Dense component entirely).</li> <li>Assign initial weights based on signal density hypothesis. Titles typically require $w_{\text{title}} \gg w_{\text{body}}$</li> <li>Optimize for <strong>NDCG@10</strong> (precision at top ranks).</li> <li>Monitor <strong>HitRate@10</strong> (recall constraint). Configurations that boost NDCG but reduce HitRate indicate over-filtering</li> </ol> <p>Field weights force correct ranking order. Doc A (title match) must rank above Doc B (body noise) before reaching fusion stage.</p> <h3 id="stage-2-fusion-level-optimization">Stage 2: Fusion-Level Optimization</h3> <p>After components are stabilized ($S_{\text{BM25}}^<em>$ and $S_{\text{dense}}^</em>$), fusion parameter is tuned:</p> \[R_{\text{final}} = \alpha \cdot S_{\text{dense}}^* + (1 - \alpha) \cdot S_{\text{BM25}}^*\] <p>With optimized inputs, $\alpha$ acts as semantic balancer rather than noise filter. Optimization surface becomes smoother. Optimal values are more stable across query types.</p> <h3 id="validation-workflow-sequential-optimization">Validation Workflow: Sequential Optimization</h3> <p>Simultaneous optimization of all parameters ($w_{\text{title}}, w_{\text{body}}, \alpha$) creates combinatorial explosion. Sequential approach is more efficient:</p> <ol> <li><strong>Step 1 (Sparse)</strong>: Fix $\alpha=0$. Optimize field weights for NDCG@10 and HitRate@10. Freeze configuration.</li> <li><strong>Step 2 (Dense)</strong>: Fix $\alpha=1$. Optimize embedding model or chunking strategy. Freeze configuration.</li> <li><strong>Step 3 (Fusion)</strong>: Sweep $\alpha \in [0.0, 1.0]$ or RRF constant $k$.</li> </ol> <p>This coordinate descent approach reduces search space while maintaining optimization quality.</p> <h3 id="practical-implementation-notes">Practical Implementation Notes</h3> <p>Field weight optimization requires validation set stratification. Query types (factual vs. conceptual) may require different weight configurations. If optimal weights vary significantly across strata, per-query routing should be considered.</p> <p>For legal/compliance corpora with structured sections (statute references, definitions, procedures), hierarchical tuning typically yields 3-5% NDCG@10 improvement over flat $\alpha=0.5$ baseline. Latency remains unchanged.</p> <h3 id="conclusion">Conclusion</h3> <p>Hybrid search quality is bounded by component quality. Fusion algorithms cannot compensate for poorly ranked inputs.</p> <p>Optimization must follow hierarchy: fields → components → fusion. This bottom-up approach produces measurable performance gains that cannot be achieved through fusion parameter tuning alone.</p> <hr/> <p align="center"> <i>Effective ensemble methods require effective base models. Optimize the parts before optimizing the whole.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Fusion algorithms like Linear Combination or RRF cannot fix poor input signals. Effective hybrid search requires a bottom-up optimization strategy: tuning field weights within BM25 and embedding strategies within Dense components before attempting to merge them.]]></summary></entry><entry><title type="html">Jensen-Shannon Divergence for Meaningful Clustering</title><link href="https://avchauzov.github.io/blog/2025/jensen-shannon-clustering-metric/" rel="alternate" type="text/html" title="Jensen-Shannon Divergence for Meaningful Clustering"/><published>2025-11-22T00:00:00+00:00</published><updated>2025-11-22T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/jensen-shannon-clustering-metric</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/jensen-shannon-clustering-metric/"><![CDATA[<p>Standard clustering metrics like Silhouette Score or Davies-Bouldin validate <strong>geometry</strong>, not <strong>meaning</strong>. In unsupervised scenarios without ground truth, we rely on these proxies, assuming that spatial compactness equals semantic relevance.</p> <p>Often, this assumption fails. You get a high Silhouette Score, but when stakeholders ask, <em>“What makes Segment A unique?”</em>, you can’t answer because feature distributions overlap. This is the <strong>Explainability Gap</strong>.</p> <h3 id="the-logic-distribution-over-distance">The Logic: Distribution over Distance</h3> <p>In my practice, the robust solution is <strong>Jensen-Shannon Divergence (JSD)</strong>. Unlike Euclidean distance, JSD measures the statistical divergence between probability distributions.</p> <p>The logic is simple: For every cluster and every feature, we calculate the divergence between:</p> <ol> <li>The distribution of the feature <strong>inside</strong> the cluster ($P$).</li> <li>The distribution of the feature <strong>outside</strong> the cluster ($Q$).</li> </ol> <p><strong>Why not Kullback-Leibler (KL)?</strong> KL divergence is asymmetric ($KL(P||Q) \neq KL(Q||P)$) and explodes to infinity if distributions don’t overlap (which happens constantly in clustering). JSD solves this: it is symmetric and strictly bounded to $[0, \ln(2)] \approx [0, 0.693]$ with natural logarithm (or $[0, 1]$ with base-2 logarithm). This bounded nature makes it numerically stable for hyperparameter tuning, whereas KL divergence often explodes and breaks optimizers.</p> <h3 id="conceptual-implementation">Conceptual Implementation</h3> <p>A critical engineering detail here is the <strong>Size Penalty</strong>. Pure JSD is mathematically maximized by singleton clusters (a single point is perfectly unique). To prevent the metric from “gaming” the system and creating micro-clusters, we must explicitly penalize them.</p> <p>The following implementation computes the weighted JSD score for a labeled dataframe:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy.spatial.distance</span> <span class="kn">import</span> <span class="n">jensenshannon</span>

<span class="k">def</span> <span class="nf">calculate_jsd_metric</span><span class="p">(</span><span class="n">df</span><span class="p">,</span> <span class="n">labels</span><span class="p">,</span> <span class="n">feature_cols</span><span class="p">,</span> <span class="n">penalty_weight</span><span class="o">=</span><span class="mf">0.1</span><span class="p">):</span>
    <span class="n">unique_labels</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">unique</span><span class="p">(</span><span class="n">labels</span><span class="p">)</span>
    <span class="n">total_score</span> <span class="o">=</span> <span class="mi">0</span>

    <span class="k">for</span> <span class="n">label</span> <span class="ow">in</span> <span class="n">unique_labels</span><span class="p">:</span>
        <span class="n">in_cluster</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="n">labels</span> <span class="o">==</span> <span class="n">label</span><span class="p">]</span>
        <span class="n">out_cluster</span> <span class="o">=</span> <span class="n">df</span><span class="p">[</span><span class="n">labels</span> <span class="o">!=</span> <span class="n">label</span><span class="p">]</span>

        <span class="c1"># 1. Calculate JSD per feature
</span>        <span class="n">c_score</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">for</span> <span class="n">f</span> <span class="ow">in</span> <span class="n">feature_cols</span><span class="p">:</span>
            <span class="c1"># Fixed binning for stability (production may use adaptive strategies)
</span>            <span class="n">p</span><span class="p">,</span> <span class="n">bins</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">histogram</span><span class="p">(</span><span class="n">in_cluster</span><span class="p">[</span><span class="n">f</span><span class="p">],</span> <span class="n">bins</span><span class="o">=</span><span class="mi">20</span><span class="p">)</span>
            <span class="n">p</span> <span class="o">=</span> <span class="n">p</span> <span class="o">/</span> <span class="n">p</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>  <span class="c1"># normalize to PMF
</span>            <span class="n">q</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">histogram</span><span class="p">(</span><span class="n">out_cluster</span><span class="p">[</span><span class="n">f</span><span class="p">],</span> <span class="n">bins</span><span class="o">=</span><span class="n">bins</span><span class="p">)</span>
            <span class="n">q</span> <span class="o">=</span> <span class="n">q</span> <span class="o">/</span> <span class="n">q</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>  <span class="c1"># normalize to PMF
</span>            <span class="n">c_score</span> <span class="o">+=</span> <span class="nf">jensenshannon</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">q</span><span class="p">)</span>

        <span class="c1"># Average over all features
</span>        <span class="c1"># Note: Production variant may select top-k features by JSD
</span>        <span class="c1"># for more focused interpretation instead of averaging all
</span>        <span class="n">avg_jsd</span> <span class="o">=</span> <span class="n">c_score</span> <span class="o">/</span> <span class="nf">len</span><span class="p">(</span><span class="n">feature_cols</span><span class="p">)</span>

        <span class="c1"># 2. Apply Size Penalty (Crucial!)
</span>        <span class="c1"># Simplified exponential penalty (production uses log-based ratio)
</span>        <span class="c1"># Penalizes clusters that are too small to be meaningful
</span>        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">in_cluster</span><span class="p">)</span>
        <span class="n">penalty</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">+</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">penalty_weight</span> <span class="o">*</span> <span class="p">(</span><span class="n">n</span> <span class="o">-</span> <span class="mi">10</span><span class="p">))))</span>

        <span class="n">total_score</span> <span class="o">+=</span> <span class="n">avg_jsd</span> <span class="o">*</span> <span class="n">penalty</span>

    <span class="k">return</span> <span class="n">total_score</span> <span class="o">/</span> <span class="nf">len</span><span class="p">(</span><span class="n">unique_labels</span><span class="p">)</span>
</code></pre></div></div> <h3 id="implementation-advice">Implementation Advice</h3> <p>I use this metric in two distinct ways:</p> <ol> <li> <p><strong>For Cluster Interpretability (Post-hoc)</strong>: To explain <em>why</em> a cluster exists, I calculate JSD for all features. Instead of staring at centroids, I sort features by JSD. The top features define the cluster’s identity (e.g., <em>“Segment B is distinct because of low ‘Recency’ and high ‘Frequency’“</em>).</p> </li> <li> <p><strong>For Optimization Target</strong>: When standard grid search yields geometric blobs with no business value, I add JSD to the objective function: \(\text{Score} = (1 - \alpha) \cdot \text{Silhouette} + \alpha \cdot \text{JSD}\) A weight of $\alpha \approx 0.3$ usually forces the algorithm to sacrifice some compactness for better distinguishability.</p> </li> </ol> <hr/> <p align="center"> <i>Interpretability is not exclusive to supervised ML/AI; it is equally vital in unsupervised learning. JSD bridges this gap by quantifying not just how far apart clusters are, but what characterizes them—turning abstract geometric blobs into meaningful, explainable segments.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Silhouette Score validates geometry, not meaning. Using Jensen-Shannon Divergence to measure feature distribution divergence bridges the gap between mathematical separation and interpretability.]]></summary></entry><entry><title type="html">Hybrid Intent Classification: The Rationale for Production-Grade Shallow-Model-First Architectures</title><link href="https://avchauzov.github.io/blog/2025/hybrid-intent-classification/" rel="alternate" type="text/html" title="Hybrid Intent Classification: The Rationale for Production-Grade Shallow-Model-First Architectures"/><published>2025-11-19T00:00:00+00:00</published><updated>2025-11-19T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/hybrid-intent-classification</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/hybrid-intent-classification/"><![CDATA[<p>Intent classification is a key component in conversational AI systems. For smooth user experience, production systems require very low latency, specifically under 100ms. Relying only on resource-intensive <strong>Large Language Models (LLMs)</strong> for every user query leads to high latency and high operational costs. Conversely, using only a small, monolithic distilled model risks lower accuracy, particularly for edge cases. A practical strategy is the <strong>Hybrid Multi-Stage Routing</strong> paradigm: compact, specialized models manage the high volume of routine requests, while the computational resources of LLMs are used only when necessary.</p> <h3 id="how-it-works">How it works</h3> <p>This architecture is often called <strong>Hierarchical Inference (HI)</strong> or <strong>Uncertainty-Based Routing</strong>. It specifies a pipeline for processing user input.</p> <p><strong>Stage 1 — The Specialized Classifier (Shallow Language Model, SLM)</strong>. Despite the rise of LLMs, compact <strong>encoder-based</strong> models remain highly effective for high-throughput scenarios. These include optimized <strong>BERT-variants</strong>, <strong>contrastive learning models</strong>, and other distilled architectures. These compact SLMs (typically 5–20 MB) perform the initial classification. They can achieve inference speeds significantly below 100ms, even on CPU-only infrastructure.</p> <p><strong>Stage 2 — Confidence and Escalation Gating</strong>. The SLM output includes a <strong>confidence score</strong> for its top prediction. This score is checked against an <strong>escalation threshold</strong> (typically between 0.7 and 0.9). If the predicted confidence is too low—meaning it falls below the threshold—the query is sent to the next processing stage.</p> <p><strong>Stage 3 — LLM Fallback and Refinement</strong>. Low-confidence queries are routed to a robust LLM. These models are used to resolve ambiguity present in low-confidence queries. This process, which engages the LLM for only 10–30% of total requests, is crucial for maintaining system accuracy while keeping high LLM costs under control.</p> <h3 id="production-performance">Production Performance</h3> <p>The deployment of this hybrid architecture shows significant performance improvements:</p> <ul> <li><strong>Latency Reduction</strong>: Average processing latency is reduced by 50% compared to monolithic LLM-only systems.</li> <li><strong>Accuracy Preservation</strong>: The classification accuracy of the hybrid system is usually within $\pm 2\%$ from the accuracy of the standalone LLM.</li> <li><strong>Cost Efficiency</strong>: A reduction in external API usage translates to an estimated 70–85% decrease in overall operational API costs.</li> </ul> <p>For instance, in a Natural Language Understanding (NLU) system for a complex domain, using a <strong>specialized, contrastive encoder model</strong> in Stage 1 yielded an accuracy of 90–94% for routine intents. The costly LLM fallback was invoked for only approximately 15% of the total query volume.</p> <h3 id="when-to-use-this-pattern">When to Use This Pattern</h3> <p>The decision to adopt this pattern depends on specific constraints and application requirements.</p> <p><strong>This hybrid routing pattern is recommended when</strong>:</p> <ul> <li>The latency requirement is very strict (e.g., $P_{95}$ latency must be less than 500ms).</li> <li>The system must process a high volume of requests (e.g., thousands of transactions per day).</li> <li>The majority of user intents are simple, repetitive, and easy to define.</li> </ul> <p><strong>Adoption is not recommended for</strong>:</p> <ul> <li>Initial prototypes or applications with low traffic, where the added complexity is not justified.</li> <li>Domains of extreme complexity where even simple queries need deep reasoning from a large model.</li> <li>Situations where there is not enough training data for the specialized SLM.</li> </ul> <h3 id="implementation-best-practices">Implementation Best Practices</h3> <p>Several implementation details matter for optimal performance.</p> <p><strong>Optimization of the Specialized Classifier (SLM)</strong>. It is effective to use <strong>knowledge distillation</strong> techniques, where the compact SLM is trained to replicate the predictions of the larger LLM. This helps transfer the LLM’s generalization capability to the compact model. Further efficiency is gained through <strong>quantization</strong> (e.g., INT8 or INT4), which reduces memory usage and increases inference speed.</p> <p><strong>Threshold Calibration</strong>. The selection of the confidence threshold is a critical hyperparameter. A threshold that is too low (e.g., 0.5) leads to unnecessary use of the costly LLM. A threshold that is too high (e.g., 0.95) results in lower accuracy because necessary escalations are blocked. An initial starting point of 0.75 is often used, followed by tuning based on the trade-off between accuracy and cost.</p> <p><strong>Drift Monitoring</strong>. User language and intent distribution change over time (concept and data drift). Monitoring the SLM average confidence scores is essential. A downward trend in average confidence indicates that the model requires <strong>retraining</strong> on recent data or that the threshold needs adjustment.</p> <hr/> <p align="center"> <i>The Hybrid Intent Classification pattern represents a standard production approach. It combines the speed and efficiency of specialized encoder-based models—often overlooked in the LLM era—with the reasoning capacity of Large Language Models, using each component only when necessary.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Production chatbots route most requests through fast shallow classifiers, escalating to Large Language Models (LLMs) only on low-confidence queries. This hybrid architecture mitigates the latency and cost overheads of monolithic LLM solutions, achieving significant speed gains while preserving high classification accuracy.]]></summary></entry><entry><title type="html">Few-Shot Prompt Ordering: The Impact of Example Position</title><link href="https://avchauzov.github.io/blog/2025/few-shot-prompt-ordering-recency-bias/" rel="alternate" type="text/html" title="Few-Shot Prompt Ordering: The Impact of Example Position"/><published>2025-11-15T00:00:00+00:00</published><updated>2025-11-15T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/few-shot-prompt-ordering-recency-bias</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/few-shot-prompt-ordering-recency-bias/"><![CDATA[<p>Few-shot prompting is a standard technique in NLP engineering. While we frequently discuss selection strategies (choosing <em>which</em> examples to use), the <em>ordering</em> of these examples often receives less attention. We typically default to random ordering or static lists. However, given the known positional biases in Large Language Models (LLMs), it makes sense to treat example ordering as an optimization variable rather than a constant.</p> <h3 id="the-mechanism-primacy-recency-and-the-u-curve">The Mechanism: Primacy, Recency, and the U-Curve</h3> <p>The assumption that models process all context equally is often incorrect. Research into attention mechanisms reveals a <strong>U-shaped performance curve</strong> (often referred to as the “Lost in the Middle” phenomenon). Although newer long-context architectures are mitigating this issue, information at the boundaries (beginning and end) is still typically prioritized over the middle.</p> <p>The direction of the bias depends on the architecture:</p> <ul> <li><strong>Primacy Bias</strong>: Causal masking creates an inherent preference for early tokens. Larger, more capable models often favor the first examples presented.</li> <li><strong>Recency Bias</strong>: Mechanisms like Rotary Positional Embeddings (RoPE) and attention weight decay can cause the model to favor the most recent tokens. This is often more pronounced in smaller models or when the semantic quality of options is low.</li> </ul> <h3 id="experiment-the-best-last-strategy">Experiment: The Best-Last Strategy</h3> <p>For context-dependent tasks, such as intent classification or complex reasoning, we can test the hypothesis that maximizing recency helps the model.</p> <p>Instead of random placement, we can order examples by <strong>semantic similarity</strong>. The logic is to place the examples most similar to the current query closest to the generation point (the end of the prompt).</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.metrics.pairwise</span> <span class="kn">import</span> <span class="n">cosine_similarity</span>

<span class="k">def</span> <span class="nf">order_examples_best_last</span><span class="p">(</span><span class="n">query_embedding</span><span class="p">,</span> <span class="n">example_embeddings</span><span class="p">,</span> <span class="n">examples</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Sorts examples by similarity to the query in ascending order.
    The most relevant example appears last (closest to generation).
    </span><span class="sh">"""</span>
    <span class="n">similarities</span> <span class="o">=</span> <span class="nf">cosine_similarity</span><span class="p">(</span>
        <span class="p">[</span><span class="n">query_embedding</span><span class="p">],</span>
        <span class="n">example_embeddings</span>
    <span class="p">)[</span><span class="mi">0</span><span class="p">]</span>

    <span class="c1"># Sort ascending: least similar first, most similar last
</span>    <span class="n">sorted_indices</span> <span class="o">=</span> <span class="n">similarities</span><span class="p">.</span><span class="nf">argsort</span><span class="p">()</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">examples</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">sorted_indices</span><span class="p">]</span>

<span class="c1"># Usage
</span><span class="n">ordered</span> <span class="o">=</span> <span class="nf">order_examples_best_last</span><span class="p">(</span>
    <span class="n">query_emb</span><span class="p">,</span>
    <span class="n">few_shot_embs</span><span class="p">,</span>
    <span class="n">few_shot_examples</span>
<span class="p">)</span>
<span class="n">prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">instruction</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span> <span class="o">+</span> <span class="sh">"</span><span class="se">\n</span><span class="sh">"</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="n">ordered</span><span class="p">)</span> <span class="o">+</span> <span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="sh">"</span>
</code></pre></div></div> <h3 id="observations-and-impact">Observations and Impact</h3> <p>Research on intent recognition datasets shows this “best last” strategy <strong>increased</strong> accuracy by <strong>0.2–1.7%</strong>.</p> <p>It is important to note two constraints:</p> <ol> <li><strong>Semantic Variance</strong>: The effect correlates with variance; if all examples are equidistant from the query, ordering impact diminishes.</li> <li><strong>Model Size</strong>: As noted in the mechanism section, larger models might benefit from a “best first” strategy due to primacy bias.</li> </ol> <p>For tasks with limited context, this difference seems marginal. However, in production environments, prompt ordering is a zero-cost hyperparameter. It requires no model fine-tuning and no additional inference latency.</p> <h3 id="broader-application">Broader Application</h3> <p>This principle extends beyond classification. Instruction-tuned models can flip preferences based solely on the order of presentation. This positional bias is particularly evident at higher temperatures ($T=1$), where adherence to ambiguous instructions can fluctuate.</p> <h3 id="conclusion">Conclusion</h3> <p>Positional bias is an architectural reality. It is not necessarily a bug to be fixed, but a behavior to be characterized.</p> <p>If you observe inconsistent performance in your few-shot tasks, don’t assume the model attends equally to all examples. Verify the distribution of your examples. If you feel that the model ignores your instructions, check the order—your best examples might be lost in the middle.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Investigating positional bias in few-shot prompting. While 'Lost in the Middle' suggests boundary importance, the specific ordering of examples remains an important factor for performance stability.]]></summary></entry><entry><title type="html">Temporal for LLM Pipelines: Durable Execution Starter Pack</title><link href="https://avchauzov.github.io/blog/2025/temporal-llm-pipelines-durable-execution/" rel="alternate" type="text/html" title="Temporal for LLM Pipelines: Durable Execution Starter Pack"/><published>2025-11-10T00:00:00+00:00</published><updated>2025-11-10T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/temporal-llm-pipelines-durable-execution</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/temporal-llm-pipelines-durable-execution/"><![CDATA[<p>LLM pipelines are fragile, often crashing mid-process from API timeouts, rate limiting, or worker evictions. When a worker restarts, its state is gone, forcing potentially expensive re-runs that burn tokens on already completed work. Traditional orchestration tools like Airflow or Prefect get applied here, but they are mostly designed for batch ETL and don’t handle state well for multi-hour agent loops.</p> <p>Temporal solves this with <strong>Durable Execution</strong>. This is a programming model that:</p> <ul> <li>Lets processes resume exactly where they failed.</li> <li>Avoids re-executing expensive steps that already completed.</li> </ul> <h2 id="what-temporal-provides">What Temporal Provides</h2> <p>Temporal is an orchestration platform built on three core primitives:</p> <p><strong>Workflows</strong> define business logic as code. A workflow can run for days or months, maintaining state in local variables that survive process crashes. The platform automatically persists execution history through event sourcing, allowing deterministic replay from any failure point. This eliminates external checkpointing systems.</p> <p><strong>Activities</strong> encapsulate non-deterministic operations—LLM API calls, database queries, external service interactions. Temporal applies configurable retry policies with exponential backoff automatically. Workflows don’t track individual retry attempts—that happens automatically in the activity layer.</p> <p><strong>Event History</strong> records every workflow execution step (activity scheduling, completion, input/output data) as an immutable audit log. You get Time-Travel Debugging—replaying production failures locally with identical state reconstruction. For debugging non-deterministic AI behavior, this history lets you replay exact execution paths.</p> <p>Workers poll task queues, take work, execute code, return results. Scale by adding more workers. State lives in Temporal Server’s event log, not worker memory.</p> <h2 id="when-to-use-temporal">When to Use Temporal</h2> <p>The decision depends on pipeline characteristics and operational constraints.</p> <p><strong>Use Temporal when</strong>:</p> <ul> <li><strong>Long-running processes</strong> (hours/days/months) where losing progress is expensive, <strong>such as</strong> multi-stage document analysis, conversational agents maintaining long-term state, or financial audit workflows.</li> <li><strong>Critical fault tolerance</strong> requirements, <strong>for example</strong> in payment processing triggered by LLM agents, medical diagnosis pipelines, or any scenario requiring compensating transactions (part of the “Saga” pattern) to handle rollbacks.</li> <li><strong>Dynamic agent loops</strong> where next steps are determined by LLM output at runtime, <strong>including</strong> ReAct agents, tool selection workflows, or multi-agent coordination that cannot be expressed as static DAGs.</li> <li><strong>Human-in-the-loop</strong> approval flows, <strong>like</strong> HR recruitment or compliance pipelines, where processes must pause indefinitely for user input and resume without state loss.</li> </ul> <p><strong>Don’t use Temporal for</strong>:</p> <ul> <li><strong>Simple batch jobs</strong> (&lt;10 minutes) with low failure risk, <strong>like</strong> standard ETL/ELT tasks where Prefect or Airflow provide faster time-to-value with less operational overhead.</li> <li><strong>Stateless API services</strong> requiring sub-100ms latency. Single synchronous LLM calls don’t benefit from durable execution—they add unnecessary complexity.</li> <li><strong>Rapid prototyping</strong> where determinism constraints slow iteration. Temporal requires learning distributed systems patterns (Event Sourcing, deterministic workflows) that increase initial development friction.</li> </ul> <p><strong>Key tradeoff</strong>: Temporal’s core workflow code must be <strong>strictly deterministic</strong>. All non-deterministic operations (LLM calls, <code class="language-plaintext highlighter-rouge">time.now()</code>, random number generation) must be isolated in Activities. Violating this causes non-deterministic errors during event replay. This is a steep learning curve for AI developers used to standard imperative Python.</p> <h2 id="practical-example-refactoring-an-llm-pipeline">Practical Example: Refactoring an LLM Pipeline</h2> <p>Start with a fragile document parser that needs reliability. We’ll refactor it step-by-step (incremental migration) instead of rewriting everything.</p> <h3 id="original-pipeline">Original Pipeline</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">parse_document</span><span class="p">(</span><span class="n">doc_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="n">doc</span> <span class="o">=</span> <span class="n">db</span><span class="p">.</span><span class="nf">fetch</span><span class="p">(</span><span class="n">doc_id</span><span class="p">)</span>
    <span class="n">parsed</span> <span class="o">=</span> <span class="nf">extract_text</span><span class="p">(</span><span class="n">doc</span><span class="p">.</span><span class="n">path</span><span class="p">)</span>
    <span class="n">summary</span> <span class="o">=</span> <span class="nf">openai_call</span><span class="p">(</span><span class="n">parsed</span><span class="p">)</span>  <span class="c1"># Fails on rate limit
</span>    <span class="n">db</span><span class="p">.</span><span class="nf">save_summary</span><span class="p">(</span><span class="n">doc_id</span><span class="p">,</span> <span class="n">summary</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Problems</strong>: no state preservation, no retry logic, crashes lose progress.</p> <h3 id="step-1-basic-workflow-with-durable-state">Step 1: Basic Workflow with Durable State</h3> <ul> <li><strong>Problem</strong>: The original <code class="language-plaintext highlighter-rouge">parse_document</code> function is completely stateless. If the worker crashes, everything is lost, including the <code class="language-plaintext highlighter-rouge">doc_id</code> being processed.</li> <li><strong>Change</strong>: We move from a simple Python function to a <code class="language-plaintext highlighter-rouge">DocumentWorkflow</code> class. State is now held in <code class="language-plaintext highlighter-rouge">self.state</code>, which Temporal automatically persists.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">temporalio</span> <span class="kn">import</span> <span class="n">workflow</span>

<span class="nd">@workflow.defn</span>
<span class="k">class</span> <span class="nc">DocumentWorkflow</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">parsed_text</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">pending</span><span class="sh">"</span>
        <span class="p">}</span>

    <span class="nd">@workflow.run</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">doc_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">doc_id</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">processing</span><span class="sh">"</span>

        <span class="c1"># Activities will go here
</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span>
</code></pre></div></div> <p><strong>Pattern</strong>: Durable state in class variables. Temporal persists <code class="language-plaintext highlighter-rouge">self.state</code> automatically via event sourcing. Worker crashes no longer lose context.</p> <h3 id="step-2-llm-activity-with-retry-policy">Step 2: LLM Activity with Retry Policy</h3> <ul> <li><strong>Problem</strong>: Our <code class="language-plaintext highlighter-rouge">openai_call</code> has no retry logic. If it fails due to a rate limit (HTTP 429), the entire workflow fails instead of just waiting and trying again.</li> <li><strong>Change</strong>: We wrap <code class="language-plaintext highlighter-rouge">openai_call</code> in an <code class="language-plaintext highlighter-rouge">Activity</code> (<code class="language-plaintext highlighter-rouge">generate_summary</code>). This isolates the non-deterministic code and allows the Workflow to add a <code class="language-plaintext highlighter-rouge">RetryPolicy</code>.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">temporalio</span> <span class="kn">import</span> <span class="n">activity</span>
<span class="kn">from</span> <span class="n">datetime</span> <span class="kn">import</span> <span class="n">timedelta</span>
<span class="c1"># Assuming 'openai' is initialized as AsyncOpenAI client
# from openai import AsyncOpenAI
# client = AsyncOpenAI()
</span>
<span class="nd">@activity.defn</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">generate_summary</span><span class="p">(</span><span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">response</span> <span class="o">=</span> <span class="k">await</span> <span class="n">openai</span><span class="p">.</span><span class="n">chat</span><span class="p">.</span><span class="n">completions</span><span class="p">.</span><span class="nf">create</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Summarize: </span><span class="si">{</span><span class="n">text</span><span class="si">}</span><span class="sh">"</span><span class="p">}]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">choices</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">message</span><span class="p">.</span><span class="n">content</span>

<span class="c1"># In workflow:
</span><span class="kn">from</span> <span class="n">temporalio.common</span> <span class="kn">import</span> <span class="n">RetryPolicy</span>

<span class="n">summary</span> <span class="o">=</span> <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">execute_activity</span><span class="p">(</span>
    <span class="n">generate_summary</span><span class="p">,</span>
    <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">parsed_text</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">retry_policy</span><span class="o">=</span><span class="nc">RetryPolicy</span><span class="p">(</span>
        <span class="n">initial_interval</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">1</span><span class="p">),</span>
        <span class="n">backoff_coefficient</span><span class="o">=</span><span class="mf">2.0</span><span class="p">,</span>
        <span class="n">maximum_attempts</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
        <span class="n">maximum_interval</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">60</span><span class="p">),</span>
        <span class="n">non_retryable_error_types</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">InvalidAPIKeyError</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">AuthenticationError</span><span class="sh">"</span><span class="p">]</span>
    <span class="p">),</span>
    <span class="n">start_to_close_timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
<span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">summary</span>
</code></pre></div></div> <p><strong>Pattern</strong>: Activities encapsulate non-deterministic LLM calls. Retry policy handles rate limits (HTTP 429) automatically with exponential backoff. <code class="language-plaintext highlighter-rouge">non_retryable_error_types</code> prevents wasted retries on permanent failures like auth errors.</p> <p><code class="language-plaintext highlighter-rouge">start_to_close_timeout</code> (30s) caps single attempt duration. <code class="language-plaintext highlighter-rouge">schedule_to_close_timeout</code> would cap total time including all retries—set higher (e.g., 5 minutes) to accommodate backoff periods.</p> <h3 id="step-3-human-in-the-loop-via-signals">Step 3: Human-in-the-Loop via Signals</h3> <ul> <li><strong>Problem</strong>: The process is fully automated. It cannot pause and wait for a human to approve the generated summary before saving it to the database.</li> <li><strong>Change</strong>: We add <code class="language-plaintext highlighter-rouge">workflow.wait_condition</code> to allow the workflow to “sleep” (without consuming worker resources). We also add a <code class="language-plaintext highlighter-rouge">Signal</code> (<code class="language-plaintext highlighter-rouge">approve_summary</code>) to let an external user “wake” the workflow with an input.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@workflow.defn</span>
<span class="k">class</span> <span class="nc">DocumentWorkflow</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="p">{...}</span>
        <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="o">=</span> <span class="bp">None</span>  <span class="c1"># Signal target
</span>
    <span class="nd">@workflow.signal</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">approve_summary</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">approved</span><span class="p">:</span> <span class="nb">bool</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="o">=</span> <span class="n">approved</span>

    <span class="nd">@workflow.run</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">doc_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
        <span class="c1"># ... generate summary ...
</span>
        <span class="c1"># Wait for human approval (can wait hours/days)
</span>        <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">wait_condition</span><span class="p">(</span>
            <span class="k">lambda</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">,</span>
            <span class="n">timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">days</span><span class="o">=</span><span class="mi">7</span><span class="p">)</span>
        <span class="p">)</span>

        <span class="k">if</span> <span class="ow">not</span> <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">rejected</span><span class="sh">"</span>
            <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span>  <span class="c1"># Compensating action
</span>
        <span class="c1"># Continue processing...
</span></code></pre></div></div> <p><strong>Pattern</strong>: Workflows can pause via <code class="language-plaintext highlighter-rouge">wait_condition</code>. This pause consumes no worker resources and can last indefinitely. Use <code class="language-plaintext highlighter-rouge">timeout</code> to prevent infinite waits—best practice shown in the code.</p> <h3 id="step-4-query-for-status-monitoring">Step 4: Query for Status Monitoring</h3> <ul> <li><strong>Problem</strong>: We have no idea what’s happening inside a running workflow. Is it stuck or waiting for approval? The only way to know is to check logs or the database.</li> <li><strong>Change</strong>: We add a <code class="language-plaintext highlighter-rouge">@workflow.query</code> method (<code class="language-plaintext highlighter-rouge">get_status</code>). This lets our UI (or any other service) synchronously read the workflow’s internal <code class="language-plaintext highlighter-rouge">self.state</code> at any time without interrupting it.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@workflow.query</span>
<span class="k">def</span> <span class="nf">get_status</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="k">return</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">],</span>
        <span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">],</span>
        <span class="sh">"</span><span class="s">progress</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">awaiting_approval</span><span class="sh">"</span> <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="ow">is</span> <span class="bp">None</span> <span class="k">else</span> <span class="sh">"</span><span class="s">processing</span><span class="sh">"</span>
    <span class="p">}</span>
</code></pre></div></div> <p><strong>Pattern</strong>: Queries provide synchronous state access for UIs/dashboards without interrupting workflow execution. They are read-only by design (meaning they cannot change workflow state, only view it).</p> <h3 id="step-5-gpu-resource-isolation-via-task-queues">Step 5: GPU Resource Isolation via Task Queues</h3> <ul> <li><strong>Problem</strong>: We have an expensive <code class="language-plaintext highlighter-rouge">run_local_model</code> Activity that requires a GPU. If a normal CPU worker picks it up, it will either crash (OOM) or block other tasks.</li> <li><strong>Change</strong>: When executing the activity, we specify <code class="language-plaintext highlighter-rouge">task_queue="gpu-workers"</code>. This ensures only workers specifically configured to listen to that queue (and deployed on GPU machines) will execute this task.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@activity.defn</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">run_local_model</span><span class="p">(</span><span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="c1"># Requires GPU, expensive
</span>    <span class="n">model</span> <span class="o">=</span> <span class="nf">load_model</span><span class="p">()</span>
    <span class="k">return</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>

<span class="c1"># In workflow:
</span><span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">execute_activity</span><span class="p">(</span>
    <span class="n">run_local_model</span><span class="p">,</span>
    <span class="n">text</span><span class="p">,</span>
    <span class="n">task_queue</span><span class="o">=</span><span class="sh">"</span><span class="s">gpu-workers</span><span class="sh">"</span><span class="p">,</span>  <span class="c1"># Dedicated queue
</span>    <span class="n">start_to_close_timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">minutes</span><span class="o">=</span><span class="mi">10</span><span class="p">)</span>
<span class="p">)</span>

<span class="c1"># Worker configuration
</span><span class="n">worker</span> <span class="o">=</span> <span class="nc">Worker</span><span class="p">(</span>
    <span class="n">client</span><span class="p">,</span>
    <span class="n">task_queue</span><span class="o">=</span><span class="sh">"</span><span class="s">gpu-workers</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">workflows</span><span class="o">=</span><span class="p">[</span><span class="n">DocumentWorkflow</span><span class="p">],</span>
    <span class="n">activities</span><span class="o">=</span><span class="p">[</span><span class="n">run_local_model</span><span class="p">],</span>
    <span class="n">max_concurrent_activities</span><span class="o">=</span><span class="mi">2</span>  <span class="c1"># Only 2 parallel GPU tasks
</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Pattern</strong>: Task queues isolate resource-intensive activities. Deploy workers with limited activity slots on GPU machines, preventing cascading failures and controlling costs. Implements Bulkhead pattern.</p> <h3 id="step-6-continue-as-new-for-long-processes">Step 6: Continue-As-New for Long Processes</h3> <ul> <li><strong>Problem</strong>: If the workflow processes 10,000 documents in a loop or runs for months (like a chatbot), its Event History becomes huge. This slows down replay/recovery.</li> <li><strong>Change</strong>: We periodically check the history size (<code class="language-plaintext highlighter-rouge">workflow.info().get_current_history_length()</code>). If it exceeds a threshold, we use <code class="language-plaintext highlighter-rouge">workflow.continue_as_new()</code> to start a fresh workflow execution, carrying over the state but with a clean history.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@workflow.run</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">doc_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">iteration</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">0</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="c1"># Process document...
</span>
    <span class="c1"># Check history size
</span>    <span class="n">history_length</span> <span class="o">=</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">info</span><span class="p">().</span><span class="nf">get_current_history_length</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">history_length</span> <span class="o">&gt;</span> <span class="mi">1000</span><span class="p">:</span>
        <span class="c1"># Start fresh workflow execution with current state
</span>        <span class="n">workflow</span><span class="p">.</span><span class="nf">continue_as_new</span><span class="p">(</span><span class="n">doc_id</span><span class="p">,</span> <span class="n">iteration</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span>
</code></pre></div></div> <p><strong>Pattern</strong>: Continue-as-new prevents event history bloat by starting a new workflow execution with a clean history. <strong>Warning</strong>: This is not a “continue”; it’s a “restart”. The new run loses all <code class="language-plaintext highlighter-rouge">self.state</code> unless you explicitly pass the necessary data as arguments to the <code class="language-plaintext highlighter-rouge">continue_as_new</code> call.</p> <h3 id="complete-integration">Complete Integration</h3> <p>Full example with error handling:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@workflow.defn</span>
<span class="k">class</span> <span class="nc">DocumentWorkflow</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span> <span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">:</span> <span class="bp">None</span><span class="p">,</span> <span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">pending</span><span class="sh">"</span><span class="p">}</span>
        <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="o">=</span> <span class="bp">None</span>

    <span class="nd">@workflow.run</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">run</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">doc_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">doc_id</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">doc_id</span>

        <span class="k">try</span><span class="p">:</span>
            <span class="c1"># Fetch with retry
</span>            <span class="n">doc</span> <span class="o">=</span> <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">execute_activity</span><span class="p">(</span>
                <span class="n">fetch_document</span><span class="p">,</span>
                <span class="n">doc_id</span><span class="p">,</span>
                <span class="n">retry_policy</span><span class="o">=</span><span class="nc">RetryPolicy</span><span class="p">(</span><span class="n">maximum_attempts</span><span class="o">=</span><span class="mi">3</span><span class="p">),</span>
                <span class="n">start_to_close_timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">10</span><span class="p">)</span>
            <span class="p">)</span>

            <span class="c1"># LLM with rate limit handling
</span>            <span class="n">summary</span> <span class="o">=</span> <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">execute_activity</span><span class="p">(</span>
                <span class="n">generate_summary</span><span class="p">,</span>
                <span class="n">doc</span><span class="p">.</span><span class="n">text</span><span class="p">,</span>
                <span class="n">retry_policy</span><span class="o">=</span><span class="nc">RetryPolicy</span><span class="p">(</span>
                    <span class="n">backoff_coefficient</span><span class="o">=</span><span class="mf">2.0</span><span class="p">,</span>
                    <span class="n">maximum_attempts</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
                    <span class="n">non_retryable_error_types</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">InvalidAPIKeyError</span><span class="sh">"</span><span class="p">]</span>
                <span class="p">),</span>
                <span class="n">start_to_close_timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
            <span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">summary</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">summary</span>

            <span class="c1"># Human approval
</span>            <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">wait_condition</span><span class="p">(</span>
                <span class="k">lambda</span><span class="p">:</span> <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">,</span>
                <span class="n">timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">hours</span><span class="o">=</span><span class="mi">24</span><span class="p">)</span>
            <span class="p">)</span>

            <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span><span class="p">:</span>
                <span class="k">await</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">execute_activity</span><span class="p">(</span>
                    <span class="n">save_results</span><span class="p">,</span>
                    <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">,</span>
                    <span class="n">start_to_close_timeout</span><span class="o">=</span><span class="nf">timedelta</span><span class="p">(</span><span class="n">seconds</span><span class="o">=</span><span class="mi">10</span><span class="p">)</span>
                <span class="p">)</span>
                <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">completed</span><span class="sh">"</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">rejected</span><span class="sh">"</span>

        <span class="k">except</span> <span class="nb">Exception</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="sh">"</span><span class="s">failed</span><span class="sh">"</span>
            <span class="n">self</span><span class="p">.</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">error</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="nf">str</span><span class="p">(</span><span class="n">e</span><span class="p">)</span>

        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span>

    <span class="nd">@workflow.signal</span>
    <span class="k">async</span> <span class="k">def</span> <span class="nf">approve_summary</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">approved</span><span class="p">:</span> <span class="nb">bool</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">approval_received</span> <span class="o">=</span> <span class="n">approved</span>

    <span class="nd">@workflow.query</span>
    <span class="k">def</span> <span class="nf">get_status</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">state</span>
</code></pre></div></div> <h2 id="best-practices">Best Practices</h2> <p><strong>Use exponential backoff for rate limits</strong>. Set <code class="language-plaintext highlighter-rouge">backoff_coefficient</code> &gt; 1.5 (typically 2.0) and <code class="language-plaintext highlighter-rouge">maximum_interval</code> to prevent retry storms. Add jitter if calling shared services to avoid thundering herd effects.</p> <p><strong>Store references, not large payloads</strong>. Event History isn’t designed for petabyte storage. For large LLM outputs or document content, store only S3 URLs or database IDs in workflow state. Fetch actual data in Activities.</p> <p><strong>Monitor Event History size</strong>. Workflows exceeding 1000–2000 events should implement Continue-as-new. High event counts degrade replay performance and risk hitting platform limits.</p> <p><strong>Set appropriate timeouts</strong>. <code class="language-plaintext highlighter-rouge">start_to_close_timeout</code> should exceed typical activity duration but fail stuck calls quickly. <code class="language-plaintext highlighter-rouge">schedule_to_close_timeout</code> should account for total retry time including backoff periods. Missing timeouts risk infinite hangs.</p> <p><strong>Make Activities idempotent</strong>. Temporal guarantees at-least-once execution. Activities may execute multiple times. Use idempotency keys for payments or other non-repeatable operations.</p> <p><strong>Use Task Queues for resource isolation</strong>. Deploy GPU workers on dedicated queues with <code class="language-plaintext highlighter-rouge">max_concurrent_activities</code> limits. This implements Bulkhead pattern, preventing resource exhaustion and controlling costs.</p> <h2 id="conclusion">Conclusion</h2> <p>Temporal trades upfront complexity for long-term reliability. The determinism constraint and operational overhead (self-hosted requires Cassandra/Postgres, Elasticsearch for Visibility) make it overkill for simple batch jobs or stateless services.</p> <p>For LLM pipelines with expensive API calls, multi-hour execution times, or critical fault tolerance requirements, the architecture works. Automatic state recovery, configurable retry policies, and Time-Travel Debugging fix entire classes of production failures.</p> <p>Add Temporal when failure recovery and durable orchestration become blocking issues, not as a default architecture choice.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[LLM agents often crash, losing state and expensive API work. Temporal provides durable execution for LLM pipelines: automatic state recovery, configurable retries, and long-running orchestration at the cost of determinism constraints and ops overhead.]]></summary></entry><entry><title type="html">GraphRAG: Beyond Vector Search for Connecting the Dots</title><link href="https://avchauzov.github.io/blog/2025/graphrag-beyond-vector-search/" rel="alternate" type="text/html" title="GraphRAG: Beyond Vector Search for Connecting the Dots"/><published>2025-11-07T00:00:00+00:00</published><updated>2025-11-07T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/graphrag-beyond-vector-search</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/graphrag-beyond-vector-search/"><![CDATA[<p>Vector search (RAG) works well for retrieving semantically similar chunks. This is fine for many information retrieval tasks. It struggles, however, with queries that require <strong>structural reasoning</strong> across multiple, logically-connected facts—facts that may not be semantically similar or appear in the same document.</p> <p>GraphRAG builds a knowledge graph to handle these multi-hop queries. It extracts entities and relationships into a structure that allows for explicit traversal. But this approach adds significant indexing costs and potential query latency, which requires careful trade-offs.</p> <h3 id="what-is-graphrag">What is GraphRAG?</h3> <p>To understand GraphRAG, let’s look at the baseline.</p> <p><strong>Baseline RAG</strong> relies on <strong>semantic similarity search</strong>. Documents are chunked, embedded, and indexed. A query finds the top-k similar chunks for the LLM. This approach fails when a query’s logic depends on connections between distant entities.</p> <p><strong>GraphRAG</strong> works differently. During indexing, it uses LLMs to extract <strong>entities</strong> (like “people”, “products”) and their <strong>relationships</strong> (like “works_for”, “bought”) from the text to build a graph.</p> <p>At query time, it uses a hybrid approach:</p> <ol> <li><strong>Graph traversal</strong> (e.g., Cypher) for precise, structured facts.</li> <li><strong>Vector search</strong> on node properties for semantic relevance.</li> <li><strong>Community summaries</strong> (hierarchical clustering) for corpus-level questions.</li> </ol> <p>This allows GraphRAG to trace a path like <code class="language-plaintext highlighter-rouge">Employee -&gt; WORKS_ON -&gt; Project A -&gt; USES -&gt; Data Center -&gt; IN_REGION -&gt; Europe</code>, instead of just finding separate documents about “Project A” and “data centers”.</p> <h3 id="graphrag-pros-cons-and-trade-offs">GraphRAG: Pros, Cons, and Trade-offs</h3> <p>GraphRAG introduces a fundamental trade-off: it shifts the compute load from query-time inference to indexing-time graph construction.</p> <h3 id="pros-where-graphrag-provides-value">Pros: Where GraphRAG Provides Value</h3> <ul> <li><strong>Multi-hop Reasoning</strong>: This is the primary use case. Queries like, “Which marketing campaigns influenced customers in region X who also purchased product Y?” require tracing connections that vector search misses. Embeddings find “customers in X” and “product Y” separately; GraphRAG finds the path connecting them.</li> <li><strong>Corpus-Level Summarization</strong>: Queries like, “What are the main risks identified across all our quarterly reports?” are hard for baseline RAG, which retrieves isolated chunks. GraphRAG can use pre-computed community summaries (clusters of related entities) to provide a high-level, synthesized answer.</li> <li><strong>Explainability and Traceability</strong>: In high-stakes domains (legal, medical, finance), GraphRAG provides auditable reasoning. The answer “A is connected to C” can be traced: <code class="language-plaintext highlighter-rouge">Entity A -&gt; [RELATION] -&gt; Entity B -&gt; [RELATION] -&gt; Entity C</code>. This is easier to debug than “the model says so because these chunks had high cosine similarity.”</li> <li><strong>Structured Data Extraction &amp; Aggregation</strong>: GraphRAG is useful when the <strong>source</strong> is unstructured text, but the <strong>query</strong> requires structured operations. An LLM can translate “How many projects use our EU data center?” into a Text-to-Cypher query that performs a <code class="language-plaintext highlighter-rouge">COUNT</code> and <code class="language-plaintext highlighter-rouge">GROUP BY</code> on the graph—operations vector search cannot do. This is distinct from Text-to-SQL, which operates on data <strong>already</strong> in a structured database.</li> </ul> <h3 id="cons-the-high-cost-of-precision">Cons: The High Cost of Precision</h3> <ul> <li><strong>Extreme Indexing Cost and Time</strong>: Building the knowledge graph is token-intensive and slow. The process involves multiple LLM-heavy steps: entity extraction, relationship extraction, entity resolution (deduplication), community detection, and summary generation. This can be <strong>orders of magnitude slower and more expensive</strong> (in API calls or compute) than simply vectorizing the same data—we’re talking hours or even days for large datasets, not minutes.</li> <li><strong>High Query Latency</strong>: While graph traversal itself can be fast, complex GraphRAG queries, especially “Global Search” modes, can be extremely slow. Latency can range from <strong>4–8s</strong> for simpler graph queries to over <strong>20–40s</strong> for corpus-wide analysis. This is often unacceptable for interactive apps needing sub-second (e.g., &lt;800ms) p95 latency.</li> <li><strong>Architectural and Maintenance Complexity</strong>: The complexity skyrockets. The system now requires managing a graph database, an ETL pipeline for graph construction, and a complex query engine, not just a vector database. Updating the graph as new documents arrive is also a non-trivial process.</li> </ul> <h3 id="the-trade-off-measured-in-metrics">The Trade-off: Measured in Metrics</h3> <p>The trade-off is clear: complexity for accuracy. On multi-hop benchmarks (like 2WikiMultiHopQA), advanced GraphRAG systems can achieve <strong>+20–30% F1 score increases</strong> over baseline RAG. On these specific tasks, a fine-tuned agentic system using a small SLM might even outperform a baseline RAG system using a much larger, state-of-the-art model.</p> <h3 id="alternatives-to-computation-heavy-indexing">Alternatives to Computation-Heavy Indexing</h3> <p>Before building a full GraphRAG pipeline, check these simpler approaches.</p> <h3 id="first-optimize-baseline-rag">First: Optimize Baseline RAG</h3> <p>Instead of jumping to a complex graph architecture, first, exhaust baseline RAG. Often, query failures “attributed to ‘missing connections’” are actually retrieval failures from a poorly tuned embedding model. Fine-tuning a bi-encoder on domain data or using robust <strong>metadata filtering</strong> in a vector DB (e.g., <code class="language-plaintext highlighter-rouge">WHERE category = 'finance' AND year = 2024</code>) may solve the problem at a fraction of the cost.</p> <h3 id="alternative-fixed-entity-architecture">Alternative: Fixed Entity Architecture</h3> <p><strong>Fixed Entity Architecture (FEA)</strong> uses a fixed ontology instead of using expensive LLMs to <strong>discover</strong> entities. Define a fixed ontology (e.g., “Drug”, “Diagnosis”, “Symptom” for a medical domain). Text chunks are then attached to these entities via fast cosine similarity.</p> <ul> <li><strong>Pros</strong>: Eliminates LLM indexing costs, very fast.</li> <li><strong>Cons</strong>: Sacrifices dynamic relationship discovery; it can’t find new, unknown connections not defined in your schema.</li> <li><strong>Best for</strong>: Narrow domains where entity types are stable (e.g., corporate policies, compliance docs).</li> </ul> <h3 id="optimization-lighter-graph-methods">Optimization: Lighter Graph Methods</h3> <p>If you must build a graph, these methods can reduce costs.</p> <ul> <li><strong>Lighter Extraction Models</strong>: Instead of massive LLMs, use smaller, <strong>specialized models for relation extraction</strong>. Fine-tuned open-source SLMs or older Seq2Seq architectures can achieve significant cost reduction (e.g., <strong>80%+</strong>) for extraction tasks.</li> <li><strong>Prompt Engineering for Graphs</strong>: Techniques exist to encode graph structure into <strong>“soft prompts”</strong>. Instead of feeding the LLM long, raw-text descriptions of graph relationships (which consumes the context window), this method encodes the relevant graph structure into a compact set of vectors (the “soft prompt”). These vectors are fed to the model, guiding its reasoning without the high token overhead.</li> <li><strong>Asynchronous Updates</strong>: Decouple expensive graph updates from live queries. Process new documents and update the graph in batches during off-peak hours (i.e., “sleep-time consolidation”) rather than in real-time. This is a common strategy for production systems. This batch-update strategy fails, however, when queries must reflect data that arrived seconds ago (e.g., fraud detection, real-time agent logs). These cases require complex hybrid systems capable of incremental, real-time graph updates.</li> </ul> <h3 id="best-practices-and-decision-framework">Best Practices and Decision Framework</h3> <p><strong>When to Choose GraphRAG</strong>:</p> <ul> <li>When you have <strong>systematic multi-hop query failures</strong> (as discussed in “Pros”).</li> <li>When you need <strong>strict answer traceability</strong> and explainability.</li> <li>When queries require <strong>corpus-level aggregation</strong> that simple chunking can’t answer.</li> </ul> <p><strong>When to Skip GraphRAG</strong>:</p> <ul> <li>If your current RAG accuracy <strong>already meets business requirements</strong>.</li> <li>If you have a <strong>strict low-latency budget</strong> (&lt;500ms) for all queries.</li> <li>If data churns rapidly, but you <em>don’t</em> have multi-hop query requirements.</li> <li>If the <strong>10–100x indexing cost</strong> isn’t justified by the precision gain.</li> </ul> <p><strong>Choosing Your Architecture</strong>:</p> <ul> <li><strong>Full GraphRAG</strong>: For complex, dynamic datasets where relationship discovery is key.</li> <li><strong>Fixed Entity Architecture (FEA)</strong>: For narrow domains with stable schemas and a need to minimize indexing cost.</li> <li><strong>Hybrid Systems</strong>: Architectures designed to balance latency and power, often using incremental updates for real-time data.</li> </ul> <p><strong>Tooling and Prototyping</strong>: Prototype with libraries like <strong>LlamaIndex (Property Graph Index)</strong> or <strong>LangChain (integrations with graph DBs like Neo4j)</strong>. For production, evaluate dedicated open-source frameworks that focus on specific trade-offs, like real-time latency or indexing efficiency.</p> <p><strong>Measuring Success</strong>: Measure success against the specific failures you’re fixing:</p> <ul> <li><strong>F1/Accuracy on your multi-hop question set</strong> (target a significant lift, e.g., +20–30%).</li> <li><strong>Query latency (p95)</strong>, especially for interactive use.</li> <li><strong>Total indexing cost and time</strong>.</li> <li><strong>Qualitative improvement</strong> in answer explainability.</li> </ul> <hr/> <p align="center"> <i>The fundamental question: does your use case require connecting logically related but semantically distant facts? If yes, and if the indexing cost and architectural complexity are acceptable, GraphRAG delivers measurable improvements. If queries are satisfied by semantic similarity alone, embeddings remain the simpler, faster, cheaper solution.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Vector search finds similar text while GraphRAG finds connected facts. A look at the trade-offs, high indexing costs, and lighter-weight alternatives.]]></summary></entry><entry><title type="html">Domain-Driven Design for AI Systems: Architectural Patterns and Production Experience</title><link href="https://avchauzov.github.io/blog/2025/domain-driven-design-ai-systems/" rel="alternate" type="text/html" title="Domain-Driven Design for AI Systems: Architectural Patterns and Production Experience"/><published>2025-11-04T00:00:00+00:00</published><updated>2025-11-04T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/domain-driven-design-ai-systems</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/domain-driven-design-ai-systems/"><![CDATA[<p>Large-scale AI systems have evolved far beyond isolated models. They are now complex ecosystems of services, agents, and pipelines — each with its own data, logic, and failure modes. Without structure, they quickly turn into what engineers call a big ball of mud: overlapping contexts, duplicated logic, and untraceable errors. Domain-Driven Design (DDD), originally formalized by Eric Evans, provides the architectural discipline needed to tame that chaos.</p> <p>While it originated in enterprise systems, DDD maps naturally to AI architectures — especially those involving LLMs, retrieval pipelines, and multi-agent coordination. This article explores how DDD principles enable modularity, safety, and traceability in production AI systems. It introduces architectural patterns, performance trade-offs, and code examples drawn from real deployments.</p> <h1 id="core-domain-driven-design-patterns-for-ai-systems">Core Domain-Driven Design Patterns for AI Systems</h1> <p>This section consolidates the key concepts, architectural patterns, and production practices into a single flow, showing how each DDD idea is applied comprehensively from theory to production.</p> <h2 id="1-bounded-contexts--agent-isolation">1. Bounded Contexts → Agent Isolation</h2> <p>A <strong>Bounded Context (BC)</strong> defines a static, architectural boundary where all terms and models have consistent meaning. It isolates domains at a design level.</p> <ul> <li><strong>Without Bounded Contexts</strong>: A single, monolithic “Support Agent” tries to handle everything: billing questions, technical support, and account upgrades. Its prompt is massive, its vector store is polluted with all document types, and it constantly confuses billing rules with technical troubleshooting steps, leading to high error rates.</li> <li><strong>With Bounded Contexts</strong>: The system is split into multiple agents, each in its own BC. A <code class="language-plaintext highlighter-rouge">BillingAgent</code> (BC_Billing) only has access to billing documents and only knows how to handle payments. A <code class="language-plaintext highlighter-rouge">TechSupportAgent</code> (BC_Support) only sees technical manuals. This isolation prevents context pollution and makes each agent smaller, faster, and more accurate.</li> </ul> <p>In multi-agent LLM systems, each agent—or agent group—should be a BC.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+----------------------+     +----------------------+
|   TechSupport BC     |&lt;---&gt;|     Billing BC       |
| (TechSupport Agent)  |     |   (Billing Agent)    |
+----------------------+     +----------------------+
</code></pre></div></div> <p>The problem of “agent sprawl” is solved by aligning each agent with a specific Bounded Context. Production metrics from representative implementations show clear gains:</p> <hr/> <table> <thead> <tr> <th style="text-align: left">Metric</th> <th style="text-align: left">Before (Shared Context)</th> <th style="text-align: left">After (DDD Bounded Context)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Token Usage / query</td> <td style="text-align: left">11,200</td> <td style="text-align: left">7,800 (<strong>-30%</strong>)</td> </tr> <tr> <td style="text-align: left">Latency (ms P95)</td> <td style="text-align: left">980</td> <td style="text-align: left">640</td> </tr> <tr> <td style="text-align: left">Error Recovery Rate</td> <td style="text-align: left"><strong>61%</strong></td> <td style="text-align: left"><strong>88%</strong></td> </tr> </tbody> </table> <hr/> <h2 id="2-context-engineering--tactical-context-selection">2. Context Engineering → Tactical Context Selection</h2> <p>Once the architectural boundary (BC) is defined, the next challenge is dynamically managing the information within it. This is the role of <strong>Context Engineering (CE)</strong>.</p> <ul> <li><strong>Without CE</strong>: A prompt is naively built with 1,000 documents from the <code class="language-plaintext highlighter-rouge">Billing</code> vector store, resulting in a slow, expensive 20,000-token prompt.</li> <li><strong>With CE</strong>: The pattern is applied: <code class="language-plaintext highlighter-rouge">Isolate</code> the context to only the <code class="language-plaintext highlighter-rouge">Billing</code> domain (the BC), then <code class="language-plaintext highlighter-rouge">Select</code> (via RAG) only the top 3 relevant documents for this specific user’s question. The result is a fast, cheap, and focused 3,000-token prompt.</li> </ul> <p>This tactical application of DDD patterns is what drives efficiency. The <code class="language-plaintext highlighter-rouge">Select</code> step often involves filtering vector search results by metadata before passing them to the LLM.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Example: Select step in Context Engineering
</span><span class="k">def</span> <span class="nf">get_context_for_user</span><span class="p">(</span><span class="n">query</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">user_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="c1"># Select: Retrieve documents that match the query AND the user_id metadata
</span>    <span class="n">results</span> <span class="o">=</span> <span class="n">vector_store</span><span class="p">.</span><span class="nf">similarity_search</span><span class="p">(</span>
        <span class="n">query</span><span class="p">,</span>
        <span class="n">k</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
        <span class="nb">filter</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">user_id</span><span class="sh">"</span><span class="p">:</span> <span class="n">user_id</span><span class="p">}</span> <span class="c1"># Tactical filtering
</span>    <span class="p">)</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">doc</span><span class="p">.</span><span class="n">page_content</span> <span class="k">for</span> <span class="n">doc</span> <span class="ow">in</span> <span class="n">results</span><span class="p">]</span>
</code></pre></div></div> <hr/> <table> <thead> <tr> <th style="text-align: left">Step</th> <th style="text-align: left">DDD Relation</th> <th style="text-align: left">Purpose</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Write</td> <td style="text-align: left">Aggregate</td> <td style="text-align: left">Capture new facts as domain events</td> </tr> <tr> <td style="text-align: left">Compress</td> <td style="text-align: left">Repository</td> <td style="text-align: left">Deduplicate and store state (e.g., summarize chat history)</td> </tr> <tr> <td style="text-align: left">Isolate</td> <td style="text-align: left">Bounded Context</td> <td style="text-align: left">Limit context window to domain-specific scope</td> </tr> <tr> <td style="text-align: left">Select</td> <td style="text-align: left">Query Model</td> <td style="text-align: left">Retrieve minimal relevant context for task</td> </tr> </tbody> </table> <hr/> <p>Applying CE inside BCs reduced prompt size by <strong>55%</strong> and inference latency by <strong>40%</strong> on average in enterprise chat workloads.</p> <h2 id="3-trust-boundaries--agent-security">3. Trust Boundaries → Agent Security</h2> <p>While a BC isolates the domain and CE filters its content, a <strong>Trust Boundary</strong> secures the agent at runtime. It ensures that even if one agent is compromised, it cannot damage another.</p> <ul> <li><strong>Without Trust Boundaries</strong>: All agents run in the same process or network space. A prompt injection attack on the <code class="language-plaintext highlighter-rouge">TriageAgent</code> (which should be low-privilege) allows it to discover and call internal functions of the <code class="language-plaintext highlighter-rouge">BillingAgent</code>, potentially accessing sensitive data or issuing an unauthorized refund.</li> <li><strong>With Trust Boundaries</strong>: Each agent runs in its own “sandbox” (e.g., a separate container, process, or serverless function). The <code class="language-plaintext highlighter-rouge">TriageAgent</code> has no direct access to the <code class="language-plaintext highlighter-rouge">BillingAgent</code>. It can only communicate by publishing a <code class="language-plaintext highlighter-rouge">Domain Event</code> (see Pattern 6). The <code class="language-plaintext highlighter-rouge">BillingAgent</code> subscribes to this event, isolating the two agents completely.</li> </ul> <p>Boundaries in DDD map directly to trust boundaries. Each agent should run in a sandbox with strict access policies.</p> <hr/> <table> <thead> <tr> <th style="text-align: left">Risk Type</th> <th style="text-align: left">Mitigation Pattern</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Cross-Agent Prompt Injection</td> <td style="text-align: left">ACL + Guardrail Sidecar</td> </tr> <tr> <td style="text-align: left">Privilege Escalation</td> <td style="text-align: left">Zero-Trust Bounded Context</td> </tr> <tr> <td style="text-align: left">Non-Converging Loops</td> <td style="text-align: left">Circuit Breaker + Checkpoint Recovery</td> </tr> </tbody> </table> <hr/> <p>In internal benchmarks, sandboxed execution prevented <strong>82.4%</strong> of attack vectors seen in unsandboxed multi-agent tests.</p> <h2 id="4-ubiquitous-language--human--model-alignment">4. Ubiquitous Language → Human + Model Alignment</h2> <p>With the agent’s container secured, the next step is to standardize the content passing through it. A <strong>Ubiquitous Language (UL)</strong> is the shared vocabulary connecting developers, domain experts, and models. In practice, this means a term like “TicketPriority” is used consistently by managers, in the code (<code class="language-plaintext highlighter-rouge">class TicketPriority</code>), and in the LLM’s system prompt.</p> <p>This removes ambiguity, which is a primary cause of model hallucinations.</p> <ul> <li><strong>Without UL (Ambiguous)</strong>: A prompt says, “Find problems in this support ticket.” The LLM must guess what a “problem” is—a typo? An angry customer? A technical bug? This leads to hallucinations.</li> <li><strong>With UL (Precise)</strong>: The prompt says, “Classify this <code class="language-plaintext highlighter-rouge">SupportTicket</code> with <code class="language-plaintext highlighter-rouge">Priority: (P0-P4)</code> and <code class="language-plaintext highlighter-rouge">Category: (Billing|Technical|Account)</code>.” The LLM is given no room to guess; it must execute a specific task using defined terms.</li> </ul> <p><strong>Example</strong>: Fragment of system prompt</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>System Role: "Support Triage Agent"
Vocabulary:
  - Priority: enum P0, P1, P2, P3, P4
  - Category: enum Billing, Technical, Account
  - Status: enum Open, InProgress, Resolved
</code></pre></div></div> <p>When UL is codified, hallucination rate typically falls by <strong>20–35%</strong>.</p> <hr/> <table> <thead> <tr> <th style="text-align: left">Prompt Type</th> <th style="text-align: left">Example</th> <th style="text-align: left">Accuracy (LLM-as-Judge)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Generic</td> <td style="text-align: left">“Summarize this customer email.”</td> <td style="text-align: left"><strong>71%</strong></td> </tr> <tr> <td style="text-align: left">Domain UL</td> <td style="text-align: left">“Extract <code class="language-plaintext highlighter-rouge">Priority</code> and <code class="language-plaintext highlighter-rouge">Category</code> from this <code class="language-plaintext highlighter-rouge">SupportTicket</code>.”</td> <td style="text-align: left"><strong>89%</strong></td> </tr> </tbody> </table> <hr/> <p>Integrating UL into prompt templates also simplifies evaluation because outputs map to structured fields already used in tests.</p> <h2 id="5-anti-corruption-layer--defensive-integration">5. Anti-Corruption Layer → Defensive Integration</h2> <p>The Ubiquitous Language defines the clean data; the <strong>Anti-Corruption Layer (ACL)</strong> is the “border control” that enforces it. It’s a translator that protects the clean domain model from “dirty” external data, whether from legacy APIs or the LLM itself.</p> <ul> <li><strong>Without ACL</strong>: An external CRM API returns <code class="language-plaintext highlighter-rouge">{"user_id": null, "name": "Guest"}</code>. This <code class="language-plaintext highlighter-rouge">null</code> value flows directly into the agent, which then fails with an error or hallucinates a response about “user null”.</li> <li><strong>With ACL</strong>: The ACL intercepts the API response. It validates the data, converts <code class="language-plaintext highlighter-rouge">null</code> to a safe, default value (like <code class="language-plaintext highlighter-rouge">GUEST_USER_ID</code>), or raises a specific <code class="language-plaintext highlighter-rouge">UserNotFound</code> exception before the agent is called. This protects the agent from “dirty” data.</li> </ul> <p>This pattern is also critical for handling LLM outputs, validating them before they are used by the system.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Example: ACL for external API data
</span><span class="k">class</span> <span class="nc">CrmACL</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">crm_to_domain</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">payload</span><span class="p">):</span>
        <span class="n">raw</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">payload</span><span class="p">)</span>
        <span class="n">user_id</span> <span class="o">=</span> <span class="n">raw</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">user_id</span><span class="sh">"</span><span class="p">)</span> <span class="ow">or</span> <span class="sh">"</span><span class="s">guest</span><span class="sh">"</span> <span class="c1"># Default value
</span>        <span class="k">return</span> <span class="nc">UserContext</span><span class="p">(</span>
            <span class="nb">id</span><span class="o">=</span><span class="n">user_id</span><span class="p">,</span>
            <span class="n">name</span><span class="o">=</span><span class="n">raw</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">),</span>
            <span class="n">level</span><span class="o">=</span><span class="n">raw</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">subscription_level</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">free</span><span class="sh">"</span><span class="p">)</span> <span class="c1"># Handle missing fields
</span>        <span class="p">)</span>

<span class="c1"># Example: ACL for LLM output
</span><span class="k">def</span> <span class="nf">parse_llm_output</span><span class="p">(</span><span class="n">text</span><span class="p">):</span>
    <span class="n">data</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="nf">extract_json</span><span class="p">(</span><span class="n">text</span><span class="p">))</span> <span class="c1"># Strips markdown, finds JSON
</span>    <span class="k">try</span><span class="p">:</span>
        <span class="k">return</span> <span class="nc">ClassifiedTicket</span><span class="p">(</span><span class="o">**</span><span class="n">data</span><span class="p">)</span> <span class="c1"># Validates against Pydantic/types
</span>    <span class="k">except</span> <span class="n">ValidationError</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="n">logger</span><span class="p">.</span><span class="nf">warning</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">ACL validation failed: </span><span class="si">{</span><span class="n">e</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">ClassifiedTicket</span><span class="p">.</span><span class="nf">fallback</span><span class="p">()</span>
</code></pre></div></div> <p><strong>Trade-off</strong>: This translation adds ≈35–120ms latency, but it prevents semantic drift and cascade failures. In production systems, ACLs can reduce data integration errors by over <strong>90%</strong>.</p> <h2 id="6-domain-events--event-driven-architecture--observability-and-asynchronicity">6. Domain Events &amp; Event-Driven Architecture → Observability and Asynchronicity</h2> <p>After the ACL validates a request, <strong>Domain Events</strong> allow the system to process it asynchronously and safely. An event (e.g., <code class="language-plaintext highlighter-rouge">TicketClassified</code>) is an immutable record of something that happened. This enables an Event-Driven Architecture (EDA).</p> <ul> <li><strong>Without Domain Events (Sequential)</strong>: <code class="language-plaintext highlighter-rouge">Request → [Agent A: Classify] → [Agent B: Update CRM] → [Agent C: Send Email] → Response</code>. The user waits for A+B+C to finish. If the email (C) fails, the whole chain fails.</li> <li><strong>With Domain Events (EDA)</strong>: <code class="language-plaintext highlighter-rouge">Request → [Agent A: Classify] → Response</code>. Agent A immediately publishes a <code class="language-plaintext highlighter-rouge">TicketClassified</code> event. Agents B and C subscribe to this event and run in the background.</li> </ul> <p>This asynchronous approach is why the metrics improve so drastically. The perceived latency for the user drops (they only wait for Agent A), throughput increases (the system isn’t locked waiting), and fault isolation is high (if the email agent fails, it doesn’t affect the user or Agent A).</p> <p><strong>Example</strong>: Event structure</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"event"</span><span class="p">:</span><span class="w"> </span><span class="s2">"TicketClassified"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agent"</span><span class="p">:</span><span class="w"> </span><span class="s2">"TriageAgent"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"timestamp"</span><span class="p">:</span><span class="w"> </span><span class="s2">"2025-10-29T12:45:11Z"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"payload"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">"ticket_id"</span><span class="p">:</span><span class="w"> </span><span class="s2">"T-123"</span><span class="p">,</span><span class="w"> </span><span class="nl">"category"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Billing"</span><span class="w"> </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <hr/> <table> <thead> <tr> <th style="text-align: left">Metric</th> <th style="text-align: left">Sequential Pipeline</th> <th style="text-align: left">EDA (DDD Events)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Avg Latency per Task (ms)</td> <td style="text-align: left">910</td> <td style="text-align: left">540</td> </tr> <tr> <td style="text-align: left">Throughput (req/s)</td> <td style="text-align: left">1.0</td> <td style="text-align: left">1.8</td> </tr> <tr> <td style="text-align: left">Fault Isolation Score</td> <td style="text-align: left">0.62</td> <td style="text-align: left">0.89</td> </tr> </tbody> </table> <hr/> <p>Having established the core patterns, let’s examine how they work together in a production system.</p> <h1 id="case-study-multi-agent-customer-service-system">Case Study: Multi-Agent Customer Service System</h1> <h2 id="architecture-overview">Architecture Overview</h2> <p>This system uses three <strong>Bounded Contexts</strong> to form a resilient pipeline:</p> <ol> <li><strong>Triage BC</strong>: A fast, cheap agent that classifies incoming tickets.</li> <li><strong>Billing BC</strong>: A specialized agent that handles payment, refund, and subscription issues.</li> <li><strong>Technical Support BC</strong>: A RAG-heavy agent that helps with troubleshooting.</li> </ol> <p>The data flow is decoupled using <strong>Domain Events</strong>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[User Request] → [TriageAgent] → (Publishes "TicketClassified" Event)
                                        |
                                        +--&gt; [BillingAgent] (Subscribes)
                                        |
                                        +--&gt; [TechSupportAgent] (Subscribes)
</code></pre></div></div> <h2 id="context-map-and-integration">Context Map and Integration</h2> <p>The <code class="language-plaintext highlighter-rouge">TriageAgent</code> (BC 1) doesn’t solve the ticket. It uses the <strong>Ubiquitous Language</strong> (<code class="language-plaintext highlighter-rouge">Category</code>, <code class="language-plaintext highlighter-rouge">Priority</code>) to classify it and then publishes a <code class="language-plaintext highlighter-rouge">TicketClassified</code> <strong>Domain Event</strong>.</p> <p>The <code class="language-plaintext highlighter-rouge">BillingAgent</code> (BC 2) and <code class="language-plaintext highlighter-rouge">TechSupportAgent</code> (BC 3) subscribe to this event, protected by <strong>Trust Boundaries</strong>. If <code class="language-plaintext highlighter-rouge">Category == "Billing"</code>, the <code class="language-plaintext highlighter-rouge">BillingAgent</code> activates. It uses an <strong>ACL</strong> to fetch the user’s payment history from an external CRM, then uses <strong>Context Engineering</strong> (RAG filtered by <code class="language-plaintext highlighter-rouge">user_id</code>) to find relevant past invoices and resolve the issue.</p> <hr/> <table> <thead> <tr> <th style="text-align: left">Component</th> <th style="text-align: left">Technology</th> <th style="text-align: left">Latency (ms)</th> <th style="text-align: left">Notes</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Triage Agent</td> <td style="text-align: left">Fast/Cheap Model</td> <td style="text-align: left">350</td> <td style="text-align: left">Fast classification (BC 1)</td> </tr> <tr> <td style="text-align: left">Billing Agent</td> <td style="text-align: left">Powerful Model</td> <td style="text-align: left">1200</td> <td style="text-align: left">Handles refunds/invoices (BC 2)</td> </tr> <tr> <td style="text-align: left">Tech Support Agent</td> <td style="text-align: left">Powerful Model + RAG</td> <td style="text-align: left">1800</td> <td style="text-align: left">Troubleshooting KBase (BC 3)</td> </tr> <tr> <td style="text-align: left">CRM ACL</td> <td style="text-align: left">FastAPI + Pydantic</td> <td style="text-align: left">80</td> <td style="text-align: left">Fetches/sanitizes user data</td> </tr> </tbody> </table> <hr/> <p>This architecture led to measurable production improvements by preventing context pollution.</p> <hr/> <table> <thead> <tr> <th style="text-align: left">Metric</th> <th style="text-align: left">Before (Monolithic Agent)</th> <th style="text-align: left">After (DDD Multi-Agent)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Avg. Resolution Time</td> <td style="text-align: left">240s</td> <td style="text-align: left">90s</td> </tr> <tr> <td style="text-align: left">Escalation to Human</td> <td style="text-align: left"><strong>45%</strong></td> <td style="text-align: left"><strong>22%</strong></td> </tr> <tr> <td style="text-align: left">Avg. LLM Cost / Ticket</td> <td style="text-align: left">~$0.08</td> <td style="text-align: left">~$0.03</td> </tr> </tbody> </table> <hr/> <h1 id="when-not-to-use-domain-driven-design-for-ai">When Not to Use Domain-Driven Design for AI</h1> <p>DDD introduces overhead — vocabulary definition, context mapping, event infrastructure.</p> <p>Avoid it when:</p> <ul> <li>The system is a simple RAG chatbot or prototype.</li> <li>You don’t yet know the final domain boundaries.</li> <li>Latency is critical (<code class="language-plaintext highlighter-rouge">&lt;200ms</code>) and each extra layer adds cost.</li> </ul> <p>In practice, teams find that DDD pays off only once the system exceeds ~5 agents or 3+ distinct data sources.</p> <h1 id="conclusion-and-next-steps">Conclusion and Next Steps</h1> <p>Domain-Driven Design gives AI engineers a framework to structure LLM systems that are otherwise chaotic and fragile. Its patterns — Bounded Contexts, Context Engineering, ACLs, and Domain Events — translate seamlessly into multi-agent and RAG architectures.</p> <p>Instead of a simple summary, here is a practical framework for implementation:</p> <ol> <li><strong>Start small</strong>. Define a single Bounded Context for one agent. Focus entirely on its <strong>Ubiquitous Language</strong> — codify every entity (<code class="language-plaintext highlighter-rouge">User</code>, <code class="language-plaintext highlighter-rouge">Ticket</code>, <code class="language-plaintext highlighter-rouge">Policy</code>) in its prompt and Pydantic models.</li> <li><strong>Build defenses</strong>. Wrap all external API calls (to databases, CRMs, or other services) in an <strong>Anti-Corruption Layer (ACL)</strong>. Do the same for the LLM’s final output to guarantee clean, structured data.</li> <li><strong>Decouple to scale</strong>. Once a second agent is needed, do not make them call each other directly. Use <strong>Domain Events</strong> to communicate, ensuring the system remains asynchronous, fault-tolerant, and respects its <strong>Trust Boundaries</strong>.</li> </ol> <hr/> <p align="center"> <i>When done right, DDD turns AI systems from experimental pipelines into resilient, auditable, and scalable software architectures fit for production.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Exploring how Domain-Driven Design principles (Bounded Contexts, Anti-Corruption Layer, Ubiquitous Language, Domain Events) enable modularity, safety, and traceability in production AI and LLM systems.]]></summary></entry><entry><title type="html">Semantic Prompt Caching: When LLM-Judge Beats Exact Match</title><link href="https://avchauzov.github.io/blog/2025/llm-judge-semantic-caching/" rel="alternate" type="text/html" title="Semantic Prompt Caching: When LLM-Judge Beats Exact Match"/><published>2025-10-30T00:00:00+00:00</published><updated>2025-10-30T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/llm-judge-semantic-caching</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/llm-judge-semantic-caching/"><![CDATA[<p>Prompt caching cuts latency by <strong>85%</strong>—but breaks completely on paraphrases. Change “Show sales data” to “Display sales data” and you get a cache miss, forcing full regeneration at 10x the cost.</p> <p>The reason is simple: KV-Cache works at token level. The system reuses precomputed Key-Value tensors only when input sequences match exactly. No semantic understanding at this layer.</p> <h3 id="the-llm-judge-approach">The LLM-Judge Approach</h3> <p>Instead of exact matching, use a small LLM to validate semantic equivalence. The judge determines if a cached query-response pair works for the new query—checking intent, not tokens.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">semantic_cache_lookup</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">vector_store</span><span class="p">,</span> <span class="n">judge_llm</span><span class="p">):</span>
    <span class="c1"># Fast embedding filter (FAISS, &lt;50ms)
</span>    <span class="n">candidates</span> <span class="o">=</span> <span class="n">vector_store</span><span class="p">.</span><span class="nf">similarity_search</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">threshold</span><span class="o">=</span><span class="mf">0.85</span><span class="p">)</span>

    <span class="k">if</span> <span class="ow">not</span> <span class="n">candidates</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>  <span class="c1"># No semantic candidates
</span>
    <span class="c1"># LLM-Judge validation (~100 input tokens, ~50 output tokens)
</span>    <span class="k">for</span> <span class="n">cached_query</span><span class="p">,</span> <span class="n">cached_response</span> <span class="ow">in</span> <span class="n">candidates</span><span class="p">:</span>
        <span class="n">prompt</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"""</span><span class="se">\
</span><span class="s">            Query 1: </span><span class="si">{</span><span class="n">cached_query</span><span class="si">}</span><span class="s">
            Query 2: </span><span class="si">{</span><span class="n">query</span><span class="si">}</span><span class="s">
            Cached Response: </span><span class="si">{</span><span class="n">cached_response</span><span class="si">}</span><span class="s">

            Is the cached response valid for Query 2? Answer YES or NO.
        </span><span class="sh">"""</span>

        <span class="n">judgment</span> <span class="o">=</span> <span class="n">judge_llm</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">max_tokens</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">judgment</span><span class="p">.</span><span class="nf">strip</span><span class="p">().</span><span class="nf">upper</span><span class="p">()</span> <span class="o">==</span> <span class="sh">"</span><span class="s">YES</span><span class="sh">"</span><span class="p">:</span>
            <span class="k">return</span> <span class="n">cached_response</span>  <span class="c1"># Cache hit
</span>
    <span class="k">return</span> <span class="bp">None</span>  <span class="c1"># Semantic validation failed
</span></code></pre></div></div> <p>Fast embedding filter + deeper judge validation. Two stages, different purposes.</p> <h3 id="the-latency-problem">The Latency Problem</h3> <p><strong>Critical constraint: TTFT budget</strong>. TTFT (Time To First Token) is what users actually feel—how long until they see the first word. The judge adds a full inference cycle. For a 7B model processing ~150 tokens, expect <strong>150–250ms overhead</strong>.</p> <p>Do the math: if your target TTFT is 300ms and exact-match caching delivers 240ms, adding a 200ms judge makes things worse.</p> <p><strong>Graceful failure is mandatory</strong>. Judge is an optimization, not a dependency. Set aggressive timeout (200–350ms). Any failure (timeout, API error, parse error) must default to cache miss and proceed to generation. Never block user requests waiting for judge.</p> <h3 id="economics-when-judge-calls-pay-off">Economics: When Judge Calls Pay Off</h3> <p><strong>Long outputs make the economics work</strong>. If your typical response is 2,000 tokens at $6/MTok ($0.012 per response) and judge costs 150 tokens at $1/MTok ($0.00015), the judge is <strong>80x cheaper</strong> than regeneration. Cache hit rates above 15% make this profitable.</p> <p><strong>Paraphrase-heavy workloads see immediate wins</strong>. I’ve seen this work best in customer support systems where users ask the same thing 20 different ways (“reset password” vs “can’t log in” vs “forgot credentials”). Technical documentation queries with varied terminology but identical intent.</p> <p>High semantic match rate (40–60% from embeddings) means the judge converts many near-misses into hits.</p> <h3 id="skip-it-when">Skip It When</h3> <p><strong>Short outputs (&lt;500 tokens)</strong>: judge overhead approaches regeneration cost.</p> <p><strong>Strict latency requirements (&lt;200ms)</strong>: no room for judge inference. Use embedding-only caching or accept exact-match limits.</p> <p><strong>Context-dependent queries create false equivalence</strong>. The judge must validate more than query text. “What’s the weather?” needs location + timestamp metadata. “Recommend products for me” from different users needs user ID validation. Context mismatch = cache miss.</p> <h3 id="implementation-notes">Implementation Notes</h3> <p><strong>Use White-Box confidence scores when available</strong>. This avoids separate judge calls but requires API support for token probabilities (<code class="language-plaintext highlighter-rouge">logprobs</code> from OpenAI). Best used as <strong>write-time filter</strong> to prevent caching low-confidence responses.</p> <p>Logic:</p> <ol> <li>Generate response with <code class="language-plaintext highlighter-rouge">return_logprobs=True</code></li> <li>Calculate confidence (geometric mean of token probabilities)</li> <li>Only cache if confidence &gt; threshold (e.g., 0.90)</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">response</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">return_logprobs</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">confidence</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">token_logprobs</span><span class="p">))</span>

<span class="k">if</span> <span class="n">confidence</span> <span class="o">&gt;</span> <span class="mf">0.90</span><span class="p">:</span>
    <span class="n">vector_store</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">response</span><span class="p">.</span><span class="n">text</span><span class="p">)</span>  <span class="c1"># Cache only high-confidence
</span>
<span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">text</span>
</code></pre></div></div> <p>This adds <strong>zero latency to retrieval</strong>—validation happens at write time, not read time.</p> <p><strong>Multi-turn conversations</strong>: include conversation state in equivalence check. Same query in different dialogue contexts needs different responses.</p> <p><strong>Monitor false positive rates</strong>. Wrong cached responses hurt users. Track through downstream metrics (feedback, task success) and adjust thresholds.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Standard prompt caching requires exact prefix match. LLM-Judge validates semantic equivalence, rescuing cache hits on paraphrases while adding controllable latency overhead.]]></summary></entry><entry><title type="html">The Reranking Trap: When Cross-Encoders Make Things Worse</title><link href="https://avchauzov.github.io/blog/2025/reranking-trap/" rel="alternate" type="text/html" title="The Reranking Trap: When Cross-Encoders Make Things Worse"/><published>2025-10-27T00:00:00+00:00</published><updated>2025-10-27T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/reranking-trap</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/reranking-trap/"><![CDATA[<p>Reranking has become a standard recommendation for improving RAG system quality. The narrative is straightforward: initial retrieval with <strong>bi-encoders</strong> casts a wide net, then <strong>cross-encoders</strong> or <strong>LLM rerankers</strong> refine the results through deeper semantic analysis. This two-stage architecture appears in virtually every production RAG guide.</p> <p>Production systems show different results. Adding neural rerankers introduces latency penalties reaching <strong>200%</strong>, systematically suppresses result diversity, and often fails to improve quality for common query patterns. For high-throughput applications with strict latency requirements, this architectural choice becomes a bottleneck rather than an optimization.</p> <h3 id="how-rerankers-work">How Rerankers Work</h3> <p><strong>Cross-encoders</strong> process query-document pairs as a single concatenated sequence, applying full self-attention across both inputs. This provides high precision but requires <code class="language-plaintext highlighter-rouge">N</code> forward passes for <code class="language-plaintext highlighter-rouge">N</code> candidates. Each pass encodes the complete pair.</p> <p><strong>LLM rerankers</strong> fall into two categories:</p> <ul> <li><em>Pointwise</em> approaches evaluate each pair independently, often generating reasoning traces before scoring.</li> <li><em>Listwise</em> methods process multiple documents in a single forward pass, significantly reducing inference overhead compared to pointwise alternatives.</li> </ul> <p><strong>Late interaction models</strong> (<strong>ColBERT</strong>) represent a middle ground. They compute token-level embeddings for queries and documents separately, then perform <strong>MaxSim</strong> matching at retrieval time. This preserves some cross-encoder precision while maintaining better scalability.</p> <h3 id="the-three-problems">The Three Problems</h3> <p><strong>Latency kills throughput</strong>. Bi-encoders work fast because they precompute document embeddings offline, enabling approximate nearest neighbor (ANN) search in milliseconds. <strong>Reranking cannot leverage precomputation</strong> — it must evaluate the interaction between a specific query and document.</p> <p>Pointwise LLM rerankers demonstrate <strong>2–2.7x higher latency</strong> than listwise alternatives due to sequential autoregressive decoding. In-context ranking with full fine-tuning exhibits exponential latency growth. At <code class="language-plaintext highlighter-rouge">N=500</code> documents, query latency reaches <strong>~1.15s</strong> on a 7B model. Even efficient alternatives like ColBERT still increase query latency compared to pure vector search.</p> <p>This computational load restricts practical application to small candidate sets — typically <code class="language-plaintext highlighter-rouge">k≤50</code>. <strong>The high per-query computational cost</strong> makes them unsuitable for high-throughput systems or initial retrieval stages.</p> <p><strong>Diversity collapse happens by design</strong>. Rerankers optimized exclusively for relevance systematically reduce result diversity in the top-<code class="language-plaintext highlighter-rouge">k</code> set.</p> <p>Cross-encoders and pointwise LLM scorers assign scalar relevance scores to individual query-document pairs. This makes systems sensitive to redundancy in candidate lists. Documents containing diverse but non-optimal facts get filtered out, even when they provide valuable alternative perspectives. If the top candidates include five documents expressing the same idea with slight variations, the reranker places them all at the top. A document with a different viewpoint gets suppressed.</p> <p>Clustering-based grouping strategies before reranking make things worse. K-Means clustering on BERT embeddings rewards redundancy rather than utility — semantically similar documents cluster together regardless of their complementary information value. For queries requiring comprehensive coverage, aggressive reranking that filters conflicting documents prevents LLM generators from accessing necessary information.</p> <p>Maximal Marginal Relevance (MMR) was designed specifically to address this problem by explicitly trading relevance for diversity — but the tradeoff remains fundamental.</p> <p><strong>Reranking often provides no benefit</strong>. I’ve noticed in deployed RAG systems that reranking frequently fails to improve metrics or actively degrades them.</p> <p>With strong bi-encoders, correct documents for basic fact-retrieval queries are often already first. Additional cross-encoder passes become redundant computational waste. <strong>Garbage in, garbage out</strong>. If the bi-encoder failed to include relevant information in the top-<code class="language-plaintext highlighter-rouge">k</code> candidates, the reranker has nothing meaningful to rank. It cannot <strong>find</strong> missed documents, only <strong>re-sort</strong> what it receives.</p> <p>High-precision requirements reveal additional limitations. Academic search systems implementing citation-graph traversal expand candidate sets through reference networks, introducing substantial noise. LLM rerankers struggle to restore precision after this expansion — the noise-to-signal ratio becomes too high for effective filtering.</p> <h3 id="when-reranking-actually-works">When Reranking Actually Works</h3> <p>Reranking proves valuable in specific scenarios where the computational cost justifies precision gains.</p> <p><strong>Hard candidates</strong> requiring nuanced distinction benefit from cross-encoders. When initial retrievers return many semantically similar but redundant candidates, deep pairwise interaction (cross-attention) distinguishes subtle relevance nuances that bi-encoders miss.</p> <p><strong>Complex instruction-following</strong> queries (e.g., BRIGHT benchmark tasks requiring specific algorithmic approaches or theorem applications) need advanced rerankers. These models generate reasoning traces that capture logical relationships unavailable through simple semantic similarity.</p> <p><strong>High-stakes domains</strong> — legal search, medical diagnosis — where small precision gains justify significant latency increases. For these high-risk research tasks, reranking remains defensible. For high-throughput applications requiring sub-100ms responses, the tradeoff becomes unacceptable.</p> <h3 id="better-approaches">Better Approaches</h3> <p>Before adding an expensive reranking step, consider more effective alternatives.</p> <p><strong>Embedding model adaptation</strong> often provides the best ROI. Fine-tuning bi-encoder models on domain-specific datasets can increase Context Recall by <strong>+95%</strong>, compared to an 8% recall improvement from adding LLM rerankers at the cost of increased latency. For scenarios requiring substantial quality gains without latency penalties, this approach delivers better system-level performance.</p> <p><strong>Late interaction architectures</strong> like ColBERT offer a viable compromise when response time constraints are strict. Token-level matching during retrieval provides precision approaching cross-encoders while maintaining scalability to large corpora.</p> <p><strong>Context compression</strong> through summarization-based approaches scales linearly <code class="language-plaintext highlighter-rouge">O(N)</code> when the challenge involves fitting many relevant but redundant documents into limited context windows. This avoids expensive candidate list analysis while addressing the core constraint.</p> <p><strong>Maximal Marginal Relevance (MMR)</strong> provides an algorithmic alternative that explicitly trades relevance for diversity through a balancing parameter <code class="language-plaintext highlighter-rouge">lambda</code>. The algorithm incorporates query similarity <code class="language-plaintext highlighter-rouge">(sim_1)</code> against dissimilarity with already-selected documents <code class="language-plaintext highlighter-rouge">(sim_2)</code>. <strong>MMR is always a tradeoff</strong> — increasing diversity deliberately reduces overall relevance, but avoids neural reranking’s computational costs.</p> <p><strong>GraphRAG</strong> proves necessary for multi-hop or logical reasoning tasks requiring structured knowledge. Vector search followed by semantic reranking cannot effectively model hierarchical structures, causal chains, or logical dependencies critical for these scenarios. However, GraphRAG introduces excessive context for simple queries and should be avoided when tasks don’t require deep contextual hierarchy.</p> <p><strong>Decision framework</strong>: Start with embedding adaptation. If latency permits and you have hard candidates or complex instructions, add reranking. For diversity-focused applications, consider MMR. For multi-hop reasoning, evaluate GraphRAG.</p> <p><strong>Implementation priority</strong>: In advanced RAG architectures, reranking should follow baseline pipeline stabilization (quality chunking, strong embeddings). It serves as an early optimization step to establish solid Recall@k and Groundedness metrics before pursuing more complex enhancements.</p> <h3 id="conclusion">Conclusion</h3> <p>Neural reranking isn’t a universal improvement — it’s a precision-latency tradeoff. The significant latency penalty, systematic diversity suppression, and failure modes on common query patterns contradict its framing as a mandatory step.</p> <p>Effective deployment requires understanding when this expensive step provides measurable value. For most high-throughput systems, embedding adaptation or late-interaction models deliver better results. The decision to add reranking should be driven by empirical measurement against your latency budget, not architectural convention.</p> <hr/> <p align="center"> <i>If your bi-encoder already works well, don't add reranking. If you need precision gains and can afford the latency cost, measure first.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Cross-encoders and LLM rerankers promise better retrieval precision, but the 200% latency penalty, diversity collapse, and production failures reveal when this expensive step becomes counterproductive.]]></summary></entry></feed>