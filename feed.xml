<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://avchauzov.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://avchauzov.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-23T14:08:50+00:00</updated><id>https://avchauzov.github.io/feed.xml</id><title type="html">Andrey Chauzov</title><subtitle>Senior AI Architect with 7+ years building enterprise-scale AI solutions, specializing in NLP, MLOps, and intelligent infrastructure. Building the future of AI systems that scale. </subtitle><entry><title type="html">Prompt caching in production: where the savings disappear</title><link href="https://avchauzov.github.io/blog/2026/prompt-caching-in-production/" rel="alternate" type="text/html" title="Prompt caching in production: where the savings disappear"/><published>2026-07-21T00:00:00+00:00</published><updated>2026-07-21T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/prompt-caching-in-production</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/prompt-caching-in-production/"><![CDATA[<p>Prompt caching is one of the few inference optimizations that’s easy to enable: through a configuration flag such as <code class="language-plaintext highlighter-rouge">enable_prefix_caching=True</code> in vLLM, automatically on OpenAI, or with a top-level <code class="language-plaintext highlighter-rouge">cache_control</code> on Anthropic and optional explicit breakpoints for finer control. Anthropic and OpenAI publish up to <strong>90%</strong> cost reduction and <strong>80%</strong> time-to-first-token improvement as ceiling numbers. What that looks like in practice is closer to Databricks’ production batch-inference pipeline: a <strong>2.5x</strong> throughput increase and <strong>3x</strong> P50 latency reduction — achieved at a cache hit rate of only <strong>30%</strong>.</p> <p>Yet production deployments often capture only a fraction of those ceiling numbers. The limiting factor is usually not the caching mechanism itself, but everything around it.</p> <h2 id="what-caching-actually-does">What caching actually does</h2> <p>LLM inference runs in two phases. During <strong>prefill</strong>, the model processes the entire input sequence — every token in the prompt — and builds a key-value (KV) cache: attention key and value vectors computed per token/layer. This phase is compute-bound and scales with prompt length.</p> <p>During <strong>decode</strong>, the model generates output tokens one at a time. Each step loads model weights and the growing KV cache from GPU memory. This phase is memory-bandwidth-bound — the bottleneck is how fast tensors can move, not how fast arithmetic can run.</p> <p>Prefix caching exploits a structural redundancy: if two requests share an identical prefix — the same system prompt, the same document, the same tool schemas — the KV cache for that prefix can be computed once and reused for every subsequent request. Subsequent requests can skip most or all prefill computation for the cached portion, subject to block boundaries and implementation details. Only the unmatched suffix requires fresh prefill.</p> <p>The savings are real, and they follow directly from the mechanism: since prefill for cached tokens is replaced by a cache lookup, those tokens are typically heavily discounted relative to standard input pricing. On self-hosted deployments using vLLM or SGLang, this shows up even more directly — the GPU cycles for that prefill simply aren’t spent.</p> <h2 id="three-ways-prompt-caching-savings-disappear">Three ways prompt-caching savings disappear</h2> <h3 id="1-dynamic-content-in-the-wrong-position">1. Dynamic content in the wrong position</h3> <p>Prefix caching requires an <strong>exact match on the token sequence from the start of the prompt</strong>. Any variation ends the reusable prefix at that position; tokens after the divergence point require fresh prefill.</p> <p>The common mistake: injecting per-request content (timestamps, request IDs, volatile session metadata, newly retrieved documents) near the beginning of the prompt, before the stable system instructions. A system prompt template that starts with <code class="language-plaintext highlighter-rouge">"Current time: {timestamp}"</code> has an effective cache hit rate of zero regardless of how much stable content follows.</p> <p>The fix is mechanical: <strong>static content first, dynamic content last</strong>. Stable system instructions, tool schemas, few-shot examples, and shared reference context belong before request-specific content. User messages and any per-request variable content go at the end, after everything cacheable. A shared reference document reused across requests is cacheable; a per-request RAG result is not — order by reuse frequency, not by content type.</p> <h3 id="2-round-robin-load-balancing-across-replicas">2. Round-robin load balancing across replicas</h3> <p>Single-replica caching works exactly as documented. The failure mode appears at scale.</p> <p>A KV cache typically lives in GPU memory on a specific instance. Naive round-robin distributes requests uniformly across replicas, ignoring where each prefix is already cached. If only one of N replicas currently holds a prefix, the next request has only about a 1/N chance of reaching it. Requests routed elsewhere must repeat the prefill, potentially warming duplicate copies across the fleet.</p> <p>Frequently reused prefixes may eventually become cached on every replica, but long-tail prefixes often do not survive long enough to reach that state. Autoscaling, cache eviction, limited KV capacity, and uneven request arrival continually reset the warm-up process. The result is duplicated computation and fragmented cache capacity rather than a permanent 1/N hit-rate ceiling.</p> <p>Prefix-aware routing addresses this by tokenizing the incoming prompt, matching it against per-instance prefix state, and routing it to a worker with reusable overlap while accounting for current load. DigitalOcean reported up to a <strong>108%</strong> throughput improvement for the same hardware and workload when comparing cache-aware routing against round-robin.</p> <p>This is the problem most teams don’t encounter until they scale — and the reason production numbers often look worse than single-server benchmarks.</p> <h3 id="3-agentic-loops-make-cache-hits-progressively-more-expensive">3. Agentic loops make cache hits progressively more expensive</h3> <p>Agent workloads have a cost structure that differs from request-response patterns in a non-obvious way.</p> <p>In an agentic loop, each LLM call receives the full conversation history to date. If each turn adds roughly <code class="language-plaintext highlighter-rouge">d</code> tokens, the total volume of tokens processed across a <code class="language-plaintext highlighter-rouge">T</code>-turn session is <code class="language-plaintext highlighter-rouge">d + 2d + 3d + ... + Td ≈ O(T²)</code> — quadratic in the number of turns, even though any single call is only linear in its current length.</p> <p>Caching reduces repeated prefill computation, but it does not eliminate the provider’s accounting of cached-input tokens or the runtime cost of attending over a long context during decode. Under token-based API pricing, the cumulative cached-input volume still grows quadratically in this simplified model; it is merely charged at a lower per-token rate (sometimes as much as <strong>10x</strong> cheaper, depending on provider and model).</p> <p>As a result, cached-input charges can eventually dominate costs that grow roughly linearly with the number of turns, such as a fixed amount of newly appended input and output per turn. The crossover depends on turn size, output length, model pricing, and the cache-read discount.</p> <p>That crossover point isn’t stable, either — it assumes the discount holds. Whenever the required prefix is missing — because of expiration, eviction, prompt changes, or routing to an unwarmed replica — the same tokens that were getting a discount suddenly don’t: they’re billed as full-price uncached input, or even at a premium cache-write rate. These events increase total cost and make it less predictable than the steady-state calculation suggests.</p> <p>This is why aggressive context management — compaction, pruning, sub-agent isolation — isn’t just about staying under context limits. It directly targets the quadratic term: shrinking or bounding the history that gets re-read on every call is what keeps the growth closer to linear instead of quadratic.</p> <h2 id="what-cache-hit-rate-actually-means-in-production">What cache hit rate actually means in production</h2> <p>A reported <strong>30%</strong> cache hit ratio produced the <strong>2.5x</strong> throughput gain Databricks observed. That is not a contradiction: the impact of a hit ratio depends on which prefixes are reused, how long they are, and how much of the workload’s latency comes from prefill. The same numerical hit ratio can produce very different results across workloads.</p> <p>Reusing a <code class="language-plaintext highlighter-rouge">4,000</code>-token prefix eliminates roughly as much repeated prefill work as reusing one hundred <code class="language-plaintext highlighter-rouge">40</code>-token prefixes. The payoff therefore depends not only on the aggregate hit ratio, but on whether the workload contains long, stable, frequently repeated prefixes.</p> <p>Two workload shapes reliably produce that: <strong>batch inference over a shared corpus</strong> — many requests against the same document, system prompt, or reference context, where precomputing the shared prefix makes that repeated prefill work nearly independent of request volume — and <strong>multi-turn chat with stable system prompts</strong>, where instructions, tool schemas, and prior history can be reused across turns, while only content appended beyond the reusable prefix requires fresh prefill.</p> <p>Two shapes don’t: <strong>highly varied, short prompts</strong> have no stable prefix worth caching, and prefill for them is fast regardless. <strong>Workloads where prefill is already a small fraction of end-to-end latency</strong> may see limited benefit, especially if cache lookup, memory pressure, or affinity-aware routing adds comparable overhead.</p> <h2 id="the-metric-that-matters-before-you-optimize">The metric that matters before you optimize</h2> <p>Before tuning prompt structure or switching to prefix-aware routing, instrument cache reuse at the token level. vLLM exposes counters for cached and queried prefix tokens and an aggregate hit rate — but not a built-in breakdown by prompt segment. Attributing hits to the system prompt, tool schemas, or conversation history requires application-level tracing; tools like Langfuse can track the underlying token usage, but the segment mapping is on you.</p> <p>What to look for:</p> <ul> <li><strong>Low reuse despite a stable system prompt</strong> — likely prompt variation, unstable serialization, cache expiration, or eviction</li> <li><strong>High reuse but lower savings than expected</strong> — likely a long uncached suffix or decode-dominated latency</li> <li><strong>Reuse degrading under load</strong> — likely loss of cache locality or increased eviction pressure</li> </ul> <hr/> <p align="center"> <i>Prompt caching is infrastructure, not configuration. Enabling it is the easy part. The gains come from aligning prompt structure, routing strategy, cache topology, and session architecture around a single constraint: repeated tokens must reach a worker or cache tier capable of reusing an unchanged prefix.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[KV cache reuse can cut inference costs and latency substantially on paper. Most production deployments capture a fraction of that gap, and the reason is structural rather than a missing configuration flag.]]></summary></entry><entry><title type="html">Why agent memory degrades in production</title><link href="https://avchauzov.github.io/blog/2026/agent-memory-write-path/" rel="alternate" type="text/html" title="Why agent memory degrades in production"/><published>2026-05-28T00:00:00+00:00</published><updated>2026-05-28T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/agent-memory-write-path</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/agent-memory-write-path/"><![CDATA[<p>Agent memory frameworks expose a clean <strong>read path</strong>: retrieve relevant context, inject it into the prompt. The <strong>write path</strong> — what gets stored, when, and how existing facts are updated or removed — receives significantly less attention in both documentation and implementation.</p> <h2 id="the-benchmark-gap">The benchmark gap</h2> <p>RankSquire benchmarked Mem0 v0.8.2 on LongMemEval and measured <strong>93.4%</strong> accuracy. The same system, running over 50,000 real sessions across 30 days, dropped to <strong>49.0%</strong> effective accuracy. The primary cause: flat memory structures accumulating a <strong>38%</strong> staleness rate — contradicted facts stored as separate <code class="language-plaintext highlighter-rouge">ADD</code> events rather than resolved at write time.</p> <p>Based on this audit, they approximated the relationship between real-world accuracy and two observable variables — staleness rate and memory store size:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Production_Accuracy ≈ Benchmark − (0.22 × Staleness_Rate) − (0.15 × log₁₀(Entities))
</code></pre></div></div> <p>This is not a retrieval problem. Retrieval is faithfully surfacing stale facts that should have been updated when new information arrived. The write path was never designed.</p> <h2 id="how-the-write-path-fails">How the write path fails</h2> <p>Four problems compound independently. Solving only one leaves the others intact.</p> <p><strong>Single-pass extraction</strong> — The default approach — one LLM call that simultaneously detects, structures, and extracts — has a joint accuracy problem. If you extract 20 fields at 97% per-field accuracy, the probability the entire record is correct is approximately <strong>54%</strong> (xmemory). Memory acts as a system of record. A hallucinated value during the initial write silently corrupts every future read from that entry.</p> <p><strong>No retrieval before write</strong> — Committing a new fact without first checking what is already stored produces semantic duplicates and unresolved contradictions. Governed Memory uses hard cosine similarity thresholds: above 0.92, the write is skipped as a duplicate; an offline job uses 0.95 to merge near-duplicates. Without explicit thresholds, the decision falls to LLM judgment under context pressure — unreliable at scale.</p> <p><strong>Append-only semantics</strong> — Mem0’s v2 architecture stored both “I use React” and “I use Vue” as independent <code class="language-plaintext highlighter-rouge">ADD</code> events when the user’s stack changed, relying on search ranking to surface current truth. This works until the store is large enough that ranking becomes unreliable. The minimum viable write decision requires four explicit operations: ADD (no relevant memory exists), UPDATE (new information supersedes old), DELETE (fact explicitly revoked), NOOP (already accurately represented).</p> <p><strong>No eviction policy</strong> — Without garbage collection, stores explode. RankSquire audited a production system with 3.2 million memory entries and found <strong>97.8%</strong> low-utility. Storage costs had spiked <strong>300%</strong>. Scaling from 50k to 200k entities without graph resolution caused a <strong>14x</strong> increase in retrieval latency — from <strong>52ms</strong> to <strong>728ms</strong> at p95. The bottleneck was not the retrieval algorithm.</p> <h2 id="when-to-write">When to write</h2> <p>Two anti-patterns dominate.</p> <p><strong>Batch compaction</strong> — buffering conversations and flushing memory when the context window fills — is lossy compression disguised as a memory strategy. OpenClaw documented the result after three months of production use: LLMs summarizing under context pressure produce unintended recency bias. Older sessions are permanently lost. The system silently truncates memory files at <strong>20,000</strong> characters.</p> <p><strong>Turn-level extraction for everything</strong> — running heavy LLM extraction after every exchange — wastes tokens on transient data. Most conversational content is not worth persisting.</p> <p>The pattern that holds up is <strong>two-timescale operation</strong>:</p> <p>SLM = Small Language Model, LTM = Long-Term Memory</p> <hr/> <table> <thead> <tr> <th>Timescale</th> <th>Operation</th> <th>Mechanism</th> </tr> </thead> <tbody> <tr> <td>Online (real-time)</td> <td>Lightweight acquisition</td> <td>Fast embeddings or SLM into mid-term buffer</td> </tr> <tr> <td>Offline (async)</td> <td>Consolidation into LTM</td> <td>Heavy LLM extraction, dedup, contradiction resolution</td> </tr> </tbody> </table> <hr/> <p>RecMem only triggers heavy extraction when sustained recurrence is observed — when an incoming interaction clusters semantically with enough past exchanges to prove the topic is durable. LightMem formalizes this as a dedicated SLM for online writes, with a heavier LLM running asynchronously for long-term consolidation. Mem0 made <code class="language-plaintext highlighter-rouge">async_mode=True</code> the production default for exactly this reason.</p> <h2 id="contradiction-resolution-at-write-time">Contradiction resolution at write time</h2> <p>Contradictions that represent genuine transitions — not errors — need a different treatment than simple UPDATE overwrites. LinkedIn’s Cognitive Memory Agent uses a temporal arbiter that generates a reconciliation summary rather than overwriting history: “User utilized React until November 2025 but has since transitioned their primary stack to Vue.” The agent can reason about the transition, not just the current state.</p> <p>For automated contradiction detection at scale, MemoryLake implements three-level conflict detection: logical conflicts (direct contradictions), implicit knowledge conflicts (requiring inference), and hallucination conflicts — with resolution rules that favor recency while preserving conflict history.</p> <h2 id="what-the-write-path-actually-costs">What the write path actually costs</h2> <p>At scale, memory operations account for <strong>60%</strong> of total agent system costs — not LLM inference. This is the number that surprises most teams when they first see production billing.</p> <p>The ProMem framing is correct: memory is write-once, read-many. Investing in rigorous write-path validation — staged extraction, retrieval-before-write operations, deduplication gates — increases initial token cost. But cheap extraction errors are paid forward across every future read from the corrupted entry.</p> <p>One concrete tradeoff: adding entity resolution to prevent graph node explosion adds <strong>80–120ms</strong> to write latency. For real-time pipelines requiring <strong>sub-100ms</strong> writes, this belongs in the async offline job, not the hot path.</p> <h2 id="what-this-requires-structurally">What this requires structurally</h2> <p>A write path that holds under production conditions:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Session ends
  → async extraction fires (focused prompt: preferences, decisions, constraints, corrections)
  → for each extracted fact:
      retrieve existing memories for this entity/topic
      LLM selects ADD / UPDATE / DELETE / NOOP
      write with timestamp + session provenance
  → scheduled: consolidation pass (episodic → semantic compression)
  → scheduled: eviction pass (drop facts un-accessed in N days)
</code></pre></div></div> <p>Three things that are easy to skip:</p> <ul> <li><strong>Provenance on every write</strong> — Trace which session introduced a bad fact. Audit trails are how corrective deletes get applied reliably</li> <li><strong>Contradiction detection at write time</strong> — Without it, conflicts surface at retrieval time and force the LLM to resolve them under context pressure</li> <li><strong>Observability on the write path</strong> — The read path is easy to instrument. The write path is invisible unless you make it visible. Log every extraction, every <code class="language-plaintext highlighter-rouge">create/read/update/delete</code> decision, every consolidation pass</li> </ul> <hr/> <p align="center"> <i>The 32-point accuracy gap between benchmark and production is not a model limitation. It is a write path that was never designed.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[What happens to agent memory stores after weeks of production use, and what the read path never exposes.]]></summary></entry><entry><title type="html">Context engineering as a production discipline</title><link href="https://avchauzov.github.io/blog/2026/context-engineering-production-patterns/" rel="alternate" type="text/html" title="Context engineering as a production discipline"/><published>2026-04-08T00:00:00+00:00</published><updated>2026-04-08T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/context-engineering-production-patterns</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/context-engineering-production-patterns/"><![CDATA[<p>Context window capacity has grown faster than our ability to use it well. As limits expanded from 4k to 128k to several million tokens, the default engineering response was straightforward: include more. In practice, this creates systems that are expensive to run, unpredictable under load, and increasingly difficult to debug as session length grows.</p> <p><strong>Context engineering</strong> is the discipline of treating context as a finite, governed resource. Not a buffer to fill, but a pipeline to assemble — per request, per session state, per agent step.</p> <h2 id="context-engineering-vs-prompt-engineering">Context engineering vs prompt engineering</h2> <p>Prompt engineering optimizes wording: role instructions, few-shot examples, chain-of-thought triggers. The unit of work is a single string.</p> <p>Context engineering operates at the system level. It requires orchestration loops, memory tiers, and data pipelines — not better phrasing.</p> <p>The shift is from static scripts to dynamic assembly.</p> <h2 id="how-context-fails-in-production">How context fails in production</h2> <p>Four failure modes appear consistently across production systems.</p> <p><strong>Context rot</strong> — as the window grows, precision drops. Models exhibit the “lost in the middle” effect: information placed at the center of a long context is systematically under-attended. Multi-hop reasoning degrades sharply past 16–32k tokens even when the answer is technically present.</p> <p><strong>Instruction fade-out</strong> — in long-running agent sessions (typically past <strong>15–30</strong> tool calls), system instructions placed at the beginning of the prompt lose influence. The model silently deviates from them — completing tasks prematurely, skipping error recovery, or changing output format.</p> <p><strong>Tool bloat</strong> — injecting large numbers of tool schemas upfront carries a significant token cost. <strong>90</strong> tools consume <strong>~50,000+</strong> tokens before a single reasoning step. When too many options are present, models spend disproportionate output budget on tool selection rather than task execution.</p> <p><strong>Summary drift</strong> — naive context compaction (summarizing history when the limit approaches) destroys exact operational details: file paths, variable names, identifiers. Agents that depend on these details after compaction fail silently, with no visible error.</p> <h2 id="patterns-that-address-this">Patterns that address this</h2> <h3 id="progressive-disclosure">Progressive disclosure</h3> <p>Load tool schemas in tiers, not upfront. At discovery, inject only names and short descriptions (~80 tokens per tool). Full schemas load only when the model explicitly selects a tool. This bounds token overhead regardless of total tool count.</p> <p><strong>Caveat</strong>: accuracy degrades past ~100 tools due to overlapping descriptions causing misactivation. Past that threshold, domain grouping or a meta-search tool becomes necessary.</p> <h3 id="sub-agent-context-isolation">Sub-agent context isolation</h3> <p>Agentic architectures delegate exploratory subtasks — codebase search, document retrieval, log analysis — to sub-agents running in isolated context windows. The sub-agent processes thousands of tokens of intermediate results; the main orchestrator receives only a distilled summary.</p> <p>This keeps the planner’s context window bounded and prevents tool output accumulation — one of the most common sources of context bloat in multi-step pipelines.</p> <h3 id="tiered-compaction-with-pressure-thresholds">Tiered compaction with pressure thresholds</h3> <p>Rather than binary compaction (summarize everything when the limit is near), apply graduated pressure:</p> <hr/> <table> <thead> <tr> <th>Threshold</th> <th>Action</th> </tr> </thead> <tbody> <tr> <td>~80% usage</td> <td>Observation masking — replace verbose tool outputs with file pointers</td> </tr> <tr> <td>~85% usage</td> <td>Fast pruning — drop low-signal turns from working memory</td> </tr> <tr> <td>~99% usage</td> <td>Full LLM compaction — summarize episodic history</td> </tr> </tbody> </table> <hr/> <p>The key architectural decision: recent turns bypass compaction entirely. <strong>Episodic memory</strong> (LLM-generated summary for strategic context) is kept separate from <strong>working memory</strong> (last N turns verbatim for operational detail).</p> <h3 id="event-driven-system-reminders">Event-driven system reminders</h3> <p>Rather than relying on system prompt instructions to persist across a long session, inject targeted reminders as <code class="language-plaintext highlighter-rouge">user</code> role messages immediately before the relevant LLM call. Single-purpose, maximum recency.</p> <p><strong>Caveat</strong>: strictly cap at three or fewer per session. Beyond that, the model treats them as background noise.</p> <h2 id="production-evidence">Production evidence</h2> <p>The following cases illustrate how the patterns above translate into concrete architectural decisions.</p> <h3 id="linkedin--tool-schema-management">LinkedIn — tool schema management</h3> <p><strong>Problem</strong>: Exposing thousands of internal tools via namespace-loaded schemas consumed prohibitive context on every request.</p> <p><strong>Approach</strong>: Replaced static schema injection with a meta-tool design. The agent issues keyword queries against an internal tool registry; only matched schemas are injected into the active context window.</p> <p><strong>Outcome</strong>: <strong>70%</strong> reduction in customer issue triage time; <strong>3x</strong> faster time-to-insight for internal data queries.</p> <h3 id="dropbox-dash--retrieval-api-consolidation">Dropbox Dash — retrieval API consolidation</h3> <p><strong>Problem</strong>: Connecting the model to dozens of app-specific retrieval APIs produced analysis paralysis — the model spent context budget on API selection rather than answering queries.</p> <p><strong>Approach</strong>: Replaced disparate APIs with a single universal search index backed by a knowledge graph. Complex query construction was isolated into a dedicated sub-agent, freeing the main planner’s context window for reasoning.</p> <h3 id="spotify--parallel-agent-isolation">Spotify — parallel agent isolation</h3> <p><strong>Problem</strong>: Sequential multi-step workflows accumulated context across planning, budget allocation, and media scheduling — unrelated state from early steps persisted into later ones.</p> <p><strong>Approach</strong>: Decomposed the pipeline into parallel agents (routing, goal resolution, budget, media planning), each operating in an isolated context window.</p> <p><strong>Outcome</strong>: Media plan creation time reduced from <strong>15–30 minutes</strong> to <strong>5–10 seconds</strong>.</p> <hr/> <p>The shared pattern across all three cases: decompose context ownership, never let a single window carry the full task state.</p> <h2 id="the-core-constraint">The core constraint</h2> <p>Every token in the context window competes for the model’s attention. Tool schemas, conversation history, retrieved documents, system instructions — all draw from the same finite budget.</p> <p>The engineering question is not “how large is the window?” but “what deserves to be in it at this step?”</p> <hr/> <p align="center"> <i>Treating context as a dynamic, tiered pipeline — assembled per request, pruned under pressure, isolated across agents — is the difference between a system that scales and one that degrades silently as usage grows.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Failure modes, architectural patterns, and evidence from real systems.]]></summary></entry><entry><title type="html">Why LLM evaluation metrics look stable but customers are unhappy</title><link href="https://avchauzov.github.io/blog/2026/evaluation-metrics-user-satisfaction-gap/" rel="alternate" type="text/html" title="Why LLM evaluation metrics look stable but customers are unhappy"/><published>2026-01-25T00:00:00+00:00</published><updated>2026-01-25T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/evaluation-metrics-user-satisfaction-gap</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/evaluation-metrics-user-satisfaction-gap/"><![CDATA[<p>LLM systems can pass their regular evaluation checks while degrading in ways users immediately notice. Aggregate accuracy and judge scores remain stable because they measure isolated outputs, not the effort required to complete a task.</p> <p>The gap is structural: classic metrics evaluate answers, while users experience interactions.</p> <h2 id="what-stable-metrics-hide">What stable metrics hide</h2> <p>Three mechanisms let traditional evaluation report green while production degrades: benchmark saturation, judge bias, and averaging effects.</p> <h3 id="benchmark-saturation">Benchmark saturation</h3> <p>When top models cluster at <strong>90–95%</strong>, the leaderboard measures who optimized harder for the test distribution, not who reasons better. Models memorize test sets during pretraining: for example, <strong>GPT-4 (GPT-4o)</strong> drops from <strong>88%</strong> to <strong>73%</strong> on contamination-free <strong>Massive Multitask Language Understanding</strong> benchmark variants — the gap reveals how much reported accuracy reflects memorization rather than reasoning.</p> <p>In production, this matters because agent workflows chain model calls. A model scoring 95% on isolated steps compounds errors across a 10-step chain: <code class="language-plaintext highlighter-rouge">0.95^10 ≈ 0.60</code>. The system passes every component test but fails the end-to-end task.</p> <h3 id="llm-as-judge-bias">LLM-as-Judge bias</h3> <p>Automated judges exhibit systematic biases: longer responses score higher regardless of accuracy, models favor outputs from their own family. The risk: if these judges are used in training loops, models may optimize for scoring preferences rather than user needs. High automated-eval scores stop predicting user satisfaction.</p> <h3 id="averaging-effects">Averaging effects</h3> <p>Aggregate metrics dilute signal: perplexity averages likelihood across tokens. Easily predicted non-critical tokens can dominate the score, while errors on answer-bearing tokens remain underweighted. Benchmarks average over query difficulty — easy cases hide collapse on the complex edge cases users actually encounter.</p> <p>High aggregate scores coexist with catastrophic failures on the subset of queries that define user trust.</p> <h2 id="what-production-systems-measure-instead">What production systems measure instead</h2> <p>Production evaluation replaces output scoring with three signal categories: behavioral friction, system-level drift, and agent trajectory completion.</p> <h3 id="behavioral-metrics--friction-signals">Behavioral metrics — friction signals</h3> <p>User actions often reveal dissatisfaction earlier than explicit ratings.</p> <ul> <li><strong>Edit rate</strong> — how often users modify generated outputs; high rates signal missed intent even when text is grammatically correct</li> <li><strong>Regeneration frequency</strong> — first-attempt failure rate</li> <li><strong>Turn count</strong> — more back-and-forth needed to complete the same task signals degradation</li> </ul> <p>Monitoring these signals reduces the need to score every output. Signal-based sampling — classifying interactions by misalignment, stagnation, and disengagement — surfaces the sessions that need human review without running LLM-as-judge on every request.</p> <h3 id="system-level-reliability--drift-signals">System-level reliability — drift signals</h3> <p>Provider updates, prompt changes, or traffic shifts can degrade systems even when component metrics stay stable. Models change tone, add preambles, or shift output format — breaking downstream parsers, prompts that expect specific structure, or agent chains tuned to previous behavior. Production systems track output length variance, format compliance, and semantic drift, alerting when distribution shifts exceed baseline thresholds.</p> <h3 id="agent-trajectory-evaluation--task-completion-signals">Agent trajectory evaluation — task completion signals</h3> <p>Component accuracy misleads in multi-step settings. Errors compound across steps — a model that passes each skill test in isolation can fail substantially when autonomously chaining those skills end-to-end.</p> <p>Success metrics must track trajectory cost: an agent taking 6 steps and 5 tool calls for a 4-step task reveals brute-force trial-and-error, not fluent reasoning. Eval datasets that mirror production noise — incomplete logs, ambiguous alerts, missing context — produce lower pass rates but better predict real-world failures.</p> <hr/> <p align="center"> <i>Evaluation is not about scoring outputs. It is about detecting when the model stops solving user problems — even if it still passes tests.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Classic metrics hide the failures users notice. Production evaluation should measure friction, drift, and task completion.]]></summary></entry><entry><title type="html">Context limits degrade routing quality faster than generation</title><link href="https://avchauzov.github.io/blog/2026/routing-quality-context-limits/" rel="alternate" type="text/html" title="Context limits degrade routing quality faster than generation"/><published>2026-01-18T00:00:00+00:00</published><updated>2026-01-18T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/routing-quality-context-limits</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/routing-quality-context-limits/"><![CDATA[<p>Context windows are growing, but more context does not always help. Long-form <strong>analysis</strong> and <strong>summarization</strong> benefit from comprehensive context; <strong>routing and classification</strong> are different — the model must emit a discrete decision on every call.</p> <p>Past a threshold, additional context <strong>degrades routing decisions</strong> before the physical window fills.</p> <p>In practice, routing quality often remains stable only within a limited fraction of the context window (around <strong>40–50%</strong> in reported setups); beyond that share, margins tend to collapse.</p> <h2 id="the-40-cliff">The 40% cliff</h2> <p><strong>Qwen2.5-7B</strong> on a <strong>128k-scale</strong> setup is one <strong>documented case</strong>: F1 falls from <strong>0.56</strong> to <strong>0.30</strong> across a narrow range of prompt lengths — a cliff, not a slow slope.</p> <p>Routing quality shifts from stable to unreliable within a small increase in context, rather than degrading gradually. That curve is <strong>one model’s</strong>; it illustrates the <strong>usable-band</strong> idea from the introduction, not a guarantee for every router.</p> <h2 id="why-routing-breaks-differently-than-generation">Why routing breaks differently than generation</h2> <p>Open-ended generation can smooth over uncertainty with generic phrasing; quality often declines gradually. <strong>Routing</strong> still forces a discrete choice every time, so <strong>logit separation</strong> matters more than fluency. The failure is not softer text but <strong>wrong or nearly tied scores</strong> among options.</p> <h2 id="score-dilution--margin-collapse--routing-collapse">Score dilution → margin collapse → routing collapse</h2> <h3 id="score-dilution">Score dilution</h3> <p>Attention over the prompt is <strong>softmax-normalized</strong>: weights sum to one, so each new token spreads a fixed mass of “focus” thinner. Current architectures do not guarantee that the gap between the correct logit and distractors grows with context length. As length increases, probability on the intended class shrinks even when the routing instruction is still present — context acts as <strong>structured noise for the routing decision</strong>, not neutral background.</p> <h3 id="margin-collapse">Margin collapse</h3> <p>When routing instructions sit mid-context instead of at the edges, accuracy falls by <strong>over 30%</strong> relative to edge placement; adding a few hundred distractor tokens can suppress reasoning traces by <strong>up to 50%</strong> (Qwen3.5-27B). For routers, that reads as a shrinking <strong>top-1 minus top-2</strong> margin: adjacent intents or model tiers stop separating cleanly.</p> <h3 id="routing-collapse">Routing collapse</h3> <p>When the <strong>top-1 vs top-2 margin</strong> becomes too small (e.g. <strong>&lt; 0.10</strong>), the router <strong>stops separating</strong> options and <strong>defaults to the safest path</strong>.</p> <p><strong>Hard negatives</strong> — retrieved chunks that appear relevant but imply the wrong route — act as additional distractors, further narrowing margins when placed alongside correct context.</p> <h2 id="minimal-production-rules">Minimal production rules</h2> <h3 id="hard-cap-at-40">Hard cap at ~40%</h3> <p>Start with a <strong>conservative fraction</strong> of <code class="language-plaintext highlighter-rouge">MODEL_CONTEXT_WINDOW</code> and adjust it based on observed margin stability; when over budget, truncate oldest history or documents first.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># For 128k model: ~50k routing budget
</span><span class="n">ROUTING_CONTEXT_CAP_FRACTION</span> <span class="o">=</span> <span class="mf">0.4</span>
<span class="n">MAX_ROUTING_TOKENS</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">MODEL_CONTEXT_WINDOW</span> <span class="o">*</span> <span class="n">ROUTING_CONTEXT_CAP_FRACTION</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">prepare_routing_context</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">history</span><span class="p">,</span> <span class="n">retrieved_docs</span><span class="p">):</span>
    <span class="n">context</span> <span class="o">=</span> <span class="nf">build_prompt</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">history</span><span class="p">,</span> <span class="n">retrieved_docs</span><span class="p">)</span>
    <span class="n">tokens</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">context</span><span class="p">)</span>

    <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span> <span class="o">&gt;</span> <span class="n">MAX_ROUTING_TOKENS</span><span class="p">:</span>
        <span class="c1"># Truncate oldest history first
</span>        <span class="n">context</span> <span class="o">=</span> <span class="nf">build_prompt</span><span class="p">(</span>
            <span class="n">query</span><span class="p">,</span>
            <span class="n">history</span><span class="p">[</span><span class="o">-</span><span class="n">N_RECENT_TURNS</span><span class="p">:],</span>
            <span class="n">retrieved_docs</span><span class="p">[:</span><span class="n">TOP_K_DOCS</span><span class="p">],</span>
        <span class="p">)</span>

    <span class="k">return</span> <span class="n">context</span>
</code></pre></div></div> <h3 id="confidence-aware-cascading">Confidence-aware cascading</h3> <p>When the margin between the top two routes is small, treat the decision as unreliable and escalate to a stronger model instead of accepting a noisy route.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">route_with_cascade</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">context</span><span class="p">):</span>
    <span class="n">scores</span> <span class="o">=</span> <span class="n">router</span><span class="p">.</span><span class="nf">predict_proba</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">context</span><span class="p">)</span>
    <span class="n">best</span><span class="p">,</span> <span class="n">second_best</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="n">scores</span><span class="p">,</span> <span class="n">reverse</span><span class="o">=</span><span class="bp">True</span><span class="p">)[:</span><span class="mi">2</span><span class="p">]</span>
    <span class="n">margin</span> <span class="o">=</span> <span class="n">best</span> <span class="o">-</span> <span class="n">second_best</span>

    <span class="k">if</span> <span class="n">margin</span> <span class="o">&lt;</span> <span class="n">CONFIDENCE_THRESHOLD</span><span class="p">:</span>  <span class="c1"># e.g., 0.10
</span>        <span class="k">return</span> <span class="n">FALLBACK_MODEL</span>

    <span class="k">return</span> <span class="nf">argmax</span><span class="p">(</span><span class="n">scores</span><span class="p">)</span>
</code></pre></div></div> <h2 id="when-context-can-be-larger">When context can be larger</h2> <p>The <strong>~40%</strong> band (of declared capacity) applies when <strong>margin quality</strong> drives system behavior — <strong>routing</strong>, intent classification, model selection. Different tasks face different constraints:</p> <ul> <li><strong>Document QA</strong> — When retrieval is high-precision, comprehensive context helps; the bottleneck is finding the right passage, not attention dilution alone</li> <li><strong>Code generation</strong> — Cross-file dependencies may justify near-full windows when structure and imports matter more than a single routing decision</li> <li><strong>Summarization</strong> — Errors are gradual, not binary; models can smooth over lost detail without catastrophic failure</li> </ul> <p>The distinction: routing failures are <strong>costly and discrete</strong> (wrong model or route wastes budget or breaks the request). Generation failures are often <strong>recoverable</strong> through iteration or reranking.</p> <h2 id="summary">Summary</h2> <p><strong>Routing quality collapses before context windows fill</strong>. Operate within the <strong>first ~40%</strong> of declared capacity, <strong>monitor top-1 vs top-2 margins</strong>, and <strong>escalate</strong> to stronger models when scores are unstable — not when the window is full.</p> <hr/> <table> <thead> <tr> <th>Rule</th> <th>Purpose</th> <th>Typical threshold</th> </tr> </thead> <tbody> <tr> <td>Hard cap at ~40%</td> <td>Prevent cliff-like degradation</td> <td>~50k for 128k models</td> </tr> <tr> <td>Confidence-aware cascading</td> <td>Escalate uncertain routes</td> <td>Margin below 0.10</td> </tr> </tbody> </table> <hr/>]]></content><author><name></name></author><summary type="html"><![CDATA[Routing and classification under long prompts: score dilution, margin collapse, routing collapse, and practical caps.]]></summary></entry><entry><title type="html">RAG fails upstream</title><link href="https://avchauzov.github.io/blog/2026/rag-knowledge-foundation-layer/" rel="alternate" type="text/html" title="RAG fails upstream"/><published>2026-01-11T00:00:00+00:00</published><updated>2026-01-11T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/rag-knowledge-foundation-layer</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/rag-knowledge-foundation-layer/"><![CDATA[<p>Most RAG debugging sessions swap around the same stock: prompt, embedding model, hyperparameters, reranker. Even when retrieval metrics improve, the system still gives wrong answers, because the problem usually started earlier — before any query was issued.</p> <p><strong>RAG</strong> systems fail at three distinct points: generation, retrieval, and knowledge preparation. The first two receive most of the engineering attention, while the third — often called the <strong>knowledge foundation layer</strong> — is where failures most often originate and least often get diagnosed.</p> <h2 id="the-knowledge-layer-nobody-names">The knowledge layer nobody names</h2> <p><strong>ECL replaces ETL</strong>. Standard ETL thinking treats document ingestion as a solved problem: parse, chunk, embed, index. What gets overlooked is that meaning does not transfer automatically through this pipeline, which is why a better mental model is ECL: Extract, Contextualize, Link. The contextualize step is where most production failures occur. It involves mapping source schemas to normalized representations, establishing metadata that will govern retrieval scope, and making chunking decisions that cannot be corrected downstream.</p> <h2 id="chunking-as-architecture">Chunking as architecture</h2> <p>Chunk boundaries determine what the model can retrieve and what it cannot, which is why chunking is an architectural decision.</p> <p><strong>Flat chunking breaks multi-hop reasoning</strong>. On multi-hop synthesis tasks, a standard flat RAG pipeline achieves <strong>33%</strong> accuracy, while indexing with hierarchical representations — storing both raw chunks and LLM-generated cluster summaries in a single flat store — reaches <strong>85%</strong> on the same benchmark, not because retrieval improved, but because what was indexed changed.</p> <p><strong>Format-agnostic chunking destroys structure</strong>. Applying text embedding logic to structured data treats every format as prose, erasing the relationships that make it meaningful: knowledge graph edges are severed, tables lose row-column structure, and multi-column layouts collapse into linear text. JSON is a simpler version of the same issue — tokenizers fragment syntax into structural noise, shifting the vector’s semantic center. Converting these objects into natural language representations before embedding improves recall and <strong>Mean Reciprocal Rank (MRR)</strong> by roughly <strong>20%</strong> while reducing token counts.</p> <p><strong>Parsing failures start before chunking</strong>. In head-to-head comparisons across real enterprise documents, common parsers:</p> <ul> <li>Returned incomplete outputs on skewed or poorly scanned documents</li> <li>Merged text from unrelated layout boxes in complex magazine-style layouts</li> <li>Stripped all table formatting from financial filings, rendering the data uninterpretable</li> </ul> <p>Parsing failures are not edge cases — they poison the context window before the first embedding is computed.</p> <h2 id="metadata-is-not-tagging">Metadata is not tagging</h2> <p>The default approach to metadata is taxonomic: add a few labels for filtering, but it is easy to miss the structural role metadata plays in a functioning knowledge foundation.</p> <p><strong>Metadata governs applicability</strong>. Without fields that define scope — plan tier, enrollment date, region, effective date — retrieval cannot determine whether a returned passage is relevant to this user’s condition. A topically relevant chunk about warranty terms is useless if it applies to a different product tier, but the system blends it into the answer anyway — high similarity score, wrong answer.</p> <p><strong>Temporal metadata is a special case</strong>. Without supersession rules and effective dates in the schema, systems retrieve historical truths with high confidence — the document is correct, but for a version of the world that no longer exists.</p> <p><strong>Schema design cannot be retrofitted</strong>. Decisions about which attributes to capture, how to represent them, and which fields to enforce need to happen at ingestion time. Changing the schema later requires full re-ingestion, and there is no patch that can be applied at query time.</p> <h2 id="query-enrichment-when-it-helps-when-it-hurts">Query enrichment: when it helps, when it hurts</h2> <p><strong>Hypothetical Document Embeddings (HyDE)</strong> generates a hypothetical answer before retrieval, then embeds that answer instead of the original query, which transforms the search from question-to-answer matching into answer-to-answer matching — a better-defined problem for dense retrieval.</p> <p>The gains are real but conditional:</p> <ul> <li>On tasks with significant vocabulary mismatch, HyDE improves retrieval accuracy by up to <strong>25%</strong></li> <li>On the TREC-COVID dataset, query rewriting boosted nDCG@10 by <strong>5.1%</strong> by standardizing varied medical queries to corpus-preferred nomenclature</li> </ul> <p>The losses are also real:</p> <ul> <li>On the FiQA financial benchmark, prompt-only rewriting dropped nDCG@10 by <strong>9%</strong> and Recall@10 by <strong>9.4%</strong> — the rewriter discarded the precise jargon that made the original query retrievable</li> <li>For factual numerical domains, hallucinated figures in the hypothetical document actively misdirect the embedding</li> <li>Full pipeline latency: HyDE combined with hybrid search reached <strong>11.7s</strong> per query versus <strong>1.4s</strong> for standard embeddings</li> </ul> <p><strong>HyPE moves the cost to index time</strong>. A more production-viable alternative is <strong>Hypothetical Prompt Embeddings (HyPE)</strong>: generate synthetic questions per chunk at indexing time and embed those instead, turning retrieval into question-to-question matching. This improves context precision by up to <strong>42 percentage points</strong> with zero query-time latency overhead — the enrichment cost is paid once, at ingestion, which is exactly where it belongs.</p> <h2 id="evaluation-tracing-and-index-health">Evaluation, tracing, and index health</h2> <p><strong>Offline retrieval metrics</strong> — Precision@k, Recall@k, MRR — are fast, cheap, and do not require an LLM call. They can catch chunking failures, metadata gaps, and embedding model mismatches before any prompt is written. Most teams run them post-deployment; running them as part of the ingestion pipeline changes what gets caught and when.</p> <p><strong>Synthetic eval datasets require careful construction</strong>. Generating queries with a generic prompt produces a shallow dataset that tests only the happy path. A more reliable approach is a reverse workflow — extract a specific fact from a chunk, then generate a query that can only be answered by that chunk.</p> <p><strong>Index degradation at scale</strong> is non-linear and frequently misdiagnosed. HNSW indexes suffer local minima traps as the corpus grows: results with high similarity scores (0.85+) that are topically irrelevant start appearing in top-k, and long-tail queries degrade in recall first.</p> <p>In controlled testing, scaling from 10k to 200k vectors caused search latency to spike <strong>12.3x</strong> — from 90ms to 1,129ms — just to maintain recall. Increasing <code class="language-plaintext highlighter-rouge">ef_search</code> to recover recall doubles latency with each step, eventually defeating the purpose of approximate search entirely. The correct fix is scheduled full index rebuilds at defined corpus size thresholds (<code class="language-plaintext highlighter-rouge">1M</code>, <code class="language-plaintext highlighter-rouge">5M</code>, <code class="language-plaintext highlighter-rouge">10M</code> vectors) with recalculated graph parameters — not incremental appending to the same index.</p> <p><strong>Tracing requires persistent identifiers</strong> at every stage: source document, chunk ID, embedding version. Without them, failure mode and failure source are indistinguishable.</p> <h2 id="what-to-enforce-four-rules">What to enforce: four rules</h2> <ol> <li><strong>Think about how to process structured data before embedding it</strong>. Raw JSON, tables, and knowledge graph nodes all carry meaning that standard tokenizers will fragment; converting structured objects into natural language representations preserves that meaning at the cost of a one-time ingestion step</li> <li><strong>Define metadata schema before indexing, not after</strong>. Temporal attributes, scope fields, and authority constraints cannot be retrofitted without full re-ingestion</li> <li><strong>Test HyPE for production</strong>. Paying the enrichment cost at index time avoids per-query latency overhead; reserve query-time expansion for cases where corpus terminology is genuinely unstable</li> <li><strong>Rebuild HNSW indexes, do not append indefinitely</strong>. Schedule full rebuilds at defined corpus size thresholds with recalculated graph parameters</li> </ol> <hr/> <p align="center"> <i>Decisions made at ingestion time set the ceiling for everything downstream. Retrieval cannot recover information that chunking destroyed, and generation cannot recover context that retrieval missed.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Why most RAG failures originate in the data preparation layer, and what to do about it.]]></summary></entry><entry><title type="html">Embeddings for intent classification: architecture trade-offs</title><link href="https://avchauzov.github.io/blog/2026/intent-routing-architecture-tradeoffs/" rel="alternate" type="text/html" title="Embeddings for intent classification: architecture trade-offs"/><published>2026-01-04T00:00:00+00:00</published><updated>2026-01-04T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2026/intent-routing-architecture-tradeoffs</id><content type="html" xml:base="https://avchauzov.github.io/blog/2026/intent-routing-architecture-tradeoffs/"><![CDATA[<p>Intent classification in chatbots is not just an ML task. It is a routing layer that determines resource consumption of the entire system. Wrong architecture forces expensive LLM calls for trivial queries, burning budget and increasing latency.</p> <p>Previously, we discussed how fine-tuned BERT can be a better alternative to the LLM-based classification. Now, let’s consider an even simpler solution: embeddings + shallow classifier — when it works, when it fails, and whether you need to fine-tune the encoder.</p> <h2 id="the-canonical-architecture">The canonical architecture</h2> <p>The pattern splits inference into two stages:</p> <ol> <li><strong>Feature extraction</strong>: Frozen compact encoder (small multilingual or contrastive embedding models, or similar) converts text to dense vector. Single forward pass, shared across all downstream tasks</li> <li><strong>Classification head</strong>: Lightweight model (Logistic Regression, Linear SVM) operates on the vector. Inference is matrix multiplication — microseconds on CPU</li> </ol> <h3 id="why-this-often-wins-over-fine-tuned-bert">Why this often wins over fine-tuned BERT</h3> <p>Real-world benchmarks (Banking77, CLINC150) show the trade-offs:</p> <hr/> <table> <thead> <tr> <th>Metric</th> <th>Embeddings + LR</th> <th>Fine-tuned BERT</th> </tr> </thead> <tbody> <tr> <td>Accuracy</td> <td>92–93%</td> <td>93–95%</td> </tr> <tr> <td>Latency (CPU)</td> <td>4–20ms</td> <td>50–300ms</td> </tr> <tr> <td>Training time</td> <td>Seconds</td> <td>Hours</td> </tr> <tr> <td>Multi-tenant scaling</td> <td>One encoder for all</td> <td>Separate model per task</td> </tr> </tbody> </table> <hr/> <p>The accuracy gap is 1–3%. The latency gap is 5–15x. For most production cases, this trade-off favors embeddings.</p> <p>The core limitation: frozen encoder cannot adapt to domain-specific vocabulary. If terminology differs significantly from pretraining corpus, semantically different intents may map to similar regions in embedding space.</p> <p>Once the architecture is set, embedding quality becomes the main variable.</p> <h2 id="embedding-dimensionality">Embedding dimensionality</h2> <p>Default 768 dimensions are often excessive for intent routing. Dimensionality directly impacts memory (index size scales linearly) and latency (scoring is O(d) per candidate).</p> <p>Practical approaches to reduction:</p> <hr/> <table> <thead> <tr> <th>Method</th> <th>Description</th> <th>Typical quality loss</th> </tr> </thead> <tbody> <tr> <td>PCA post-processing</td> <td>Reduce 768→256 on your dataset</td> <td>&lt;1% (often)</td> </tr> <tr> <td>Matryoshka models</td> <td>Truncate vector at inference</td> <td>Depends on cutoff</td> </tr> <tr> <td>Binary quantization</td> <td>1 bit per dimension</td> <td>2–5%, but 32x memory savings</td> </tr> </tbody> </table> <hr/> <p>Run ablation before accepting d=768 as given. For routing tasks, 256 dimensions are often sufficient.</p> <h2 id="shallow-classifier-selection">Shallow classifier selection</h2> <p>In practice, two linear models dominate:</p> <p><strong>Logistic Regression</strong></p> <p>Default choice. Outputs calibrated probabilities — critical for confidence-based routing. Inference is dot product. Works well when embedding space is linearly separable (which it usually is with modern encoders).</p> <p><strong>Linear SVM</strong></p> <p>Better generalization on small or noisy data (&lt;50 samples per class). Margin-based optimization is less sensitive to outliers. Downside: no native probability output — requires calibration wrapper.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>
<span class="kn">from</span> <span class="n">sklearn.calibration</span> <span class="kn">import</span> <span class="n">CalibratedClassifierCV</span>
<span class="kn">from</span> <span class="n">sklearn.svm</span> <span class="kn">import</span> <span class="n">LinearSVC</span>

<span class="c1"># Default choice
</span><span class="n">clf</span> <span class="o">=</span> <span class="nc">LogisticRegression</span><span class="p">(</span><span class="n">max_iter</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span> <span class="n">C</span><span class="o">=</span><span class="mf">1.0</span><span class="p">)</span>

<span class="c1"># For small/noisy data — SVM with calibration
</span><span class="n">clf</span> <span class="o">=</span> <span class="nc">CalibratedClassifierCV</span><span class="p">(</span>
    <span class="nc">LinearSVC</span><span class="p">(</span><span class="n">C</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">max_iter</span><span class="o">=</span><span class="mi">10000</span><span class="p">),</span>
    <span class="n">method</span><span class="o">=</span><span class="sh">'</span><span class="s">sigmoid</span><span class="sh">'</span>
<span class="p">)</span>
</code></pre></div></div> <p>Non-linear models (Random Forest, deep MLP) typically do not improve over linear ones. The representation is already separable — added complexity increases latency without accuracy gain.</p> <h2 id="training-data-strategies">Training data strategies</h2> <p><strong>Real labeled data</strong>: High accuracy on frequent queries, expensive to collect, poor coverage of rare cases.</p> <p><strong>Synthetic data (LLM-generated)</strong>: Fast to scale, but risks distribution shift and style bias. Critical point: generate hard negatives — queries that share keywords with an intent but have different meaning.</p> <p><strong>Hybrid approach</strong>: Real data for frequent queries (“head”), synthetic for rare cases (“tail”). Apply round-trip filtering: generate example → classify with stronger model → discard if mismatch.</p> <p>Hard negatives matter more than volume. Example: query “What documents are NOT needed for mortgage?” should not match intent “Mortgage document list”. Adding such near-miss examples to training improves Out-of-Scope (OOS) detection — AUROC can jump from 0.91 to 0.98.</p> <h2 id="confidence-and-routing">Confidence and routing</h2> <p>Logistic Regression outputs calibrated probabilities by design. The practical challenge is not calibration — it is setting thresholds.</p> <h3 id="per-class-thresholds">Per-class thresholds</h3> <p>Global threshold (e.g., 0.7 for all intents) fails when intents have different cluster densities in embedding space. Narrow intent like “password reset” needs higher threshold (0.9) than broad intent like “general complaint” (0.6).</p> <p>Automatic threshold formula: <code class="language-plaintext highlighter-rouge">T_i = max(0.5, μ_i - α·σ_i)</code> where μ and σ are mean and standard deviation of confidence scores on validation set for intent i.</p> <h3 id="tiered-routing">Tiered routing</h3> <p>Do not make routing binary. Three zones work better:</p> <hr/> <table> <thead> <tr> <th>Confidence</th> <th>Action</th> </tr> </thead> <tbody> <tr> <td>&gt; τ_high</td> <td>Deterministic response or API call</td> </tr> <tr> <td>τ_low &lt; c ≤ τ_high</td> <td>Route to LLM for verification</td> </tr> <tr> <td>&lt; τ_low</td> <td>Fallback: human handoff or “I don’t know”</td> </tr> </tbody> </table> <hr/> <p>Middle zone catches uncertain cases without expensive LLM calls for obvious ones. This reduces average cost by 40–60% compared to routing everything through LLM.</p> <h3 id="out-of-scope-detection">Out-of-scope detection</h3> <p>Out-of-Scope queries (user asks something outside known intents) are tricky. Classifier will still output some class with non-trivial confidence.</p> <p>Options:</p> <ul> <li>Train explicit OOS class using samples from open-domain datasets</li> <li>Use distance to class centroids in embedding space — large distance signals OOS</li> <li>Set conservative τ_low and route uncertain queries to fallback</li> </ul> <h2 id="common-failure-modes">Common failure modes</h2> <p>Most failures are not classifier bugs, but representation limits.</p> <p><strong>Negation blindness</strong></p> <p>Mean pooling loses syntactic structure. “Denies chest pain” embeds similarly to “has chest pain” because dominant tokens are the same.</p> <p>Mitigation: For high-stakes domains (medical, legal), add rule-based layer or LLM verification for edge cases.</p> <p><strong>Fine-grained intent collapse</strong></p> <p>Semantically close intents (<code class="language-plaintext highlighter-rouge">card_arrival</code> vs <code class="language-plaintext highlighter-rouge">card_tracking</code>) overlap in embedding space. Linear classifier cannot separate what encoder did not distinguish.</p> <p>Mitigation: Merge into single intent with sub-classification, or fine-tune encoder with contrastive loss on hard pairs.</p> <h2 id="do-you-need-to-fine-tune-the-encoder">Do you need to fine-tune the encoder?</h2> <p>Short answer: usually no. Off-the-shelf embedding models from commercial APIs or open encoder families handle most intent classification tasks.</p> <h3 id="when-frozen-embeddings-are-sufficient">When frozen embeddings are sufficient</h3> <ul> <li>Domain is close to general text (support tickets, FAQ, e-commerce)</li> <li>Intents are semantically distinct</li> <li>You have enough labeled data for the classifier head</li> <li>Accuracy on validation set meets requirements</li> </ul> <h3 id="signs-that-encoder-quality-is-the-bottleneck">Signs that encoder quality is the bottleneck</h3> <ul> <li>Close intents “collapse” — confusion matrix shows systematic errors between specific pairs</li> <li>Accuracy plateaus despite sufficient training data</li> <li>Domain has specialized terminology (medical codes, legal jargon, internal product names)</li> </ul> <h3 id="alternatives-before-full-fine-tuning">Alternatives before full fine-tuning</h3> <ol> <li> <p><strong>Try stronger encoder</strong>: Switch to a larger contrastive embedding model. Latency increases but may solve the problem</p> </li> <li> <p><strong>Contrastive fine-tuning on hard pairs</strong>: Lighter than full fine-tuning. Collect pairs of confused intents, train with contrastive loss. Often sufficient</p> </li> <li> <p><strong>Add verification layer</strong>: Keep fast classifier for 80% of queries, route confused pairs to LLM or cross-encoder for final decision</p> </li> <li> <p><strong>LoRA/Adapters</strong>: If you need fine-tuning but serve multiple tenants, parameter-efficient methods keep single base model with lightweight per-task modules</p> </li> </ol> <h3 id="when-full-fine-tuning-is-justified">When full fine-tuning is justified</h3> <ul> <li>Significant domain shift (medical, legal, highly specialized)</li> <li>Classification depends on syntax (negations, conditionals)</li> <li>Sentence-pair tasks where cross-attention matters</li> <li>You have &gt;1000 samples per class and can afford training time</li> </ul> <p>Fine-tuning adapts embedding space itself — attention weights learn domain-specific patterns. But cost is real: hours of training, separate model per task, higher inference latency.</p> <h2 id="production-considerations">Production considerations</h2> <p>For chatbot latency (&lt;100ms end-to-end):</p> <ul> <li>Use small compact encoder (distilled transformer variants)</li> <li>Export to ONNX, apply INT8 quantization — gives 2–5x speedup</li> <li>Add semantic cache for frequent queries — hash lookup is faster than any model</li> </ul> <h2 id="summary">Summary</h2> <p>Embeddings + shallow classifier is a strong production baseline. It scales across tenants, iterates in seconds, handles few-shot scenarios well.</p> <p>The decision flow:</p> <ol> <li>Start with off-the-shelf encoder + Logistic Regression</li> <li>Check confusion matrix — if specific pairs collapse, try stronger encoder or contrastive tuning</li> <li>Add tiered routing with per-class thresholds</li> <li>Invest in hard negative generation — improves decision boundaries more than adding positive samples</li> <li>Fine-tune encoder only if domain shift is significant and alternatives fail</li> </ol> <p>Component metrics (F1, accuracy) are diagnostic tools. Optimize for business metrics: resolution rate, cost per query, fallback rate.</p> <hr/> <table> <thead> <tr> <th>Approach</th> <th>Latency</th> <th>Memory</th> <th>When to use</th> </tr> </thead> <tbody> <tr> <td>Frozen encoder + LR</td> <td>&lt;20ms</td> <td>O(1)</td> <td>Default starting point</td> </tr> <tr> <td>Stronger encoder + LR</td> <td>30–50ms</td> <td>O(1)</td> <td>Close intents collapse</td> </tr> <tr> <td>Contrastive fine-tuning</td> <td>&lt;20ms, 30–50ms</td> <td>O(1)</td> <td>Hard pairs, moderate domain shift</td> </tr> <tr> <td>Full fine-tuning</td> <td>50–300ms</td> <td>O(N) for N tasks</td> <td>Significant domain shift, syntax-dependent</td> </tr> </tbody> </table> <hr/>]]></content><author><name></name></author><summary type="html"><![CDATA[Practical guide to building intent classifiers with embeddings. When shallow classifiers beat fine-tuning, how to handle confidence thresholds, and what actually matters in production.]]></summary></entry><entry><title type="html">Similarity metrics for embeddings</title><link href="https://avchauzov.github.io/blog/2025/similarity-metrics-embeddings/" rel="alternate" type="text/html" title="Similarity metrics for embeddings"/><published>2025-12-29T00:00:00+00:00</published><updated>2025-12-29T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/similarity-metrics-embeddings</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/similarity-metrics-embeddings/"><![CDATA[<p>In retrieval and RAG pipelines, the choice of similarity metric is rarely questioned. Default is cosine, and we move on. But a metric is not just a post-processing step. It is part of the model. It defines what “close” means in your embedding space.</p> <h2 id="why-cosine-similarity-became-de-facto-standard">Why cosine similarity became de-facto standard</h2> <p>Cosine compares the direction of vectors, ignoring their magnitude (L2 norm). Mathematically it is just the dot product on normalized vectors.</p> <p>Why this is convenient:</p> <p><strong>Scale invariance</strong>. A long document and a short summary about the same topic end up close. The norm of a vector (which often correlates with text length or token frequency) does not affect the result. This was critical in the TF-IDF era, and the habit carried over to dense embeddings.</p> <p><strong>Bounded output</strong>. Result is always in <code class="language-plaintext highlighter-rouge">[-1, 1]</code>. You can set threshold like <code class="language-plaintext highlighter-rouge">0.75</code> and it means something. Compare this to L2 distance where you need to guess what “distance 47.3” means in 768-dimensional space.</p> <p><strong>Training alignment</strong>. Most embedding models are trained with <strong>InfoNCE (Information Noise Contrastive Estimation)</strong> or Cosine Embedding Loss. Using a different metric at inference means violating geometric assumptions that the model learned during training.</p> <p>Hidden assumptions that cosine makes:</p> <ul> <li>Semantic meaning is encoded in direction, not magnitude</li> <li>Norm is noise (artifact of text length, word frequency)</li> <li>Embedding space lives on hypersphere (due to LayerNorm or explicit normalization)</li> </ul> <h2 id="where-cosine-similarity-breaks-down">Where cosine similarity breaks down</h2> <h3 id="anisotropy-problem">Anisotropy problem</h3> <p><strong>What you observe</strong>: All similarity scores compressed into narrow range like <code class="language-plaintext highlighter-rouge">0.92–0.98</code>. Documents that are clearly irrelevant have scores almost identical to relevant ones.</p> <p><strong>Root cause</strong>: Representation collapse. In transformer-based embedding models, embeddings tend to occupy a narrow cone in high-dimensional space. When everything points roughly the same direction, cosine loses discriminative power. Ranking becomes sensitive to floating-point noise. Small changes in query formulation can dramatically change top-k results because the margin between relevant and irrelevant is tiny.</p> <h3 id="frequency-bias">Frequency bias</h3> <p><strong>What you observe</strong>: Documents with common vocabulary consistently rank lower than documents with rare words, even when semantically they should be closer.</p> <p><strong>Root cause</strong>: High-frequency tokens occupy constrained angular regions of embedding space. Cosine similarity between tokens in these crowded regions is artificially inflated, regardless of semantic relatedness.</p> <h3 id="noise-amplification">Noise amplification</h3> <p><strong>What you observe</strong>: Empty strings, garbage inputs, or <strong>Out-of-Distribution (OOD)</strong> samples appear in top results with surprisingly high scores.</p> <p><strong>Root cause</strong>: Model encodes uncertainty through low vector norm. Cosine normalization (dividing by norm) stretches these “uncertain” vectors to unit length, making them indistinguishable from confident predictions. If you accidentally index garbage, it will match everything.</p> <h3 id="confidence-signal-loss">Confidence signal loss</h3> <p><strong>What you observe</strong>: In recommendations, niche items with perfect angle match crowd out popular items that a user would actually prefer.</p> <p><strong>Root cause</strong>: In <strong>Matrix Factorization (MF)</strong> and collaborative filtering, vector norm encodes meaningful signal — popularity for items, activity level for users. Cosine destroys this information. Research showed that using cosine on MF embeddings can produce mathematically arbitrary results because regularization creates scaling freedom that dot product ignores but cosine does not.</p> <h2 id="dot-product-when-it-outperforms-cosine">Dot product: when it outperforms cosine</h2> <p>Key difference: dot product combines direction and magnitude. <code class="language-plaintext highlighter-rouge">A · B = ||A|| ||B|| cos(θ)</code></p> <p>This matters when norm carries useful signal:</p> <p><strong>Confidence encoding</strong>. In some self-supervised setups, norm correlates with model certainty. These methods use contrastive or non-contrastive objectives where prototypical, frequently-seen samples develop larger norms, while rare or OOD samples have smaller norms. Dot product down-weights these uncertain predictions.</p> <p><strong>Popularity signal</strong>. In <strong>Recommendation System (RecSys)</strong>, “long” item vector typically means popular or high-quality item. “Long” user vector means active user with stable preferences. You often want to boost popular items slightly, and dot product does this automatically.</p> <p><strong>Feature intensity</strong>. In sparse representations (TF-IDF), magnitude indicates how strongly a feature is present. A document where a keyword appears 10 times should score higher than a document where it appears once. Cosine normalizes this away.</p> <p>Production advantages:</p> <ul> <li>Computationally cheaper (no <code class="language-plaintext highlighter-rouge">sqrt</code>, no division)</li> <li>Vector databases (Milvus, Pinecone, Faiss) optimize heavily for Inner Product</li> </ul> <p>Risks to consider:</p> <ul> <li>Without normalization, long documents dominate results regardless of relevance</li> <li>Unbounded output makes threshold selection difficult</li> </ul> <p><strong>Important note</strong>: If vectors are already normalized (<code class="language-plaintext highlighter-rouge">||v||=1</code>), dot product and cosine produce identical ranking. In this case, choosing dot product is purely computational optimization.</p> <h2 id="euclidean-distance-correct-but-demanding">Euclidean distance: correct but demanding</h2> <p>The L2 (Euclidean) norm is the standard geometry for this metric. When it applies:</p> <ul> <li>Clustering algorithms (k-means is built on L2)</li> <li>Computer vision tasks (comparing pixel intensities)</li> <li>Data where magnitude has physical meaning</li> </ul> <p>Why it is rarely used directly for retrieval:</p> <p><strong>Distance concentration</strong>. In high dimensions (&gt;1000), ratio of maximum to minimum distance approaches 1. Mathematical result known as “curse of dimensionality”. Distinction between nearest and farthest neighbor becomes statistically meaningless for uniformly distributed data. Search quality degrades.</p> <p><strong>Outlier sensitivity</strong>. Quadratic dependence on coordinate differences means single spike in one dimension dominates total distance. Model artifacts or noisy dimensions have outsized impact.</p> <p><strong>Scale sensitivity</strong>. Without normalization, longer vectors are always “farther” from query, even if semantically they are richer.</p> <h2 id="manhattan-distance-where-it-applies">Manhattan distance: where it applies</h2> <p>L1 (Manhattan) distance treats space as axis-aligned. Where it has legitimate use:</p> <ul> <li>Sparse vectors (BM25-style representations)</li> <li>Binary embeddings (where L1 reduces to Hamming distance) — used in production for first-stage retrieval with 32x memory savings</li> <li>Tabular data with interpretable per-feature meaning</li> <li>Histogram comparison in computer vision</li> </ul> <p>Why it fails for dense embeddings:</p> <p><strong>Why L1 fails for dense embeddings</strong>. L1 distance is axis-aligned: it assumes that each coordinate has independent semantic meaning. This assumption holds for tabular or sparse features, but breaks completely for neural embeddings, where meaning is distributed across rotated subspaces.</p> <p>As a result, L1 distance is highly sensitive to arbitrary basis rotations introduced during training. Two semantically equivalent embeddings can have very different L1 distances depending on how the network chose its internal representation.</p> <p>From a systems perspective, L1 is also poorly supported by <strong>Approximate Nearest Neighbor (ANN)</strong> indexes and hardware acceleration paths, making it both <strong>geometrically unsound and operationally inefficient</strong> for dense embeddings.</p> <h3 id="quick-reference-map">Quick reference map</h3> <hr/> <table> <thead> <tr> <th>Metric</th> <th>What it preserves</th> <th>What it discards</th> <th>Typical use case</th> </tr> </thead> <tbody> <tr> <td>Cosine</td> <td>Angular semantics</td> <td>Magnitude / confidence</td> <td>Text embeddings, RAG</td> </tr> <tr> <td>Dot product</td> <td>Direction + norm</td> <td>Scale invariance</td> <td>RecSys, learned retrievers</td> </tr> <tr> <td>L2</td> <td>Absolute geometry</td> <td>Robustness in high-d</td> <td>Clustering, ANN internals</td> </tr> <tr> <td>L1</td> <td>Axis-aligned differences</td> <td>Rotational invariance</td> <td>Sparse / tabular features</td> </tr> </tbody> </table> <hr/> <h2 id="when-metric-choice-becomes-a-problem">When metric choice becomes a problem</h2> <p>These symptoms indicate that the similarity metric is masking issues in embedding space rather than solving them:</p> <ul> <li>Top-k results have nearly identical scores (&gt;0.95 similarity)</li> <li>Reranker gives no improvement — suggests first-stage retrieval is already broken</li> <li>Hard metadata filters severely hurt recall</li> <li>Embedding norms correlate with document quality but you are using cosine</li> <li>Long documents systematically win or lose regardless of actual relevance</li> </ul> <h2 id="core-principle-what-cosine-hides">Core principle: what cosine hides</h2> <p>Cosine similarity dominates not because it is optimal, but because it hides modeling imperfections. It suppresses magnitude, smooths anisotropy, and produces stable rankings even when embedding space is poorly calibrated. This makes it a safe default — but also a lossy one.</p> <p>It works well when embedding magnitude is poorly controlled, when space is anisotropic, and when semantic signal mostly lives in direction. This describes the majority of modern text embedding models — especially those trained with contrastive objectives.</p> <p>Other metrics are not worse — they are stricter. Dot product preserves confidence and popularity encoded in vector norm. L2 exposes absolute geometric structure. L1 assumes axis-aligned meaning that dense embeddings do not have.</p> <p>The important point is not to “pick the best metric”, but to understand what information your embedding space actually encodes — and what your metric silently discards.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Why almost always cosine and what actually works?]]></summary></entry><entry><title type="html">Tokenizers: production economics cheat-sheet</title><link href="https://avchauzov.github.io/blog/2025/tokenizer-production-cheatsheet/" rel="alternate" type="text/html" title="Tokenizers: production economics cheat-sheet"/><published>2025-12-25T00:00:00+00:00</published><updated>2025-12-25T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/tokenizer-production-cheatsheet</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/tokenizer-production-cheatsheet/"><![CDATA[<p>Tokenization is the exchange rate between raw text and compute: it converts text → tokens → cost, latency, and context usage. Word-level tokenization creates very large vocabularies (hundreds of thousands to over a million entries); character-level creates 5–10× longer sequences making transformer attention prohibitive. Subword tokenization balances both: frequent sequences become single tokens, rare characters decompose into bytes, controlling vocabulary size and sequence length simultaneously.</p> <p>In practice, two metrics determine whether a tokenizer is production-viable:</p> <ul> <li><strong>TpW</strong> (Tokens per Word) measures language efficiency: <ul> <li>Thresholds: &lt;1.5 OK (≈1.3 baseline), 1.5–2.0 Warning, &gt;2.0 Bad</li> <li>Context impact: 8k window ≈ 6150 words at 1.3 TpW vs ≈ 2660 words at 3.0 TpW (56% loss)</li> </ul> </li> <li><strong>CpT</strong> (Characters per Token) measures code efficiency: <ul> <li>Thresholds: &gt;2.0 OK, 1.25–2.0 Warning, &lt;1.25 Bad (whitespace waste)</li> <li>Lossless round-trip <code class="language-plaintext highlighter-rouge">decode(encode(text)) == text</code> is required for code/structured data</li> </ul> </li> </ul> <p>Production risks include whitespace boundaries, digit splitting, glitch tokens, and normalization traps.</p> <h2 id="algorithm-comparison">Algorithm comparison</h2> <p>Four families dominate production systems, each optimizing different constraints:</p> <hr/> <table> <thead> <tr> <th>Family</th> <th>Core Mechanism</th> <th>Strength</th> <th>Weakness</th> <th>Primary Users</th> </tr> </thead> <tbody> <tr> <td><strong>BPE</strong></td> <td>Merge frequent pairs</td> <td>Simple, fast, greedy</td> <td>Inconsistent number splitting</td> <td>Decoder-style LMs, RoBERTa-style encoders</td> </tr> <tr> <td><strong>WordPiece</strong></td> <td>Merge by information gain</td> <td>Better morpheme alignment</td> <td>Mostly legacy</td> <td>Encoder-only transformers (BERT-style)</td> </tr> <tr> <td><strong>Unigram</strong></td> <td>Start large, prune by loss</td> <td>Supports regularization</td> <td>Slower training (EM algorithm)</td> <td>Encoder–decoder seq2seq, multilingual seq2seq</td> </tr> <tr> <td><strong>Byte-level</strong></td> <td>Base vocabulary = 256 bytes</td> <td>Zero unknown tokens</td> <td>Can inflate token count by several× for some scripts</td> <td>Frontier chat models, large open-weight chat LMs</td> </tr> </tbody> </table> <hr/> <p><strong>Critical implementation details</strong>:</p> <ul> <li><strong>Pre-tokenization boundaries matter</strong>: Regex rules prevent cross-category merges. Without strict boundaries, the algorithm tokenizes <code class="language-plaintext highlighter-rouge">123</code> as one token but splits <code class="language-plaintext highlighter-rouge">1234</code> into <code class="language-plaintext highlighter-rouge">12 + 34</code>, breaking arithmetic consistency</li> <li><strong>Byte-level fallback</strong>: Byte fallback guarantees coverage; tokenization cost differs across languages</li> </ul> <h2 id="tokenizer-selection-decision-matrix">Tokenizer selection decision matrix</h2> <p>Match tokenizer architecture to workload characteristics:</p> <hr/> <table> <thead> <tr> <th>Use Case</th> <th>Recommended Tokenizer</th> <th>Vocabulary Size</th> <th>Rationale</th> </tr> </thead> <tbody> <tr> <td><strong>English-only (RAG, chat)</strong></td> <td>Standard BPE</td> <td>32k–50k</td> <td>Compact embeddings, max speed</td> </tr> <tr> <td><strong>Multilingual (CJK heavy)</strong></td> <td>Large vocab BPE/Unigram</td> <td>100k–200k</td> <td>Avoids character-level fallback, reduces TpW</td> </tr> <tr> <td><strong>Code generation</strong></td> <td>Whitespace-aware BPE</td> <td>50k–80k</td> <td>Preserves indentation structure, lossless round-trip</td> </tr> <tr> <td><strong>Domain-specific</strong></td> <td>Extend existing tokenizer</td> <td>+5k–10k terms</td> <td>Add medical/legal jargon to prevent over-fragmentation</td> </tr> </tbody> </table> <hr/> <p><strong>Rules of thumb</strong>:</p> <ul> <li>If TpW doubles vs baseline, treat it as an infrastructure issue — API cost doubles, context halves</li> <li>Vocabulary scaling: ~60k vocab is optimal for 7B models, while ~200k is justified for ~70B models. Beyond ~200k vocabulary, embedding memory and serving cost grow linearly, while compression gains become marginal (&lt;5%). Rare tokens (&lt;0.0001% frequency) remain under-trained even in large vocabularies</li> </ul> <h2 id="production-failure-modes">Production failure modes</h2> <h3 id="1-whitespace-boundary-mismatch">1. Whitespace boundary mismatch</h3> <p><strong>Symptom</strong>: Model generates awkwardly or fails to continue prompts correctly.</p> <p><strong>Cause</strong>: Tokenizers distinguish <code class="language-plaintext highlighter-rouge">"word"</code> and <code class="language-plaintext highlighter-rouge">" word"</code> as different tokens. If a prompt ends with a space, the model expects the next token to merge with that space. But tokenization already split them — breaking continuation patterns learned during training.</p> <p><strong>Mitigation</strong>: Token Healing (re-tokenize boundary) or template hygiene (avoid trailing spaces, use explicit markers).</p> <h3 id="2-inconsistent-digit-splitting">2. Inconsistent digit splitting</h3> <p><strong>Symptom</strong>: Degraded performance on structured identifiers (IDs, SKUs, timestamps).</p> <p><strong>Cause</strong>: BPE tokenizes <code class="language-plaintext highlighter-rouge">710</code> as one token, <code class="language-plaintext highlighter-rouge">711</code> as two (<code class="language-plaintext highlighter-rouge">7</code>, <code class="language-plaintext highlighter-rouge">11</code>); digit boundaries are unpredictable, breaking place-value consistency.</p> <p><strong>Mitigation</strong>: Digit isolation (pre-tokenization regex) or right-to-left tokenization, which can substantially improve multi-digit reasoning.</p> <h3 id="3-glitch-tokens-under-trained-embeddings">3. Glitch tokens (under-trained embeddings)</h3> <p><strong>Symptom</strong>: Specific strings cause hallucinations, refusals, or infinite loops.</p> <p><strong>Cause</strong>: Token exists in vocabulary but was filtered from model’s training corpus; embedding remains near random initialization. These tokens can be detected via embedding-weight signals and tokenizer configuration audits.</p> <p><strong>Mitigation</strong>: Audit token frequency (&lt;100 occurrences in 1GB corpus are risky), normalize inputs to canonical forms.</p> <p>Detect glitch token candidates:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Detect glitch token candidates
</span><span class="kn">from</span> <span class="n">collections</span> <span class="kn">import</span> <span class="n">Counter</span>
<span class="n">token_freq</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">large_corpus</span><span class="p">))</span>
<span class="n">rare_tokens</span> <span class="o">=</span> <span class="p">[</span><span class="n">tid</span> <span class="k">for</span> <span class="n">tid</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">)</span> <span class="k">if</span> <span class="n">token_freq</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">tid</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span> <span class="o">&lt;</span> <span class="mi">100</span><span class="p">]</span>
</code></pre></div></div> <h3 id="4-unicode-normalization-traps">4. Unicode normalization traps</h3> <p><strong>Symptom</strong>: Visually identical text produces different outputs; safety filters bypassed.</p> <p><strong>Cause</strong>: Homoglyphs (Cyrillic <code class="language-plaintext highlighter-rouge">а</code> vs Latin <code class="language-plaintext highlighter-rouge">a</code>) tokenize differently despite visual identity; used for prompt injection.</p> <p><strong>Mitigation</strong>: Apply NFKC normalization before tokenization (trade-off: may alter legitimate rare Unicode)</p> <p>Apply normalization:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">unicodedata</span>
<span class="n">normalized</span> <span class="o">=</span> <span class="n">unicodedata</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="sh">'</span><span class="s">NFKC</span><span class="sh">'</span><span class="p">,</span> <span class="n">user_input</span><span class="p">)</span>
</code></pre></div></div> <h2 id="minimal-benchmark-recipe">Minimal benchmark recipe</h2> <p>Test compression efficiency across representative data types:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">tiktoken</span>

<span class="c1"># Replace with your encoding (e.g. cl100k_base for OpenAI)
</span><span class="n">encoding_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">your-encoding</span><span class="sh">"</span>
<span class="n">enc</span> <span class="o">=</span> <span class="n">tiktoken</span><span class="p">.</span><span class="nf">get_encoding</span><span class="p">(</span><span class="n">encoding_name</span><span class="p">)</span>

<span class="n">samples</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">english</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">The quick brown fox jumps over the lazy dog.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">russian</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Быстрая коричневая лиса перепрыгивает через ленивую собаку.</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">chinese</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">敏捷的棕色狐狸跳过懒狗。</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">japanese</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">素早い茶色の狐が怠けた犬を飛び越える。</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">code</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">def process(data):</span><span class="se">\n</span><span class="s">    return [x for x in data]</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">json</span><span class="sh">"</span><span class="p">:</span> <span class="sh">'</span><span class="s">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="s">: </span><span class="sh">"</span><span class="s">active</span><span class="sh">"</span><span class="s">, </span><span class="sh">"</span><span class="s">count</span><span class="sh">"</span><span class="s">: 42}</span><span class="sh">'</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">logs</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">[2024-01-15 10:23:45] ERROR: Connection timeout</span><span class="sh">"</span>
<span class="p">}</span>

<span class="k">for</span> <span class="n">label</span><span class="p">,</span> <span class="n">text</span> <span class="ow">in</span> <span class="n">samples</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
    <span class="n">tokens</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">enc</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">text</span><span class="p">))</span>
    <span class="n">words</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">text</span><span class="p">.</span><span class="nf">split</span><span class="p">())</span>
    <span class="n">chars</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">text</span><span class="p">)</span>
    <span class="n">tpw</span> <span class="o">=</span> <span class="n">tokens</span> <span class="o">/</span> <span class="n">words</span> <span class="k">if</span> <span class="n">words</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="k">else</span> <span class="mi">0</span>
    <span class="n">cpt</span> <span class="o">=</span> <span class="n">chars</span> <span class="o">/</span> <span class="n">tokens</span> <span class="k">if</span> <span class="n">tokens</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="k">else</span> <span class="mi">0</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">label</span><span class="si">:</span><span class="mi">8</span><span class="si">}</span><span class="s"> </span><span class="si">{</span><span class="n">tokens</span><span class="si">:</span><span class="mi">3</span><span class="si">}</span><span class="s"> tok  </span><span class="si">{</span><span class="n">tpw</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s"> TpW  </span><span class="si">{</span><span class="n">cpt</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="s"> CpT</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Expected ranges</strong>:</p> <hr/> <table> <thead> <tr> <th>Category</th> <th>TpW</th> <th>CpT</th> </tr> </thead> <tbody> <tr> <td>English</td> <td>1.2–1.4</td> <td>3.3–4.0</td> </tr> <tr> <td>Russian</td> <td>1.4–1.8</td> <td>2.5–3.3</td> </tr> <tr> <td>Chinese</td> <td>—</td> <td>1.4–2.0</td> </tr> <tr> <td>Japanese</td> <td>—</td> <td>1.4–2.0</td> </tr> <tr> <td>Code</td> <td>1.5–2.0</td> <td>1.7–2.5</td> </tr> </tbody> </table> <hr/> <h2 id="when-to-train-custom-tokenizer">When to train custom tokenizer</h2> <p><strong>Do train custom</strong> when:</p> <ul> <li>Target language TpW &gt; 2.5 with standard tokenizers (TpW can drop substantially with custom tokenizer depending on corpus and vocab size)</li> <li>Domain-specific corpus &gt;100MB with high OOV rate (medical terms, legal jargon)</li> </ul> <p><strong>Vocabulary extension pattern</strong> (for fine-tuning):</p> <p>When extending vocabulary for domain-specific terms, initialize new embeddings as averages of constituent tokens:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">new_tokens</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">cytokine</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">interleukin</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">CD4+</span><span class="sh">"</span><span class="p">]</span>
<span class="n">tokenizer</span><span class="p">.</span><span class="nf">add_tokens</span><span class="p">(</span><span class="n">new_tokens</span><span class="p">)</span>
<span class="n">model</span><span class="p">.</span><span class="nf">resize_token_embeddings</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">))</span>

<span class="c1"># Initialize as average of constituent tokens (converges 5–10× faster)
</span><span class="k">for</span> <span class="n">token</span> <span class="ow">in</span> <span class="n">new_tokens</span><span class="p">:</span>
    <span class="n">token_id</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">convert_tokens_to_ids</span><span class="p">(</span><span class="n">token</span><span class="p">)</span>
    <span class="n">original_ids</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">token</span><span class="p">,</span> <span class="n">add_special_tokens</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
    <span class="n">model</span><span class="p">.</span><span class="n">embeddings</span><span class="p">.</span><span class="n">weight</span><span class="p">[</span><span class="n">token_id</span><span class="p">]</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">embeddings</span><span class="p">.</span><span class="n">weight</span><span class="p">[</span><span class="n">original_ids</span><span class="p">].</span><span class="nf">mean</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
</code></pre></div></div> <hr/> <p align="center"> <i>Tokenization determines the exchange rate between human text and computational resources. Efficient tokenizers reduce API costs 2–5× and preserve context capacity.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Compact reference for tokenizer selection, metrics, and failure modes in production LLM systems.]]></summary></entry><entry><title type="html">The metric gap: bridging business outcomes and AI component optimization</title><link href="https://avchauzov.github.io/blog/2025/metric-gap-e2e-evaluation/" rel="alternate" type="text/html" title="The metric gap: bridging business outcomes and AI component optimization"/><published>2025-12-19T00:00:00+00:00</published><updated>2025-12-19T00:00:00+00:00</updated><id>https://avchauzov.github.io/blog/2025/metric-gap-e2e-evaluation</id><content type="html" xml:base="https://avchauzov.github.io/blog/2025/metric-gap-e2e-evaluation/"><![CDATA[<p>Engineering teams often face an observability disconnect: despite having dashboards for embedding latency, RAGAS scores, and reranker precision, production deployments with improved component metrics frequently fail to lift user satisfaction.</p> <p>Here’s a common pattern:</p> <ol> <li>Days are spent on optimizing a specific component (e.g., improving reranker precision by 5%)</li> <li>The update is deployed to production</li> <li><strong>User satisfaction metrics (Customer Satisfaction or Resolution Rate) remain flat</strong></li> </ol> <p>The problem isn’t a lack of validation; it is validating in a vacuum. Component metrics are often treated as the goal rather than as diagnostic signals for the actual product.</p> <p><strong>End-to-End</strong> evaluation is not just a final quality check; it is a <strong>prioritization engine</strong>. By linking business outcomes (what the user cares about) to component metrics (what you control), you can identify which technical improvements will actually impact the user experience.</p> <h2 id="the-probability-leak">The probability leak</h2> <p>AI pipelines are probabilistic chains.</p> <pre><code class="language-mermaid">flowchart LR
    A([User query]) --&gt;|100%| B["Embedding model&lt;br/&gt;95% accuracy"]
    B --&gt;|95% flow| C["Retrieval&lt;br/&gt;90% recall"]
    C --&gt;|85.5% flow| D["Reranker&lt;br/&gt;92% precision"]
    D --&gt;|78.7% flow| E[LLM generation]
    E --&gt; F(["User outcome&lt;br/&gt;~78% success"])
</code></pre> <p>The diagram illustrates error compounding in a typical RAG pipeline. Each component’s failure probability multiplies, creating a “probability leak” where system-level performance degrades faster than component metrics suggest.</p> <p>This creates a hidden cost inefficiency. If the embedding model fails to capture the user’s intent, the downstream components (reranker and LLM) are processing noise.</p> <p>Downstream improvements cannot recover upstream information loss.</p> <p>This failure mode requires systematic diagnosis. The Drill-Down methodology provides a framework for isolating bottlenecks by working backwards from business metrics.</p> <h2 id="the-methodology-drill-down-evaluation">The methodology: drill-down evaluation</h2> <p>To avoid wasted engineering effort, evaluation should work <strong>backwards</strong> from the business metric. Do not start by asking “How do we improve retrieval?” Start by asking “Why is the Task Success Rate low?” and drill down until the technical bottleneck is isolated.</p> <pre><code class="language-mermaid">flowchart TD
    A(["Satisfaction &lt; 4.0"]) --&gt; B{"Task success low?"}
    B --&gt;|Yes| H1[Hypothesis: intent routing]
    B --&gt;|No| C{"Answer quality low?"}
    C --&gt;|Yes| H2[Hypothesis: context or hallucination]
    C --&gt;|No| D{"Latency or cost high?"}
    D --&gt;|Yes| H3[Hypothesis: retrieval or model size]
    H1 --&gt; I1[Inspect intent classifier]
    H2 --&gt; I2[Check recall vs LLM adherence]
    H3 --&gt; I3[Profile vector search]
    I1 --&gt; O[Optimize component]
    I2 --&gt; O
    I3 --&gt; O
    O --&gt; RE([Re-evaluate E2E])
</code></pre> <h3 id="step-1-defining-success-criteria">Step 1: defining success criteria</h3> <p>Effective evaluation requires metrics that proxy user value, not just system output.</p> <p><strong>Output Metrics (Operational)</strong>:</p> <ul> <li>Tokens generated</li> <li>Latency per chunk</li> <li>Number of docs retrieved</li> </ul> <p><strong>Outcome Metrics (Business)</strong>:</p> <ul> <li>Code Generation: User copy-paste rate for generated code blocks</li> <li>Support: Conversations that end without a “talk to human” escalation</li> <li>Search: Citation click-through rate</li> </ul> <h3 id="step-2-the-golden-dataset">Step 2: the golden dataset</h3> <p>Outcomes cannot be evaluated with simple unit tests. A “Golden Dataset” must capture <strong>intent</strong> and <strong>business constraints</strong>.</p> <p>Instead of complex evaluator classes, structure test data to reflect business needs:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"query"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Can I return the laptop if I opened it?"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"business_constraints"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"must_contain"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"restocking fee"</span><span class="p">,</span><span class="w"> </span><span class="s2">"14-day window"</span><span class="p">],</span><span class="w">
    </span><span class="nl">"must_refuse_if"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"damaged by user"</span><span class="p">],</span><span class="w">
    </span><span class="nl">"tone"</span><span class="p">:</span><span class="w"> </span><span class="s2">"empathetic but firm"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"expected_outcome"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Policy_Clarification_Success"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The outcome should be a binary Pass/Fail check: the test passes only if all business constraints are met.</p> <p><strong>Note on Evaluation Methods</strong>: Avoid relying solely on LLM-as-a-Judge for E2E metrics. Use deterministic checks where possible: JSON schema validation, entity extraction, user behavior signals (copy-paste rate, click-through). LLM-as-a-Judge is useful for nuanced tasks, but it introduces variance and cost. Prefer rule-based evaluators for binary outcomes (e.g., “contains required field”, “response time &lt; threshold”).</p> <h3 id="step-3-correlation-and-conditional-analysis">Step 3: correlation and conditional analysis</h3> <p>This is the critical diagnostic step. Once E2E results and component logs are collected, the goal is to find which component metric correlates with E2E failure.</p> <p><strong>Scenario A</strong>: Direct Correlation If <code class="language-plaintext highlighter-rouge">Retrieval Recall</code> has a high correlation (e.g., 0.7) with <code class="language-plaintext highlighter-rouge">Task Success</code>, optimizing the embedding model is the correct priority.</p> <p><strong>Scenario B</strong>: The Dependency Trap (Conditional Analysis) In sequential chains, direct correlation can be misleading. A reranker might be performing perfectly (giving high scores to the “best” available docs), but if the Retrieval step only returned irrelevant documents, the User Outcome will be 0.</p> <p>If you simply calculate <code class="language-plaintext highlighter-rouge">Correlation(Reranker_Score, User_Outcome)</code>, you might see near-zero correlation and wrongly assume the reranker is irrelevant.</p> <p><strong>Solution</strong>: Apply conditional analysis. Evaluate the reranker’s impact only on the subset of queries where Retrieval was successful (Retrieval_Recall &gt; 0.8).</p> <h3 id="case-study-the-iteration-trade-off">Case study: the iteration trade-off</h3> <p>Consider a Deep Research Agent where the engineering team must determine the optimal number of research loop iterations (1, 2, or 3).</p> <p><strong>The Engineering View</strong>: “3 iterations are superior. The system gathers 40% more documents, and Retrieval Recall hits 0.95.”</p> <p><strong>The Business/E2E View</strong>: Applying Drill-Down evaluation against User Satisfaction and Cost reveals a different reality:</p> <hr/> <table> <thead> <tr> <th>Iterations</th> <th>Retrieval Recall</th> <th>Satisfaction</th> <th>Cost</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>0.75</td> <td>6.2/10</td> <td>$0.12</td> </tr> <tr> <td><strong>2</strong></td> <td><strong>0.88</strong></td> <td><strong>8.4/10</strong></td> <td><strong>$0.28</strong></td> </tr> <tr> <td>3</td> <td>0.95</td> <td>8.5/10</td> <td>$0.58</td> </tr> </tbody> </table> <hr/> <p><strong>The Insight</strong>: Moving from 1 to 2 iterations yields a significant jump in satisfaction (+35%). Moving from 2 to 3 yields negligible user value (+1%) but <strong>doubles the cost</strong>. Without E2E metrics, the team might ship “Iteration 3” based on superior recall scores, unknowingly degrading the product’s unit economics.</p> <h3 id="technical-implementation-lean-instrumentation">Technical implementation: lean instrumentation</h3> <p>A minimal logging pattern is sufficient to enable this analysis. The key is to store component metrics alongside outcome metrics in a flat structure.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Minimal structure for Drill-Down analysis
</span><span class="nf">log_event</span><span class="p">({</span>
    <span class="sh">"</span><span class="s">trace_id</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">abc-123</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">component_metrics</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">retrieval_score</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.85</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">latency_ms</span><span class="sh">"</span><span class="p">:</span> <span class="mi">450</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">token_count</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1200</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">outcome_metrics</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">user_rating</span><span class="sh">"</span><span class="p">:</span> <span class="mi">5</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">task_resolved</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">est_cost_usd</span><span class="sh">"</span><span class="p">:</span> <span class="mf">0.04</span>
    <span class="p">}</span>
<span class="p">})</span>

</code></pre></div></div> <p>By storing this data, you can run the necessary SQL/Pandas analysis to detect bottlenecks without complex observability platforms.</p> <p><strong>Example SQL Query for Correlation Analysis</strong>:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">-- Find impact of reranker when retrieval succeeds</span>
<span class="k">SELECT</span>
    <span class="n">retrieval_score</span><span class="p">,</span>
    <span class="k">AVG</span><span class="p">(</span><span class="k">CASE</span> <span class="k">WHEN</span> <span class="n">task_resolved</span> <span class="k">THEN</span> <span class="mi">1</span><span class="p">.</span><span class="mi">0</span> <span class="k">ELSE</span> <span class="mi">0</span><span class="p">.</span><span class="mi">0</span> <span class="k">END</span><span class="p">)</span> <span class="k">as</span> <span class="n">success_rate</span>
<span class="k">FROM</span> <span class="n">log_events</span>
<span class="k">WHERE</span> <span class="n">retrieval_score</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">.</span><span class="mi">8</span>  <span class="c1">-- Conditional: only when retrieval works</span>
<span class="k">GROUP</span> <span class="k">BY</span> <span class="n">retrieval_score</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">success_rate</span> <span class="k">DESC</span><span class="p">;</span>
</code></pre></div></div> <p>This query shows how to apply conditional analysis: it evaluates correlation between component metrics and outcomes, filtering to cases where upstream dependencies succeeded. This avoids the dependency trap described in Step 3.</p> <h3 id="summary">Summary</h3> <ol> <li><strong>Component scores mask system failures</strong>. High precision in one component does not guarantee a successful system if the input to that component is flawed</li> <li><strong>Work backwards</strong>. Start with the Success Criteria (User Satisfaction/Task Success). If it falls below target, drill down to find the specific component causing the bottleneck</li> <li><strong>Handle dependencies</strong>. When analyzing pipelines, remember that downstream performance is conditional on upstream success</li> <li><strong>Prioritize via impact</strong>. Only optimize components whose metrics demonstrably correlate with business outcomes</li> </ol> <hr/> <p align="center"> <i>System value exists only at the End-to-End level. Optimization without this context is effectively tuning parameters at random.</i> </p>]]></content><author><name></name></author><summary type="html"><![CDATA[Why high component scores often mask system failures. A methodology for using E2E evaluation to prioritize engineering work.]]></summary></entry></feed>